def maiores(lista, n):
    ''' função que retorna numeros da lista maiores que n
        list, int ---> list '''
    list.append(lista, n)
    list.sort(lista, reverse=True)
    a = list.index(lista, n)
    if a == 0:
        b = lista[0:0]
        return list.reverse(b)
    if a == 1:
        b = lista[0:1]
        return list.reverse(b)
    else:
        b = lista(0:list.index(lista,n)
        return list.reverse(b)