def maiores(lista,n):
    if lista[0] < n:
        lista.remove(lista[0])
    elif lista[0] < n:
        lista.remove(lista[0])
    elif lista[0] < n:
        lista.remove(lista[0])
    elif lista[0] < n:
        lista.remove(lista[0])
    lista.sort()
    return lista