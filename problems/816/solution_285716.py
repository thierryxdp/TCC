def maiores(lista, n):
    ''' função que retorna numeros da lista maiores que n
    	list, int ---> list '''
    list.append(lista, n)
    list.sort(lista)
    len(lista)
    a = list.index(lista, n)
    b = len(lista) - a
    return a[0:b]