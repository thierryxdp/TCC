def maiores(listaint,n):
    'dada uma lista de números inteiros e um número inteiro n retorne outra lista que contenha todos os números maiores que n'
    list.append(listaint,n)
    list.sort(listaint)
    x= list.index(listaint,n)
    return listaint[x: ]