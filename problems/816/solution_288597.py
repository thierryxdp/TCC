def maiores(list,n):
    """função que recebe uma lista e um numero e avalia quais numeros da lista são maiores que o numero n. list-> list"""
    l.fin = []
    for i in range(len(list))
        if list[i] > n:
           l.fin.append(list[i])
    l.fin.sort()
    return l.fin