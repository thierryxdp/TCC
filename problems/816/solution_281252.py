def inserir (lista_numeros, n):
    '''recebe uma lista de numeros inteiros e um numero inteiro n, retorna uma nova lista com apenas os numeros maiores que n'''
    '''list->list'''
    lista_numeros= lista
    um = list.append(lista, n)
    list.sort(lista)
    return lista_numeros

def maiores (inserir):
    indice = lista [n:]
    if n in lista:
        return indice
    else:
        return []