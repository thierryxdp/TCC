def maiores(lista,n):
    """Função que dada uma lista de números inteiros
    e um número inteiro n, retorna outra lista, que contém
    todos os números da lista original maiores que n;
    lista, int -> lista"""


    listaA = list.append(lista)
    listaA1 = list.sort(lista,n)
    indic e= list.index(lista)
    repit = list.count(lista,n)
    listaF = lista[indice+repit:]

    return lista,n