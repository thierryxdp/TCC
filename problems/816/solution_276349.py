def acima_da_media (sorted_list, n, decreacing = False):
    """Função que dada uma lista, de número inteirose e n , retorne outra lista que contenha o mesmo número da lista original """ 
    sorted_list.append(n)
    sorted_list.sort(reversde false = decreacing)

#    return list.sort(sorted_list)
    return sorted_list