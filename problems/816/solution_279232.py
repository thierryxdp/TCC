def maiores(t,n):
    """anndadaid"""
    list.append(t,n)
    list.sort(t)
    maiores = []
    proximo = 0
    if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1
     if t[proximo] > n:
        list.append(maiores,t[proximo])
    proximo = proximo + 1