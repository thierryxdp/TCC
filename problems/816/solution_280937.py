def maiores(lista,n):
    """Função que dada uma lista de números inteiros
       e um número inteiro n, retorna uma lista com
       os números da lista original maiores que n.
       list->list"""
    listaoriginal[]
    novalista[n]
    list.append(listaoriginal,novalista)
    list.sort(novalista) 
    return novalista