def maiores(lista_int,n):
    """Retorna uma lista que contém todos os números da lista original
       maiores que n
       list, int --> list"""
    lista_int = ()
    if lista_int[:] > n:
        newLista = lista_int[:] 
        return newLista
    else: 
        return newLista