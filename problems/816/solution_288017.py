def maiores(lista,n):
    sorted(lista, key=int)
    list.sort(lista)
    lista_final = []
    for i in lista:
        if i[] > n:
            lista_final.append(i)
            return lista_final
        else:
            return []