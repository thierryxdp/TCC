def maiores(lista_numero,n):
    '''retorna lista de numeros só que apenas os numeros maiores que n'''
    list.append(lista_numero, n)
    list.sort(lista_numero)
    lista_numero
    maiores = lista_numero[len(n)]:
    return maiores