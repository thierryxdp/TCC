def maiores(listacompleta, n):
    '''função que mostraapenas os numeros de uma lista que são maiores que n. list -> list'''
	contagem = len(listacompleta.split())
    for i in contagem:
        if [n]>(listacompleta[]):
            return []
        if [n]<(listacompleta[]):
            list.sort(listacompleta)
            return listacompleta