def maiores(ls, n):
	"""
	maiores ([1, 2, 10, 17, 19], 18) -- [17,19]
	"""
	list.sort(ret)
    if n in ls:
    	pos = list.index(ls, n)
        qts = list.count(ls, n)
		return 1s[pos + qts :]
    return -1