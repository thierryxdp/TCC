def maiores(dados):
    """Função que, dada uma lista de números inteiros e um número 'n', retorna
outra lista que contenha apenas os números da lista maiores que 'n'."""
    """list-->list"""
    resultado=[]
    for c in range(0,20):
        if c > dados[-1]:
            resultado= resultado+resultado.append(c)
            return resultado