def maiores(lista_int,n):
    """Funcao que recebe uma lista de inteiros e um numero n inteiro, e retorna
outra lista que contenha todos os numeros da lista original maiores que n"""
    list.append(lista_int,n)
    list.sort(lista_int)
    return lista_int[n::]