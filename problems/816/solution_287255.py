def maiores(ls,n):
    """
    retorna os elementos de ls que são maiores que n em ordem 
    crescente,  ls(int),int->ls(int)
    """
    i=[]
    for x in ls:
        if:
            x>n
        i+=x
    return i