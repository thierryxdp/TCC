def maiores(l,n):
    """Para retornar uma lista que contenha todos os
    números, os maiores da lista original, digite;
    int,int->int"""
    list.append(l,n)
    l=sorted(l)
    a=list.index(l,n)
    return lista