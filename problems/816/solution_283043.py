def maiores(x,num): 
    [] = lsita
    if x[0] > num:
        list.insert[lista,-1,num]
    if x[1] > num:
        list.insert[lista,-1,num]
    if x[2] > num:
        list.insert[lista,-1,num]
    if x[3] > num:
        list.insert[lista,-1,num]
    return list.sort(lista)