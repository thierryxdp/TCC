def maiores(lista_numeros,n):
    """auhyadhaduh"""
    lista_vazia = []
    list.extend(lista_vazia,lista_numeros)
    list.append(lista_numeros,n)
    list.sort(lista_numeros)
    if x in lista_numeros > n:
        return lista_numeros[x]
    else:
        return