def faltante(listaL):
    
    listaL.sort()
    i = 0
    L1 = listaL[0]
    L2 = len(listaL) + 1
    listaCop = list(range(L1, L2)
    
    if listaL == listaCop:
    	return listaCop[-1] + 1
                    
    while listaComp[i] in listaL:
       	i = i + 1
                    
    return listaComp[i]