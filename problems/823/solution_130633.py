def faltante(L)
    list.sort(L)
    y=0
    resp=0
    for x in L:
        if L[y]+1==L[y+1]
            y+=1
        else:
            resp=x+1
            break
    if resp==0
        resp=1
    return resp