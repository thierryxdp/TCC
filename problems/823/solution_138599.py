def faltante(lista=[]):
    '''função que dada uma lista com
    n-1 inteiros numerados de 1 a n,
    descobre qual inteiro do intervalo
    está faltando
    lista -> int'''
    lista_ideal = L
    lista_atual = (1,n)
    list.append(1,n)
    list.sort(x)
    
    for item in lista_atual :
        if item is not in lista_ideal :
            return x