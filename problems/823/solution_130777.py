def faltante(lista):
    num_pecas=len(lista)+1
    lista.sort(lista)
    c=0
    while c<pecas:
        if c+1 not in lista:
            return c+1
        if lista[c]!=c+i:
            return c+1
        else:
            return c+1