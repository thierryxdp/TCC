def faltante(numeros):
    '''função que retorna o número inteiro que não está na lista, dado a lista de números;list->int'''
    indice=0
    faltante=0
    while indice<len([list.sort(numeros)]):
        if [list.sort(numeros)][indice]!=[list.sort(numeros)][indice+1]-1:
            faltante=faltante+indice+2
        indice=i+1
    return faltante