def faltante(lista):
    """Dada uma lista com n-1 elementos sendo numeros inteiros, retorna o numero que estiver faltando na sequencia"""
    """entrada: lista
    saida: int"""
    
    pos=0
    diferenca=0
    
    while pos<len(lista)
    	if lista[pos]-pos==2:
        	diferenca=diferenca+1
        pos=pos+1
        
    if diferenca==0:
        return lista[-1]+1
    else:
        return lista[-1]-diferenca