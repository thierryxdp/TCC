def faltante(lista):
    """Função que dada uma lista retorna qual int está faltando,lista-->int"""
    lista.append(0)
    list.sort()
    i=0
    resp=0
    while i <= len(lista):
        if i != lista[i-1] and i!= =):
            resp=1
        i+=1
    return resp