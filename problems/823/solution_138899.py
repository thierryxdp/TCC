def falante(lista):
    i=0
    s=0
    n=0
    soma = (((1+(len(lista)+1))*(len(lista)=1))/2)
    list.sort(lista):
    while i<len(lista):
        if sum(lista) != soma:
            s= (soma)-sum(lista)
        i=i+1
        n=n+1
    return s