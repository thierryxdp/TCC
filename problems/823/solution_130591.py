def faltante(lista):
    i=0
    b=lista[-1]
    a=list(range(b))
     
    while i<len(a[1:]):
        if a[i]!=lista[i]:
            return x=a[i]
        else:
            return i=i+1
    
    return x