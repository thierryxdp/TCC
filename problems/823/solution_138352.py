def faltante (x):
    lista_ideal = L
    lista_presente = (1,n)
    list.append(1,n)
    list.sort(x)
    
    for item in lista_presente :
        if item não está contido em lista_ideal:
        return x