def real(numero):
    '''funcao que retorna o fatorial de um numero'''
    i=(len(numero)+1)
    lista=[]
    while i>0:
        list.append(lista,i)
        i=i-1
    return lista
def faltante(numero):
    j=0
    while j<=(len(numero)):
        if numero[j] not in real(numero) and j<len(numero):
            return numero[j]
        elif j=len(numero):
            return len(numero)
        j=j+1