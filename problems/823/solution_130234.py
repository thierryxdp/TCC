def faltante(lista):
    resultado=0
    i=0
    x=list(range(1,lista[-1]+1))
    if x==lista:
    	return lista[-1]+1
    while i<len(lista)+1:
        if x!=lista:
        	
        i=i+1
    return x