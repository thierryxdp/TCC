def faltante(numeros):
    """Dada uma lista sequencial retorna o número que falta.
    	lista -> int"""
    i = 0
    falta = 0
    comparador = 1
    while i < len(numeros):
        
        if numeros[i] != comparador
        	falta = i + 1
            break
            
        i += 1
        comparador += 1
    
    return falta