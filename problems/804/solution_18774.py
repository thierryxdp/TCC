def  filtra_pares(a, b, c, d):
    
    '''Função para determinar os ementos pares'''
    x = int(input( a, b, c, d))
    y = [],[]
    y0 = x % 2 ==0
    
    if y0 != 0
        return list(y0)
    else:
        return ( )