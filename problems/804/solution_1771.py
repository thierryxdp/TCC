def filtra_pares(tupla):
    '''Calcula e retorna uma tupla apenas com os elementos pares e na mesma ordem da tupla original
    int,int,int,int -> int'''
    tupla = (a, b, c, d)
    tupla_filtrada = filter(tupla, %!=0)
    lista(list(tupla_filtrada)
    return lista