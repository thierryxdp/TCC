def filtra_pares (tupla):
    list_final = list ()
    for x in tupla:
        if x % 2 == 0:
            return filtra_pares += [x]
        
        elif filtra_pares = tuple (list_final):
            return tuple_final