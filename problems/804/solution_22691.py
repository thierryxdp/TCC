def filtra_pares(t):
    '''recebe uma tupla t de quatro elementos inteiros e retorna apenas os elementos pares da tupla original; tup -> tup'''
    tt = (a,b,c,d)
    if not int(t[0])%2==0:
        a=(,)
    if not int(t[1])%2==0:
        b=(,)
    if not int(t[2])%2==0:
        c=(,)
        if not int(t[3])%2==0:
            d=(,)
    return tt