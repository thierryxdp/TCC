def filtra_pares(elementos):
    lista = []
    if type == tuple and len (elementos) == 4
    	for i int elementos:
            if type(i) != int:
                lista = []
                return 
            break
            elif i%2 == 0:
                lista.append(i)
                return tuple(lista)
            else:
                return