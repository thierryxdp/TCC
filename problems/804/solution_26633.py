def filtra_pares(numeros):
    'Dado quatro num inteiros,a funcao retorna em ordem de entrada aqueles que forem par'
    'int,int,int,int->tupla'
    numeros(a,b,c,d)
    pares=()
    if numeros[0]%2==0:
    	 pares+=(numeros[0],)
    if numeros[1]%2==0:
    	pares+=(numeros[1],)
	if numeros[2]%2==0:
   		pares+=(numeros[2],)
	if numeros[2]%2==0:
   	    pares+=(numeros[2],)
    return pares