def filtra_pares():
    '''recebe uma tupla com 4 números inteiros e retorna
    apenas aqueles que são pares
    tupla -> tupla'''
    
    if a % 2 == 0
    filtra_pares += (a,)
    if b % 2 == 0
    filtra_pares += (b,)
    if c % 2 == 0
    filtra_pares += (c,)
    if d % 2 == 0
    filtra_pares += (d,)
    
    return tuple ()