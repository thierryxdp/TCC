def par(x):
    return x%2

def filtra_pares(tupa):
    if par(tupa[0])==0:
        return tupa[0],
    else:
        return ()
    if par(tupa[0])==0 and par(tupa[1])==0:
        return (str(tupa[0]),+str(tupa[1]))