def filtra_pares(a):
    '''Função que recebe uma tupla com 4 elemetos inteiros como
    parametro e retorna uma nova tupla contendo apenas os elementos
    pares da tupla original, na ordem dada
    '''
    elem1, elem2, elem3, elem4 = a 
    pares = tupla()
    if ele1[0]%2==0:
        tupla = (elem1[0],)
    if a[1]%2==0:
        tupla = (elem2[1],)
    if a[2]%2==0:
        tupla = (elem3[2],)
    if a[3]%2==0:
        tupla = (elem4[3],)
    return pares