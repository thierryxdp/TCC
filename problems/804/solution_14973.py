def filtra_pares(s):
    """função que retorna somente os números 
    pares da tupla original
    tuple=>tuple
    resultado_filter = filter(lambda filtragem: 
                              filtragem % 2 == 0, s)
    tuple_final = tuple(resultado_filter)  # transforma em tuple
    return tuple_final  # retornando tuple#Start your python function here"""
    s[0] = w
    s[1] = x
    s[2] = y
    s[3] = z
    if w%2==0:
        tupla_final=tupla_final +tuple(w)
    if x%2 == 0:
		tupla_final= tupla_final + tuple(x)
    if y % 2 == 0:
        tupla_final= tupla_final + tuple(y)
    if z % 2 ==0:
        tupla_final= tupla_final + tuple(z)
    
    return tupla_final