#Função que dada uma tupla com quatro elementos inteiros, filtra e retorna os pares na ordem
tup --> tup
def filtra_pares(t):
pares = ()
    if t%2==0(t[0]):
        pares = pares + (t[0],)
    if t%2==0(t[1]):
        pares = pares + (t[1],)
    if t%2==0(t[2]):
        pares = pares + (t[2],)
    if t%2==0(t[3]):
        pares = pares + (t[3],)
    return pares