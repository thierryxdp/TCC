#Start your python function here
def filtra_pares(a,b,c,d):
    f=()
    if (a)/2=int:
        f=('a')
    if (b)//2=int:
        f= f+('b')
    if (c)//2=int:
        f=f+('c')
    if (d)//2=int:
        f=f+('d')
    return f