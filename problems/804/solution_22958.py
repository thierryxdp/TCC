def e_par(x):
    if x%2==0:
        return x
    else:
        return
def filtra_pares(a,b,c,d):
    """usando o procedimento auxiliar acima, define quais dos elementos dados são pares, retornando-os se afirmativo e retornando none se constatado que são ímpares. Os valores são retornados na ordem em que são colocados na função."""
    return e_par(a),e_par(b),e_par(c),e_par(d)