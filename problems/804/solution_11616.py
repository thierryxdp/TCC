filtra_pares(tupla):
    tuplaX = ()
    if tupla[0] %2 == 0
 		tuplaX = tuplaX + (tupla[0],)
        return tuplaX
    if tupla[1] %2 == 0
 		tuplaX = tuplaX + (tupla[1],)
        return tuplaX
    if tupla[2] %2 == 0
 		tuplaX = tuplaX + (tupla[2],)
        return tuplaX
    if tupla[3] %2 == 0
 		tuplaX = tuplaX + (tupla[3],)
        return tuplaX