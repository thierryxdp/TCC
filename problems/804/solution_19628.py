def filtra_pares (tupla):
    '''função que recebe um tupla e devolve somente seus numeros pares; tupla->tupla'''
    tupla = (int(a),int(b),int(c),int(d))
    if (tupla[a])%2==0 and (tupla[b])%2==0 and (tupla[c])%2==0 and (tupla[d])%2==0:
        return (a,b,c,d)