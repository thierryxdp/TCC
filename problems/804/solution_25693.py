#Start your python function here
def filtra_pares(x,y,z,a):
    filtra_pares = []
    if type(filtra_pares) == tuple and len(filtra_pares)==4:
        for i in filtra_pares:
            if type(i) != int:
                lista = [] 
                return lista = []
    
    elif i%2 == 0: 
        lista.append(i) 
        return lista[] 
    
    else:
        return lista []