# Função que recebe quatro elementos inteiros e retorna uma nova tupla contendo apenas os elementos pares da tupla original
def filtra_pares(a,b,c,d):
    if (a/2)=int:
        return a
    else:
        if b/2=int:
            return b
        if c/2=int:
            return c
        if d/2=int:
            return d