def filtra_pares(x):
    "Retorne os pares de uma dada tupla; tupla->tupla"
    a = x[0] 
    b = x[1] 
    c = x[2]
    d = x[3]
    tf = ()
    if w%2!=1:
        tf=tf + (w,)
    if x%2 != 1:
		tf= tf + (x,)
    if y % 2 != 1:
        tf= tf + (y,)
    if z % 2 !=1:
        tf= tf + (z,)
    
    return tupla_final