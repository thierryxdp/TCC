def filtra_pares (t):
    pares=()
    
	if t[0]%2==0:
        pares=(pares[0],)
	if t[1]%2==0:
        pares=(pares[1],)
	if t[2]%2==0:
        pares=(pares[2],)
	if t[3]%2==0:
        pares=(pares[3],)
	
        return pares