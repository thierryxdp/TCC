def filtra_pares(valor)
'''funcao que filtra os elementos pares da tupla e os retorna na mesma ordem'''
par=()
if (valor[0])%2==0:
    par=par + (t[0],)
if (valor[1])%2==0:
    par=par + (t[1],)
if (valor[2])%2==0:
    par=par + (t[2],)
if (valor[3])%2==0:
    par=par + (t[3],)