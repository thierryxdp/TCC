#Start your python function here
def filtra_pares(a,b,c,d):
    pares = ()
    if a/2==0:
        pares = pares+ (a,)
	elif b/2==0:
        pares = pares+(b,)
	elif c/2==0:
        pares = pares+(c,)
	elif b/2==0:
        pares = pares+(d,)
        return pares