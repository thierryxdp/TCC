def filtra_pares(t):
    """ receba uma tupla com 4 elementos inteiros como  parâmetro, e retorne uma nova tupla original na mesma ordem. tup -> tup."""
    pares= t%2
    if t[0]=pares:
        return pares
    if t[1]=pares:
        return pares
    if t[2]=pares:
        return pares
    if t[3]=pares:
        return pares