#Start your python function here
def par(x):
    """informa se x é par"""
    return x%2=0

def filtra_pares(t):
    """retorna os valores pares da tupla"""
    tupla()
    
    if par(t[0]):
        tupla=tupla+(t[0],)
    if par(t[1]):
        tupla=tupla+(t[1],)
    if par(t[2]):
        tupla=tupla+(t[2],)
    if par(t[3]):
        tupla=tupla+(t[3],)
    return