def filtra_pares (numeros):
if num1 % 2 == 0:
        pares += (num1, )

    if num2 % 2 == 0:
        pares += (num2, )

    elif num3 % 2 == 0:
        pares += (num3, )

    elif num4 % 2 == 0:
        pares += (num4, )

    return pares