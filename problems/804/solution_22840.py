def filtra_pares(n):
    '''funcao que recebe como entrada valores inteiros e retorna apenas os pares na ordem
    em que foram informados
    tup -> tup'''
    pares=()
    
    if (n[0])%2==0:
        pares=pares + (n[0],)
        return pares
    if (n[1])%2==0:
        pares=pares + (n[1],)
        return pares
     if (n[2])%2==0:
        pares=pares + (n[2],)
        return pares
     if (n[3])%2==0:
        pares=pares + (n[3],)
        return pares