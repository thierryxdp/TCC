#Start your python function here
def filtro(a):
    '''retorna verdadeiro se o nuxero for par, e falso se for ixpar
       entrada: int
       saida: bool'''
    return a%2==2
def filtra_pares(a):
    '''recebe uxa tupla e cria outra tupla cox os elexentos pares da prixeira
       entrada= tupla
       saida= tupla'''
    pares=()
    if (t[0]):
        pares= pares+(t[0,])
    if (t[1]):
        pares= pares+(t[1,])
    if (t[2]):
        pares= pares+(t[2,])
    if (t[3]):
        pares= pares+(t[3,])
        return pares