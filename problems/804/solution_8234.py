"""recebe uma tupla com quatro elementos e retorna apenas os elementos pares da tupla
x, s, y, z ->int
int, int, int, int ->int"""
def filtra_pares(x,s,y,z):
    if x/2=int and s/2=int and y/2=int and z/2=int:
        return (x,s,y,z)