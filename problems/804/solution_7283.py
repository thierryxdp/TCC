def filtra_pares(x,y,z,i):
    """função que retorna os elementos pares da tupla
    int,int,int,int -> int,int"""
    if x // 2= 0:
        return x
    else:
        if y // 2=0:
            return y
        elif z//2=0:
            return z
        else i//2=0:
            return i