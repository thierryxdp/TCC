def filtra_pares(a,b,c,d):
    '''Função que retorna uma tupla com quatro elementos inteiros como parâmetro, e retorne uma nova tupla contendo apenas os elementos pares da tupla original, na mesma ordem que se encontravam; int, int, int ,int -> tupla'''
    
    pares = ((a%2)(b%2)(c%2)(d%2))
    if pares == 0:
        return (pares)