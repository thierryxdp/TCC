'''Função que recebe:
uma tupla de 4 elementos int
e retorna apenas os pares da tupla;
tupla(int, int, int, int)-> tupla'''
def filtra_pares(t):
    n = ()
    if t[0] % 2 == 0:
        tuplapar = n+(t[0],)
        return tuplapar
    if t[1] % 2 == 0:
        tuplapar = n+(t[1],)
        return tuplapar
    if t[2] % 2 == 0:
        tuplapar = n+(t[2],)
        return tuplapar
    if t[3] % 2 == 0:
        tuplapar = n+(t[3],)
        return tuplapar
    if t[4] % 2 == 0:
        tuplapar = n+(t[4],)