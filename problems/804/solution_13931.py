def filtra_pares([a,b,c,d]):
    """retorna uma nova tupla contendo apenas os elementos pares da tupla original;
    tuple -> tuple"""
    int(a)=a
    int(b)=b
    int(c)=c
    int(d)=d
    if a%2==0:
        a=par
    if b%2==0:
        b=par
    if c%2==0:
        c=par
    if d%2==0:
        d=par
    return par