def filtra_pares(tuplas):
    '''função que retorna uma tupla com quatro elementos
    inteiros como argumento,e retorne uma nova tupla
    contendo apenas os elementos pares da tupla original;
    tupla ->tupla'''
     pares=()
        if (tuplas[0]%2)==0:
            pares+=(tup[0],)
        if (tuplas[1]%2==0:
            pares+=(tup[1],)
        if (tuplas[2]%2==0):
            pares+=(tup[2],)
        if (tuplas[3]%2==0):
            pares+=(tup[3],)
            return pares