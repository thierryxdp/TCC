def filtra_pares (tupla):
    '''função que recebe uma tupla e devolve só os seus elementos pares; tupla->tupla'''
    tupla=()
    if tupla[0]%2==0:
        tupla=tupla+(p[0],)
    if tupla[1]%2==0:
        tupla=tupla+(p[1],)
    if tupla[2]%2==0:
        tupla=tupla+(p[2],)
    if tupla[3]%2==0:
        tupla=tupla+(p[3],)
    return tupla