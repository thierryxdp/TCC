def filtra_pares (numeros): 
    '''função que filtra elementos pares de uma certa tupla e retorna os pares em uma nova tupla
    tuple (int,int,int,int) -> tuple'''
    tuple (a,b,c,d) == numeros
    resultado = ()
    if numeros[0]%2==0:
    return resultado=resultado+(numeros[0],)