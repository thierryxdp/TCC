def par(a):
    if a%2==0:
        return True 
    else: 
        return False
    r = ([])
    def a(a):
        if par(a[0] == True:
         return r + [a[0],]
     else:
         return ([])
     def b(a):
        if par(a[1]) == True:
               

def filtra_pares(e)
  """Esta função recebe uma quatro numeros inteiros e retorna uma tupla
  somente em numeros pares.
  """