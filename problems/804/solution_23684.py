def filtra_pares(a, b, c, d):
    """Função retorna os valores pares na ordem imputada
    assinatura: int, int, int, int --> int, int
    """
    if a%2==0:
        return (a)
    	if b%2==0:
            return (a)+(b)
        	if c%c2==0:
            	return (a)+(b)+(c)
        			if d%2==0:
                		return (a)+(b)+(c)+(d)