def filtra_pares(a, b, c, d):
    """ retorna os numeros pares da tupla"""
    pares= ()
    if filtra_pares[0]%2==0:
        pares = pares +(filtra_pares[0],)
        if filtra_pares[1]%2==0:
            pares = pares + (filtra_pares[1])
            if filtra_pares [2]%2==0:
                pares = pares + (filtra_pares[2])
                if filtra_pares[3]%2==0:
                    pares = pares = (filtra_pares[3])
                    return pares