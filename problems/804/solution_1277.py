def filtra-pares(lista):
    """filtra apenas os números pares de uma lista"""
    par = list(lista)
    if (par[0])%2==0 and (par[1])%2==0 and (par[2])%2==0 and (par[3])%2==0:
        return par[0:]