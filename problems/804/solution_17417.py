#Start your python function here
def filtra_pares(x1, x2, x3, x4):
    ''' função chamada filtra pares que receba uma tupla com quatro elementos inteiros como
argumento, e retorne uma nova tupla contendo apenas os elementos pares da tupla original, na mesma
ordem em que se encontravam. '''
    nova_tupla = tuple ()
    
    if (t[0]%2) -- 0:
        nova_tupla = nova_tupla + (t[0],)
    if (t[1]%2) -- 0:
        nova_tupla = nova_tupla + (t[1],)
    if (t[2]%2) -- 0:
        nova_tupla = nova_tupla + (t[2],)
    if (t[3]%2) -- 0:
        nova_tupla = nova_tupla + (t[3],)
    return nova_tupla