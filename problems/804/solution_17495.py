def filtra_pares (tupla):

    a,b,c,d = tupla 

    if T[a] % 2 == 0:
    
        tupla_nova (a,)

    if T[b] % 2 == 0:

        tupla_nova (a,b,)

    if T[c] %  2 == 0:

        tupla_nova (a,b,c,)

    if T [d] % 2 == 0:

        tupla_nova (a,b,c,d,)