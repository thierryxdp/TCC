def par(x):
    return x%2


def filtra_pares(tupa):
    x=tupa[0]
    y=tupa[1]
    z=tup[2]
    w=tup[3]
    if par(tupa[0])==0:
        return x,
    else:
        return ()
    if par(tupa[0])==0 and par(tupa[1])==0:
        return (str(tupa[0]),+str(tupa[1]))