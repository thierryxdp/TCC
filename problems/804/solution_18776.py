def  filtra_pares(a, b, c, d):
    
    '''Função para determinar os ementos pares'''
    x = int(input( a, b, c, d))
    
    z = list(x) % 2 == 0
    np = ().join(z)
   
    if z != 0:
        
        return list(np)
    else:
        return ( )