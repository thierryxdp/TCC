def filtra_pares(objetos):
    objetos_pares = ()
    if objetos[0] % 2 ==0:
        objetos_pares = objetos_pares + (elementos[0])
    if objetos[1] % 2 ==0:
        objetos_pares = objetos_pares + (elementos[1])
    if objetos[2] % 2 ==0:
        objetos_pares = objetos_pares + (elementos[2])
    if objetos[3] % 2 ==0:
        objetos_pares = objetos_pares + (elementos[3])