#Start your python function here
def filtra_pares(x,y,z,w):
    """analisa os numeros e retorna somente os que sao par"""
    restox = x%2
    restoy = y%2    
    restoz = z%2    
    restow = w%2 
    
    if restox%2,restoy%2,restoz%2,restow%2:
        pares.append(num)