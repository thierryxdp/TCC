def filtra_pares(a,b,c,d):
    A = a % 2
    B  = b % 2
    C = c % 2
    D = d % 2
    if not bool(A)==True:
        return (a)
    else:
        return()
    if not bool(B)==True:
        return (b)
    else:
        return ()
    if not bool(C)==True:
        return c
    else:
        return ()
    if not bool(D)==True:
        return (d)
    else:
        return ()