def filtra_pares(tupla):
    """ Para fazer a filtragem de numeros pares, digite;
    int,int,int,int->int"""
    if len(tupla) > 0:
	tupla = lista.pop(0)
	elif tupla %2 == 0:
        return tupla, end= ''