def filtra_pares(objetos):
    objetos_pares = ()
    if objetos[0] % 2 ==0:
        objetos_pares = objetos_pares + (objetos[0],
    if objetos[1] % 2 ==0:
        objetos_pares = objetos_pares + (objetos[1],
    if objetos[2] % 2 ==0:
        objetos_pares = objetos_pares + (objetos[2],
    if objetos[3] % 2 ==0:
        objetos_pares = objetos_pares + (objetos[3],