def filtra_pares(a,b,c,d):
    """a"""
    x = tuple()
    """a"""
    if a%2 == 0: x += (a,)
    """a"""
    if b%2 == 0: x += (b,)
    """a"""
    if c%2 == 0: x += (c,)
    """a"""
    if d%2 == 0: x += (d,)
    """a"""
    return x
	"""a"""