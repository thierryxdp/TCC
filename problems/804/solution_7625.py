def filtra_pares(a):
    '''funcao que recebe uma tupla com 4 numeros inteiros e retorna uma nova tupla contendo apenas os elementor pares;tuple-->tuple'''
    if int(filtra_pares[0])%2==0:
        z=int(filtra_pares[0])
        if int(filtra_pares[1])%2==0:
            x=filtra_pares[1]
            if int(filtra_pares[2])%2==0:
                c=fultra_pares[2]
                if int(filtra_pares[3])%2==0:
                    v=filtra_pares[3]
                    return z,x,c,v