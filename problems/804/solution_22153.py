def par(x):
    return x%2==0
def filtra_pares(k):
    r=()
    if par(t[0])==True:
        r=r+t[0],
    if par(t[1])==True:
        r=r+t[1],
    if par(t[2])==True:
        r=r+t[2],
    if par(t[3])==True:
        r=r+t[3],
    return r