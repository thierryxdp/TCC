def filtra_pares(lista):
    """filtra os elementos pares de uma lista"""
    lista2 = []
    for valor in lista:
    	if valor % 2 == 0:
        	lista2.append(valor)
    		return filter(valor, lista)