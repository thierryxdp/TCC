def filtra_pares(a,b,c,d):
    '''Função que retorna uma nova tupla contendo apenas
    os elemetos pares da tupla original, na mesma ordem em que
    se encontravam.
    a=int,b=int,c=int,d=int->tuple'''
    lista = []
 	if tuple[0] % 2 == 0:
       lista.append(tuple[0])
    if tuple[1] % 2 == 0:
       lista.append(tuple[1])
    if tuple[2] % 2 == 0:
       lista.append(tuple[2])
    if tuple[3] % 2 == 0:
       lista.append(tuple[3])       
    return tuple(lista)