def filtra_pares(tupla):
    """função que retorna apenas elementos pares da tupla de entrada, de 4 elementos inteiros. tulpa->tupla"""
	a,b,c,d=tupla
    nova_tupla=[]
    a in new_list if a%2==0
    b in new_list if b%2==0
    c in new_list if c%2==0
    d in new_list if d%2==0
    return nova_tupla