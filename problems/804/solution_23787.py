def filtra_pares(t):
    ''' funcao que dada uma tupla retorna somente os numeros pares'''
    tup=()
    if (t[0],)/2) == int 
    tup= tup +(t[0],):
        elif (t[1],/2)==int
        tup= tup + (t[1],):
            elif (t[2],/2) == int 
            tup= tup+ (t[2],):
                elif (t[3],/2)==int
                tup = tup + (t[3],):
                    return tup