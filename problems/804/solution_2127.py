def filtra_pares(a,b,c,d):
    '''uma função que retorna uma tupla contendo apenas os elementos pares da tupla original.'''
    pares= ()
    if filtrar_pares[a] % 2 == 0:
        pares += a
    if filtra_pares[b] % 2 ==0:
        pares += b
    if filtra_pares[c] % 2 ==0:
        pares += c
    if filtra_pares[d] % 2 ==0:
        pares += d
        return pares