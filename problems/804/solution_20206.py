def par(x):
    """Define se um número é par"""
    """int/float->bol"""
    return num%2==0
def filtra_par(a,b,c,d):
    """Função que recebe 4 números e filtra somente os números pares"""
    if par(x)==true:
        par.append(x)
        return [par]