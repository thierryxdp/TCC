"""Retorna os elementos pares de uma tupla:
int, int, int,int -> tuple"""
def filtra_pares(a,b,c,d):
geral[a,b,c,d]
pares=[]
for pares in geral(a,b,c,d):
    if (geral(a,b,c,d)//2)==0
        return (pares)