def formato_data(tuple1):
    a,b,c,d = tuple1
    possiveis = ()
    if (a%2)==0:
        possiveis+=(a,)
    if (b%2)==0:
        possiveis+=(b,)
    if (c%2)==0:
        possiveis+=(c,)
    if (d%2)==0:
        possíveis+=(d,)
        return (possíveis)