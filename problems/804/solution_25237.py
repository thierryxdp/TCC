def filtra_pares(tup):
    'filtra os numeros pares de uma tupla com 4 valores tup->tup'
    numpar=()
    if Par(tup[0]):
        tuppares = tuppares+(t[0],)
    if Par(tup[1]):
        tuppares = tuppares+(t[1],)
    if Par(tup[2]):
        tuppares = tuppares+(t[2],)
    if Par(tup[3]):
        tuppares = tuppares+(t[3],)
    return tuppares#Start your python function herdef filtra_pares(tup):
    'filtra os numeros pares de uma tupla com 4 valores tup->tup'
    numpar=()
    if Par(tup[0]):
        tuppares = tuppares+(t[0],)
    if Par(tup[1]):
        tuppares = tuppares+(t[1],)
    if Par(tup[2]):
        tuppares = tuppares+(t[2],)
    if Par(tup[3]):
        tuppares = tuppares+(t[3],)
    return tupparese