def filtra_pares(a,b,c,d):
    '''Funçao que retorna o numero pares dados os numeros
    inteiros de entrada
    Entrada: int, int, int
    Saida: tuple'''
    pares=()
    if tupla[0]%2==0:
        pares=tupla[0],
    if tupla[1]%2==0:
        pares2=tupla[1],
        pares=pares+pares2
    if tupla[2]%2==0:
        pares3=tupla[2],
        pares=pares+pares3
    if tupla[3]%2==0:
        pares4=pares+pares4
    return pares