"""Retorna apenas os elementos pares da tupla inicial
assinatura: int, int, int, int -> int"""
def filtra_pares (tuple)
    r = ()
if par(t[0]):
    r=r+(t[0],)
if par(t[1]):
    r=r+(t[1],)
if par(t[2]):
    r=r+(t[2],)
if par(t[3]):
    r=r+(t[3],)
if par(t[4]):
    r=r+(t[4],)
        return r