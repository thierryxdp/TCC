def filtra_pares(a,b,c,d):
    A=(a,b,c,d)
    if not a/2=int:
        del A[0]
    if not b/2=int:
        del A[1]
    if not c/2=int:
        del A[2]
    if not d/2=int:
        del A[3]
    return A