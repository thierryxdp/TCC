def filtra_pares (a):
    """função que filtra os numeros pares de uma turpla"""
    """int,int,int -> int"""
    b = b + (a[0],)
    if ((a[0])%2)==0:
        return a[0]
    if((a[1])%2)==0:
        return a[1]
    if((a[2]%2)==0:
       return a[2]
  	if((a[3]%2)==0:
        return a[3]
   	else:
       return()