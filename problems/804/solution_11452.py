def filtra_pares(T):
    """filtra os números pares da tupla P e os retorna em uma outra tupla
    tuple->tuple"""
    M = ()
    if int(T[0]) % 2 == 0
    	M += T[0]
    if int(T[1]) % 2 == 0
    	M += T[1]
    if int(T[2]) % 2 == 0
    	M += T[2]
    if int(T[3]) % 2 == 0
    	M += T[3]
    return M