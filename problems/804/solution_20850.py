#Start your python function here
def filtra_pares(tupla):
    '''a'''
    batata = int(tupla)
    int(tupla, tupla, tupla, tupla) = (n1, n2, n3, n4)
    resposta = ()
    if (tupla%2) == 0:
        resposta = resposta + (tupla,)
    if (tupla%2) ==0:
        resposta = resposta + (tupla,)
    if (tupla%2) == 0:
        resposta = resposta + (tupla,)
    if (tupla%2) == 0:
        resposta = resposta + (tupla,)
    else:
        resposta = resposta
    return resposta