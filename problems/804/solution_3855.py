def filtra_pares (tuple):
        
    pares = ()
    
    if tuple [0] % 2 == 0:
        pares = pares + (tupla [0], )
    if tuple [1] % 2 == 0:
        pares = pares + (tupla [1], )
    if tuple [2] % 2 == 0:
        pares = pares + (tupla [2], )
    if tuple [3] % 2 == 0:
        pares = pares + (tupla [3], )
        
	return pares