def filtra_pares (tupla):
    list_final = list ()
    for x in tupla:
        if x % 2 == 0:
            return list_final += [x]
        
        elif tuple_final = tuple (list_final):
            return tuple_final