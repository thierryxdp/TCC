def filtra_pares(p):
    lista = []
      
    in (p[0]%2) == 0:
        lista =+ p[0],
    in (p[1]%2) == 0:
        lista =+ p[1],
    in (p[2]%2) == 0:
        lista=+ p[2],
    in (p[3]%2) == 0:
        lista =+ p[3],
        
    return lista