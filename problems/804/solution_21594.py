def filtra_pares(t):
    lista = []
    lista3=[]
    for sub[:] in t:
        if sub%2==0:
            lista.append(sub)
            return tuple(lista)
        else:
            if sub%2!=0:
                return tuple(lista3)