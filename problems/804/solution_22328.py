def filtra_pares (a,b,c,d):
    """retorna apenas elementos pares"""
    numero = [a,b,c,d]
    resultado = numero%2
   if resultado = 0:
   		return str('PAR')
   else resultado = 1:
        return str('IMPAR')