#Start your python function here
def filtra_pares(tup):
  '''funcao que retorna apenas os elementos pares'''
	pares=()
	if (tup[0]%2)==0:
       pares+=(tuplas[0],)
    if (tup[1]%2==0):
       pares+=(tuplas[1],)
    if (tup[2]%2==0):
       pares+=(tuplas[2],)
    if (tup[3]%2==0):
       pares+=(tuplas[3],)
        return pares