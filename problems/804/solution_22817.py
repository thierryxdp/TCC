#Start your python function here
def filtra_pares(t):
    '''funcao que filtra somente os numeros pares de uma tupla; tup->tup'''
    pares=()
    if(t[0])%==0:
        pares=pares+ (t[0],)
    if(t[1])%==0:
        pares=pares+ (t[1],)
    if(t[2])%==0:
        pares=pares+ (t[2],)
    if(t[3])%==0:
        pares=pares+ (t[3],)
    return pares