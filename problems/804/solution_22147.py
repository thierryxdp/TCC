def par(x):
    return x%2

def filtra_pares(tupa):
    if par(tupa[0])==0:
        return tupa[0],
    elif par(tupa[0])!=0:
        return ()
    if par(tupa[0])==0 and par(tupa[1])==0:
        return (str(tup[0]),+str(tup[1]))
    
    elif par(tupa[0])==0:
        return tupa[0],
    elif par(tupa[1])==0:
        return (tupa[1])