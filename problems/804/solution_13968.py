def par(x):
    if x % 2 == 0:
        return x
    else:

def filtra_pares(a,b,c,):
    tup = (par(tup[a]),par(tup[b]),par(tup[c),par(tup[d)
    return tup
    

    
    #Start your python function here