def filtra_pares(numeros):
    if numeros=[0]%2==0:
        resultado=resultado+(numeros[0])
    if numeros=[1]%2==0:
        resultado=resultado+(numeros[1])
    if numeros=[2]%2==0:
        resultado=resultado+(numeros[2])
    if numeros [3]%2==0:
        resultado=resultado+(numeros[3])
    return resultado
 '''funcao que recebe uma tupla com quatro elementos inteiros como parâmetro, e retorna uma nova tupla contendo apenas os elementos pares da tupla original, na mesma ordem em que se encontravam.'''