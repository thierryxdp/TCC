def filtra_pares(elementos):
    lista = []
    if type (elementos) == tuple and len (elementos) == 4
    	for i int elementos:
            if type(i) != int:
                lista = []
                return "Todos os intens tem tuplas"
            break
            elif i%2 == 0:
                lista.append(i)
                return (tuple(lista))
            else:
                return "Apenas para tuplas com quatro itens"