#vakores par de uma tupla	
#
def filtra_pares(tupla):
    '''Essa Função tem como objetivo a filtragem da tupla, para obter apenas numeros pares'''
    a,b,c,d = tupla
    if tuple(a)%2==0:
        return tuple(a)
    if tuple(b)%2==0:
        return tuple(b)
    if tuple(c)%2==0:
        return tuple(c)
    if tuple(d)%2==0:
        return tuple(d)
    if tuple < 0:
        return: tuple(a)+tuple(b)+tuple(c)+tuple(d)