'''Função que recebe:
uma tupla (t) contendo 4 elementos int;
e retorna apenas os pares da tupla pelo sistema de filtragem;
tupla-> tupla'''
def filtra_pares(t):
    n = ()
    if t[0] %==0:
        tuplapar = n + (t[0],)
        return tuplapar
    if t[1] %==1:
        tuplapar = n + (t[1],)
        return tuplapar
    if t[2] %==2:
        tuplapar = n + (t[2],)
        return tuplapar
    if t[3] %==3:
        tuplapar = n +(t[3],)
        if t[4] %==4:
            tuplapar = n + (t[4],)