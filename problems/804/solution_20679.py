def par(n):
    if(n%2==0):
        return n
def filtra_pares(l):
    """Informe uma trupla com 4 números e a função irá retornar os números pares
        number, number, number, number -> string"""
	final = ()	
    if(par(l[0]=true):
        return final = final + (l[0],)
    if(par(l[1])):
       return final = final + (l[1],)
    if(par(l[2])):
       return final = final + (l[2],)
    if(par(l[3])):
       return final = final + (l[3],)
    return final