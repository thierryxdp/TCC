from math import
def filtra_pares(a,b,c,d):
    """Funcao que recebe 4 elementos inteiros e retorna apenas os elementos pares:int,int,int,int=>tuple"""
    x=(a,b,c,d)
    y=()
    for valor in x:
        if (valor 2%)==0:
            return y.append(valor)