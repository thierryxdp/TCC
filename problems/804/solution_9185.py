'''função que recebe uma tupla com 4 elementos inteiros e retorna uma nova tupla contendo apenas os elementos pares da tupla original'''
# tuple->tuple
#(a),(b),(c),(d) são os números inteiros de entrada
def filtra_pares(a,b,c,d):
    if a/2==int:
        return x=(a,)
    if b/2==int:
        return y=(b,)
    else
    if c/2==int:
        return z=(c,)
    else
    if d/2==int:
        return w=(d,)
    return x+y+z+w