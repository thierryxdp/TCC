def filtra_pares(tup):
    """Calcule e retorne os números pares de uma tupla"""
    tup = ('a', 'b', 'c', 'd')
    if tup[0]/2 = int:
        return tup[0]
    if tup[1]/2 = int:
        return tup[1]
    if tup[2]/2 = int:
        return tup[2]
    if tup[3]/2 = int:
        return tup[3]
    else:
        return