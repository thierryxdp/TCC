def filtra_pares(tup):
    '''função que retorna uma nova tupla somente com elementos pares; tupla -> tupla'''
    if tup[0] for par:
        return tup1 = tup1 + tup[0]
    if tup[1] for par:
        return tup1 = tup1 + tup[1]
    elif tup[2] for par:
        return tup1 = tup1 + tup[2]
    else tup[3] for par:
        return tup1 = tup1 + tup[3]