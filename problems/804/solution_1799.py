def filtra_pares(tupla):
    """dada uma tupla com uatro elementos inteiros, a função retorna
uma nova tupla contendo apenas os elementos pares.
tupla-->tupla

Parâmetros
tupla: tupla de quatro números inteiros (a,b,c,d)
"""
    a,b,c,d==tupla
    x=()
    A==bool(a%2==0)
    B==bool(b%2==0)
    C==bool(c%2==0)
    D==bool(d%2==0)
    
    if A==True:
        x=x+(a,)
    if B==True:
        x=x+(b,)
    if C==True:
        x=x+(c,)
    if D==True:
        x=x+(d,)