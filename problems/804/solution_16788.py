#Start your python function here
def filtra_pares(tup: tuple) -> tuple:
    '''
    Retorna
    '''
    tupla = ()
    if tup[0] % 2 == 0:
        tupla = tupla + (a[0],)
    if tup[1] % 2 == 0:
        tupla = tupla + (a[1],)
    if tup[2] % 2 == 0:
         tupla = tupla + (a[2],)
    if tup[3] % 2 == 0:
         tupla = tupla + (a[3],)
    return tupla