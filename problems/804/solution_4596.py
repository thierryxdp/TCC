#Start your python function here
def filtra_pares(a,b,c,d):   
    pares = 0
    if a %2 ==0:
        pares = pares + 1
    if b %2 ==0:
        pares = pares + 1
    if c %2 ==0:
        pares = pares + 1
    if d %2 ==0:
        pares = pares + 1
    return (pares)