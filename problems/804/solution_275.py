def filtra_pares(tupla):
    '''Função retorna uma nova tupla, entretanto agora
    apenas com os valores pares da tupla anterior
    tupla -> tupla'''
    
a,b,c,d = tupla
 
    if a%2==0:
    if b%2==0:
    if c%2==0:
    if d%2==0:
        return tuple(a) + tuple(b) + tuple(c) + tuple(d)