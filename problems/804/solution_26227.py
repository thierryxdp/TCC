def filtra_pares(tup):
    pares = ()
    contador = 0
    while contador < len(tup):
        if tup[proximo]%2 ==0:
        pares = pares + tup[contador]
        contador = contador + 1
    return pares