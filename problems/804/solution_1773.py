#Start your python function here
def filtra_pares(tupla):
    """Funcao que recebe uma tupla e retorna uma nova tupla contendo apenas pares da tupla
    original, na mesma ordem em que se encontravam. tupla->tupla."""
    
    a, b, c, d = t
    nova_tupla = ()
    
    if(a%2 == 0):
        nova_tupla = nova_tupla + (x,)
    if(b%2==0):
        nova_tupla = nova_tupla + (y,)
    if(c%2==0):
        nova_tupla = nova_tupla + (w,)
    if(d%2==0):
        nova_tupla = nova_tupla + (z,)
    return o