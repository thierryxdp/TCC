def filtra_pares(t):
  '''funcao que filtra os numeros pares de uma tupla e retorna uma outra tupla seguindo a mesma ordem
  tup -> tup'''  
    pares= () 
if (t[0])%2==0:
    pares=pares + (t[0],)
if (t[1])%2==0:
    pares=pares + (t[1],)
if (t[2])%2==0:
    pares=pares + (t[2],)
if (t[3])%2==0:
    pares=pares + (t[3],)
return