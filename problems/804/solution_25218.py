def filtra_pares(a):
    '''Dados quatro números de entradas, retorna somente os pares na ordem.'''
    pares = ()
    if (x%2 ==0):
        pares = pares+(a[0])
    if (y%2 == 0):
        pares = pares+(a[1])
    if (z%2 == 0):
        pares = pares +(a[2])
    if(w%2 == 0 ):
        pares = pares +(a[3])
    return pares