def filtra_pares(t):
    ''' funcao que dada uma tupla retorna somente os numeros pares'''
    tup=()
    tup2=()
    if int(t[0],)%2 == 0:
        tup = tup + (t[0],)
    if int(t[1],)%2 == 0:
        tup = tup + (t[1],)
    if int(t[2],)%2 == 0:
        tup = tup + (t[2],)
    if int(t[3],)%2 == 0:
        tup = tup + (t[3],)
        return tup
    else:
        return tup2()