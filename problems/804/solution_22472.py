def filtra_pares(tup):
    '''função que retorna com uma tupla de quatro elementos inteiros
       como parâmetro, e uma nova tupla contendo apenas os elementos pares
       da tupla original; tup->tup'''
    pares=()
    if (tup[0]%2)==0:
        pares+=(tup[0],)
    if (tup[1]%2==0):
        pares+=(tup[1],)
    if (tup[2]%2==0:
        pares+=(tup[2],)
    if (tup[3]%2==0):
        pares+=(tup+=(tup[3],)
                return pares