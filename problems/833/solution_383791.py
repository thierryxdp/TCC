def conta_numero(numero,matriz):
    nlin = len(matriz)
    ncol = len(matriz[0])
    resposta= ()
    conta= ()
    
    for i in range(nlin):
        for j in range(ncol):
            list.count(resposta,numero)
            list.append(conta,reposta)
    return len(conta)