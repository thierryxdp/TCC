def conta_numero (numero, matriz):
    """ Calcula e retorna quantas vezes um numero aparece um uma matriz. int -> list"""
    quantidade = [list.count(len(matriz), numero) + list.count(len(matriz[0]), numero)]
    return quantidade