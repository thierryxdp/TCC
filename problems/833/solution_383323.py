def conta_numero(numero,m):
    nlin = len(m)
    ncol = len(m[0])
    lista = []
    for i in range(nlin):
        for j in range(ncol):
            list.append(lista,m[i][j])
    x = lista.count(numero)
    return x