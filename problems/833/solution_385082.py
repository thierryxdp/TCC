def conta_numero(numero, matriz):
    """..."""
    a = []
    for i in range(0, len(matriz)):
        for j in range(0, len(matriz)):
            if matriz[i][j] == numero:
                list.apend(a(list.count(matriz,numero)))
                
               
    return a