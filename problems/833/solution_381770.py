def conta_numero(numero,matriz):
    contador=0
    indice=0
    while indice<len(matriz):
        for elemento in range(len(matriz[indice]):
        	if elemento==numero:
                              contador=contador+1
        indice=indice+1