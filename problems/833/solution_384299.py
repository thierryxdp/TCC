def conta_numero(numero, matriz):
    ''' função que dado um número inteiro e uma matriz de inteiros, retorna a quantidade de ocorrências desse número;
    int, list -> int '''
    quantidade = 0
    for i in matriz:
    	quantidade = quentidade + list.count(i, numero)
    return quantidade