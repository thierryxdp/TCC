def conta_numero(numero, matriz):
    """ função dado um numero inteiro e uma matriz de tamanho qualquer retona
    a quantidade de vezes que o numero aparece na matriz.
    int, matriz --> int"""
    repetido = []
    for numero in matriz:
        list.append(numero,repetido)
        conta = len(repetido)
    return conta