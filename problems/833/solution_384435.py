def conta_numero(numero, matriz):
    '''funcao que conta e retorna o numero de vezes que o numero dado apareceu na matriz;
    list-> int'''

    repeticoes=[]
    for l in range(len(numero))
        for c in range(len(numero[l])):
            if numero[l][c] == matriz:
                list.append(repeticoes,matriz)
    return len(repeticoes)