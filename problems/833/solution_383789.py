def conta_numero(numero,matriz):
    nlin = len(matriz)
    ncol = len(matriz[0])
    resposta= ()
    
    for i in range(nlin):
        for j in range(ncol):
            list.count(resposta,numero):
    return len(resposta)