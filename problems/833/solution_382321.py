def conta_numero(numero,matriz):
    linha = len(matriz)
    coluna = len(matriz[0])
    for i in range(linha):
        lista = []
        for j in range(linha):
            matriz[i][j]==8
                lista.append(matriz[i][j])
    return lista