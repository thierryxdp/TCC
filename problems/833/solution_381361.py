def conta_numero(numero,matriz):
    '''funcao que retorna quantas vezes o numero dado como entrada aparece na matriz tambem dada como entrada
    int, matriz-> lista'''
    quantidade=[]
    for i in range(len(matriz)):
                   for j in range(len(matriz[0])):
                                  if j==numero:
                   	list.append(quantidade,j)
    return len(quantidade)