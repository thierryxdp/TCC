def conta_numero(numero, matriz):

    linha_atual = 0

    while linha_atual < len(matriz):
        for numero in range(matriz[0], matriz[len(matriz)]
            qtd_num = matriz[linha_atual].count(numero) + matriz[linha_atual-1].count(numero)
        
    linha_atual = linha_atual + 1

    return qtd_num