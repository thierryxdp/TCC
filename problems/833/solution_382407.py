def conta_numero(numero,matriz):
    soma = 0
    x = 0
    for numero in range(len(matriz)):
        soma = soma = 1
        for numero in range(len(matriz[x])):
            soma = soma + 1
    return soma