def conta_numero(numero, matriz):
    lista = []
    for i in len(matriz):
        for j in len(matriz[i]):
            a = matriz[i][j]
            list.append(a, lista)
            
    return list.count(numero,lista)