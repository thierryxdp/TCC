def conta_numero(numero,matriz):
    """..."""
    contador = 0
    matriz = []
    for i in range(len(matriz)):
        if numero in range(len(matriz):
                           list.count(matriz,numero)
                           contador = contador + 1
    return contador