def conta_numero(numero,matriz):
    """Função que dado um número e uma matriz de inteiros, conta e retorna
    quantas vezes aquele número aparece na matriz. 
    int, matriz(lista de listas) -> int"""
    
    quant_vezes = 0
    
    for i in range(len(matriz)):
        for j in range(len(matriz[0]):
            if j == numero:
               quant_vezes = quant_vezes + 1
        
    return quant_vezes