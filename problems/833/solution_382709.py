def conta_numero(numero,matriz):
    """funcao que conta qunatas vezes o numero aparece na matriz.
    int,list->int."""
    m=[]
    for i in len(matriz):
        list.append(m,list.count(matriz[i],numero))
    return sum(m)