def conta_numero(numero,matriz):
    i=0
    for i in range(len(matriz)):
        j+=0
        for j in range(len(matriz[j]):
            quantidade=matriz[j].count(numero)
        i+=1
            return quantidade