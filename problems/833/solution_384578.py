def contato_numero(numero, matriz):
    """..."""
    
    matriz = [0]
    ocorre = 0
    for l in range(0, 1):
        for c in range(0,1):
            matriz[l][c]
    if numero i len(matriz):
        ocorre+=1
        matriz+=1 
        return ocorre