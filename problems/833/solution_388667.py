def conta_numero(numero, matriz):
    contador=0
    for indice in range(0, len(matriz)):
        contador+=1 
        if matriz[indice] == numero:
            return  contador+=1
        else contador

    return contador