def conta_numero(numero,matriz):
    """conta quantas vezes o número inteiro está em uma matriz de números inteiros. Lista, int->"""
    n=0
    lista=[]
    for i in range(len(matriz)):
        for j in range (len(matriz[n]):
                        lista=lista+[j]
                        n=0+1
    return lista