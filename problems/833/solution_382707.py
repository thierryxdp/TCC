def conta_numero(numero,matriz):
    k=0
    for numero in range(len(matriz)):  
        for numero in range(len(matriz[0])):
            if numero==range(len(matriz)) or numero==range(len(matriz[0]):
                k+=1
            else:
                k+=0
    return k