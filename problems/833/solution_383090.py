def conta_numero (numero, matriz):
    """Funcao que dado um numero inteiro e uma matriz de inteiros conta e retorna quantas vezes aquele numero aparece na matriz"""
    contador = 0
    numero = x
    for x in len(matriz):
        for x in len(matriz[0]):
            matriz.count(matriz[x])
            contador += 1
    return contador