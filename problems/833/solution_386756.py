def conta_numero(numero,matriz):
    ls=[]
    l=len(mat)
    c=len(mat[0])
    for i in range(l):
        for j in range(c):
            if mat[i][j] == n:
                ls.append(n)
    return len(ls)