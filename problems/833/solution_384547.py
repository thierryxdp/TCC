def conta_numero(numero,matriz):
    nlin = len(matriz)
    ncol = len(matriz[0])
    contador = 0
    for i in range(nlin):
        linha = []
        for j in range(ncol):
            linha.append(numero)
    return linha