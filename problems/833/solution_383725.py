def conta_numero(numero,matriz):
    """conta quantas vezes um numero aparece em uma matriz;int,list->int"""
    i=0
    resposta=[]
    while i<len(matriz):
        if numero in matriz[i]:
            lista=matriz[i]
            list.append(resposta,list.count(lista,numero))
    return resposta