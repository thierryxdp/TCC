def contato_numero(numero, matriz):
    """..."""
    
    matriz = [0]
    ocorre = 0
    for l in range(0, 1):
        for c in range(0,1):
            matriz[l][c]
    for i range(0, len(matriz)):
        if numero in len(matriz):
            occore+= 1