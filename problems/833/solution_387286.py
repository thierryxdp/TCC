def conta_numero(numero,matriz):
    qtd_linhas=len(matriz)
    qtd_colunas=len(matriz[0])
    a=0
    
    for i in matriz:
        if i == numero:
            a[matriz]=matriz.count(numero)
        for j in matriz:
            if j == numero:
                a[matriz]=matriz.count(numero)
    return  a