def conta_numero(num, matriz):
    qtd_num=0
    for L in range(len(matriz)):
        for C in range(len(matriz[L]):
            if matriz[L][C]==num:
               qtd_num=qtd_num+1
    return qtd_num