def conta_numero(numero, matriz):
    
    acumulador = 0
    
    m = len(matriz)
    n = len(matriz[0])
    
    if m = 0 or n = 0:
    else:
        for i in range(m):
        	for j in range(n):
            	if matriz[i][j] == numero:
                	acumulador += 1
    
    return acumulador