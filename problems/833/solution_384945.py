def conta_numero(numero,matriz):
    '''...'''
    
    R=[]
    cont=0
    n_linha=(len(matriz))
    
    for i in range(n_linha):
        linha = n_linha*[0]
        list.append(R,linha)    
    for elemento in matriz:
        if elemento == numero:
            list.count(elementos)
            cont+=1 
    return cont