def conta_numero(n,matriz):
    '''A função retorna a quantidade de vezes em que um dado número aparece numa matriz
    int,list->int'''
    contagem=0
    for i in range(len(matriz)):
        for j in range(len(matriz[0])):
            if matriz[i][j]==n:
                
    return contagem