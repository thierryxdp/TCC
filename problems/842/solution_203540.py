def pontos_por_time (times, pontos):
    if(3*cv)>(3*fv):
        return str ("Cormengo")
    elif (3*cv+ce)<(3*fv+fe):
        return str ("Flaminthians")
    elif(3*cv+ce)==(3*fv+fe) and cs>fs:
        return str ("Cormengo")
    elif(3*cv+ce)==(3*fv+fe) and cs<fs:
        return str ("Flaminthians")
    else:
        (3*cv+ce)==(3*fv+fe) and cs==fs
        return str ("Empate")