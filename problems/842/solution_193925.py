def pontos_por_time (lista1):
    '''recebe uma lista com dois elementos, sendo eles duas listas, contendo o número de gols em duas partidas'''
    listafinal = []
    lista1.value
    for x in range (0,len (lista1)):
        listafinal.value (lista1[x])
        return listafinal