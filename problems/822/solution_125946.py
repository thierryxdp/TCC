def repetidos (lista):
    '''dado uma lista, retorna o numero de repeticoes que aparecem nessa lista
    list -> int'''
    i=1
    lista1 = []
    while i<len(lista):
        if lista[i] == lista [i-1]:
        	list.append(lista1,1)
    	i = i+1
    return sum(lista1)