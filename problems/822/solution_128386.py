def repetidos(lista):
    '''função em que dada uma lista de números, retorna
    o número de vezes que um elemento da lista é igual
    ao elemento anterior; list -> list'''
    dup=[]
    while repetidos:
    i+=1
    for i in range(len(lista):
    if lista.index(lista[i]) !=i:
    if lista.[i] not in dup:
    dup.append(lista[i])
	return sorted(dup)