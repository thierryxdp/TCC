def repetidos(lista[]):
    '''funcao que recebe uma lista de numeros e retorna o 
    numero de vezes que um elemento da lista e igual ao 
    seu anterior'''
    valo = 0
    for l in range(len(lista)):
        if i > 0 and lista[i]==lista[i-1]:
            valor+=1
	return valor