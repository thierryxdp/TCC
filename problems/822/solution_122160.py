def repetidos(lista):
    """Função que recebe como entrada uma lista de numeros e retorna o numero de vezes que um elemento da lista é igual ao elemento anterior
       list -> int"""
    vezes = ()
    proximo = 0
    anterior = proximo - 1
    while lista[proximo]= anterior:
        vezes += (lista[proximo])
        proximo += 1
    return vezes