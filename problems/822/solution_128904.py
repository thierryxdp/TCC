def repetidos(lista):
    """ Retorna quantas vezes um elemento da lista é igual ao 
    elemento anterior"""
    R = 0 
    proximo = 0 
    while proximo < len(lista):
        if lista[n] == lista[n - 1]
        R = R + 1
    return R