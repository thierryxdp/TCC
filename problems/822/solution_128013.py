def repetidos(lista):
    '''
    Função que recebe uma lista de números retorna quantas vezes um
    elemento da lista é igual ao seu anterior anterior
    list-> int
    '''
    lista = []
    indice = 0
    while indice < len(lista):
        if lista[indice] in list.count(lista, indice)
        	quantas_vezes = lista[indice]
    	indice = indice + 1
        return quantas_vezes