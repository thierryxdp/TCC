def repetidos (lista):
    '''retorne o numero de vezes que um elemento da lista e igual ao anterior'''
    l.sum(lista)
    i=0
    numeros=1
    while i<len(lista):
        if lista[i] in 'numero':
            numeros = numeros +1
        i = i+1
    return numeros