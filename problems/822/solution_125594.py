def repetidos(lista):
    indice = 0
    vezes = []
    while indice < len(lista):
        vezes = list.append(list.count(lista, lista[indice]))
        if list.count(lista, lista[indice])
            vezes = list.append(lista, lista[indice])
        indice = indice + 1
    return vezes