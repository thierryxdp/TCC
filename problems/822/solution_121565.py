def repetidos(lista):
    resposta=[]
    antes=0
    lista.sort()
    while antes<len(lista)-1:
        if lista[antes+1]==lista[antes]:
        list.append(resposta,lista[antes])
        antes=antes+1
    return len(reposta)