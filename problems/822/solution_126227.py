def repetidos(numeros):
    """
    	Função que retorna o número de vezes que um elemento
        da lista é igual ao elemento anterior.
    	list -> int
    """
    i = 0
    numeros[i]
    num = numeros.count(numeros[i])
    return num