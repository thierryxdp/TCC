def repetidos(l):
    '''Recebe uma lista de números como entrada, e retorna o número de vezes que um elemento da lista é igual ao anterior.
    list -> list'''
    x = 0
    l_1 = []
    while x < len(l)-1:
        if l[x] == l[x+1]
        	list.append(l_1,True)
        else:
            list.append(l_1,False)
        x += 1
    return l_1.count(True)