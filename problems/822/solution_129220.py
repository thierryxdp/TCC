def repetidos(numeros):
    i=1
    repetir=
      while i<len(numeros)-1:
        if numeros[i+1]==numeros[i]:
            rep+=str.count()
        else:
            rep+=1
        i+=1
        return rep