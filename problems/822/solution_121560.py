def repetidos(lista):
    resposta=[]
    antes=0
    while antes<len(lista)-1:
        if lista[antes]==lista[antes+1]:
        list.append(resposta,lista[antes])
        antes=antes+1
    return len(reposta)