def repetidos(lista):
    resposta=[]
    antes=0
    depois=1
    while lista[antes]=lista[depois]:
        list.append(resposta,lista[antes])
        antes=antes+1
        depois=depois+1
    return len(reposta)