def repetidos(lista):
    """ recebe uma lista e torna a qunatidade de vezes que um lemento da lista 
    é igual ao anterio;list->int"""
    indice=0
    resposta=[]
    while(indice<len(lista)):
       list.append(resposta,list.count(lista,lista[0:indice]))
       indice+=1
    return resposta