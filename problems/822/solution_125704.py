def repetidos(lista):
    variavel=0
    listaN=[]
    while variavel<len(lista):
        if lista[variavel]==lista[variavel]:
            list.append(listaN,lista[variavel]
        variavel=variavel+1
     return listaN