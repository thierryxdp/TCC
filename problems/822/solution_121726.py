def repetidos(numeros):
    """Função que recebe como entrada uma lista de números e e retorna
    o número de vezes que um elemento é igual o anterior"""
    indice = 1
    listadeindices = 0
    while indice 
    len(numeros):
    if numeros[indice] == numeros(indicelistadeindices += (numeros(indice]
    indice += 1
    return len(listadeindices)