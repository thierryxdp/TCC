def repetidos(lista):
    '''funcao que recebe uma lista de numeros e retorna
    o numero de vezes que um elemento da lista eh igual
    ao elemento anterior
    lista->int'''
    proximo=0
    ocorrencia=0
    while proximo<len(lista) :
        if lista[proximo]=lista[proximo+1]
            ocorrencia= ocorrencia +2
        proximo= proximo + 1
        if proximo==len(lista):
            return ocorrencia