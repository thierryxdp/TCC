def repetidos(lista_num):
    """
    	Funcao que recebe uma lista de numeros.
        Retorna o numeros de vezes que o elemento atual é igual ao elemento anterior;
        list -> int
    """
    i = 1
    repetidos = 0
    while i < len(lista_num):
        if lista_num[i] == lista_num[i - 1]
        	repetidos = repetidos + 1
    	i = i + 1
        
    return repetidos