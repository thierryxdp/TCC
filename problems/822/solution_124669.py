def repetidos (listaNum):
    '''...'''
    r = ()
    
    for elemento in listaNum:
        if repetido = list.count(listaNum,elemento) > 1:
            quantidade = len(repetido)
    return quantidade