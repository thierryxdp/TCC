def repetidos(lista):
    ''' retorna o número de vezes que um elemento é igual ao 
    elemento anterior;entrada->lista ; list->list'''
	qtd_num = 0
    i = 0
    while i < len(lista):
        if lista[i]  lista[i] 
            qtd_num = qtd_num + 1
        i = i + 1
    return qtd_num