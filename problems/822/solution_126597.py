def repetidos(lista):
	"""função que conta os numeros repetidos
    list->int"""
    n=0
    contagem=[0]
    while n<max(lista):
        list.append(lista,list.count(lista,n))
        n=n+1
    return (contagem)