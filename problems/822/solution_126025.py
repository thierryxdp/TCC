def repetidos(lista_num):
    indice=1
    resultado=int(resultado)
    while indice<=len(lista_num):
        if lista_num[indice] == lista_num[indice-1]:
            resultado =list.count(lista_num, lista_num[indice]                        
        indice = indice + 1
    return resultado