def repetidos(numeros:list)->list:
    
    nova_lista = []
    contador = 0
    
    while contador < len(numeros):
        
        if (numeros[contador] < 0)
        
        	list.append(nova_lista, numeros[contador])
            
        contador = contador + 1
        
    return nova_lista