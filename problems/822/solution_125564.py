def repetidos(lista):
    indice = 0
    vezes = []
    while indice < len(lista):
        if lista[indice] in lista[:indice]:
            vezes = list.append[lista, lista[indice]
        	indice = indice + 1
    return vezes