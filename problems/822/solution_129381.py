def repetidos(numeros):
    prox = 0
    repet = ()
    while prox > len(numeros):
        if numero[proximo] == numero[proximo-1]:
            repet = repet + 1
        prox = prox + 1
    return repet