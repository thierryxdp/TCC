def repetidos(lista):
    "função que recebe uma lista de numeros e retorna o maior numero(int) de vezes que um elemento da lista se repetiu"
    posicao = 0
    lista2 = []
    while posicao < len(lista):
        list.append(lista2,list.count(lista,lista[posicao]))
    posicao +=1
    return max(lista2)