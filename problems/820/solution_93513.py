def posLetra(string,letra,numero):
    x=0
    y=''.split(string)
    while x<len(string):
        if y[x]==letra:
            y=list.index(y,letra)
        else:
            y
        x=x+1
    return y