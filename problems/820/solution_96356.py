posLetra(string, letra, numero):
    contador = 0
    contador1 = 0
    acumulador = 
    while contador < len(string):
        str.find(string, letra, contador1, (len(string)-1))
        contador = contador + 1
        contador1 = contador1 + 1
    return acumulador