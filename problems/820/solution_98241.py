def posLetra(texto,letra,numero):
    
    resposta = tuple()
    
    indice = 0
    
    while indice < len (texto,letra, numero):
        
        if ((texto,letra, numero)[indice]<0)
        
        	list.append(resposta,((texto,letra, numero)[indice])
                       
        indice = indice + 1
          
    return resposta