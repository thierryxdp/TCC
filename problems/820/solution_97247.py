def posLetra(string, l, ocorrencia):
    i = 0
    posicao = 0
    while i < len(string):
        if la in string:
            posicao = string[l]
            i = i+1
        else:
            if la not in string:
                return -1
    return posicao