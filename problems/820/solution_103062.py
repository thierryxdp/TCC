def posLetra(string, busca, n):
    posicao=string.find(busca)
    while posicao>=0 and n>1:
        if busca!=string
        	return -1
        pos=string.find(busca,posicao+1)
        n=n-1
    return posicao