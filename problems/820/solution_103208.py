def posLetra(string, letra, numero):
    '''Função que retorna a posição da string de ocorrência, str, str, int -> int'''
    numero = 0
    posicao = ''
    while posicao in numero:
        if letra = string[numero]:
        posicao = string
        numero = numero - 1
    return posicao