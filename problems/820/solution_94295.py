def posLetra(frase, letra, n):
    i = i2 = pos1 = pos2 = 0
    while i < n:
        if letra not in frase:
            pos2 = -1
        '''if frase.find(letra, pos1, pos2) != -1:'''
    	i += 1
    return pos2