def posLetra(fr,l,n):
    """ Função que, dados uma string, uma letra e um número n que indica
    a ocorrencia desejada da letra, na string.
    str, str, int-> int"""
   
    fr = []
    contador = 0 
    tamanho = len(fr)
    
    while contador < tamanho:
        if l in fr:
           
    
    return  fr.count(fr,n)