def posLetra(palavra, l, n):
    i = 0
    if l not in palavra:
        return -1
    while i<len(palavra):
        while i<len(palavra):
            if palavra[i]=l :
                x = palavra[i]
            i = i + 1
    return x