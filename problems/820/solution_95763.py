posletra(string, letra, num):
    if num > str.count(string, letra):
        
        return '-1'
    while cont < num:
        count>=1
        ind = str.ndex(string[ind+1:], letra)+len(string[:ind+1])
        return ind