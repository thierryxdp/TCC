#
#
#
#
def posLetra(string,letra,numero):
    i=0
    j=0
    posicao=int
    while j <= numero:
        while i < len(string):     
        if string(i) == letra
        posicao=index(strig(i))
        i=i+1
        j=j+1
    return posicao