#
#
#
#
def posLetra(string,letra,numero):
    i=0
    j=0
    posicao=int
    while i < len(string):
        while j <= numero:
            if string(i) == letra
            posicao=index(strig(i))
        j=j+1
    i=i+1
    return posicao