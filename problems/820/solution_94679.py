def posLetra(texto,letra,n):
    cont = 0
    
    while cont < len(texto):
        if letra in texto:
            if texto.count(letra) == n
            	return texto.find(letra,n)
            else:
                return -1
        cont = cont +1