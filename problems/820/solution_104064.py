def posLetra(string, letra, n):
    f = ""
    contador = 0
    while contador < n:
        str.find(string, letra, (str.find(string, letra), ) = f
                 return f
                 contador = contador + 1
    return f