def posLetra(frase,referente,indice):
    i = 0
    s = []
    while i < len(frase):
        if  frase[i]==referente:
            s = s + [i]
        if len(s)+1<indice
            return -1
        i = i + 1
    return s[indice-1]