def posLetra(string,letra,numero):
    x=0
    
    while x<len(string):
        if letra in string:
            y=list(string).index(letra)
            numero
        elif n>list(string).count(letra)
            return -1
        
        x=x+1
    return y