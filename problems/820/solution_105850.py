def posLetra(string,letra,x):
    '''funcao recebe uma string e retorna em que a letra esta. caso essa
    letra nao esteja no -1. str, str, int-->int'''
    i=x
    loc=0
    while o!=0:
        loc = loc + str.find(string,letra,loc):l
        i= i-1
    return loc-1