def posLetra(frase,l,n):
    """
       """
    posicao = 0
    contador = 0
    while posicao < len(frase):
        if frase(posicao) == l:
            contador = contador + 1
            if contador = n:
    return contador