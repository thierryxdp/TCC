def posLetra(string,letra,numero):
    x=0
    z=list(string).count(letra)
    while x<len(string):
        if letra in string:
            y=list(string).index(letra)
            numero
        elif n>z
            return -1
        
        x=x+1
    return y