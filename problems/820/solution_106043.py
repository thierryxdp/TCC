def posLetra(texto, letra, f):
    i = 0 
    while i < len(texto):
        if texto[i] == letra:

            pos = texto.index(texto[f])
        if texto [i] =! letra:
            pos = -1
        i +=1 
    return pos