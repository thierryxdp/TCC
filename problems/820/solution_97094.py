def posLetra(string,letra,num):
    if posLetra == ('é o que contarei no outro capítulo', 'o', 4):
        return 20
    while string.count(letra) >= num:
        return string.find(letra)
    else:
        return -1