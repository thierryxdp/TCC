def posLetra(string,letra,ocorrencia):
    '''função que recebe uma string, uma letra e uma ocorrencia desejada da letra e retorna em que posição da string a ocorrência da letra está. se houver menos ocorrências, a função retorna -1'''
    ocorrencias = []
    i = 0
    while i < len(string):
        if string[i] == letra:
            ocorrencias.append(i)
            i += 1
        i=+ 1
    if ocorrencia <= (len(ocorrencia)):
        ocorrencia = ocorrencia-1
        ocorrencias[ocorrencias]
    return ocorrencia[ocorrencias] else:
    return -1