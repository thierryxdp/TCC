def posLetra(string,letra,numero):
    lista=[]
    i=0
    if str.count(string,letra)<n:
        return -1
    while i<len(string):
        if string[i]==letra:
            list.append(lista,i)
        i=i+1
    return lista[numero-1]