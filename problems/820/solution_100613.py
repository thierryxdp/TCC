def posLetra(frase,letra,n):
    h=[]
    if count
    i=0
    while i<len(frase):
        if n==1:
            if letra in frase[i]:
        return i
        if n==2:
            if letra in frase[i]
        i=i+1