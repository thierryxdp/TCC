def posLetra(string, letra, n):
    """Retrona em que posição da string a ocorrencia, n, da letra está;
       Entrada: str, str, int;
       Saida: int;
    """
    x = len(string)
    for letra in string:
        return len(letra)
    if n>len(letra: