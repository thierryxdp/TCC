def posLetra(string,letra,numero):
    x=0
    
    while x<len(string):
        if letra in string:
            y=list(string).index(letra)
            n
        else:
            return string
        
        x=x+1
    return y