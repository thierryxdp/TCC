def posLetra(string, letra, num):
    '''função que dados uma string, uma letra e um número que indica a 
    ocorrência desejada da letra, retorna em que posição da string aquela
    ocorrência está. Caso exista menos ocorrências da letra do que a 
    ocorrência pedida, a função retorna -1; str, str, int -> int'''
    i = 0
    while i < len(s):
        if string[i] == letra:
            ocorrencias = str.count(string,letra)
            if 
                return i
        i = i + 1
    return i