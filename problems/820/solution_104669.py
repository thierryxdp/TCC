def posLetra(frase,"",n):
    "função que recebe uma string, uma letra e um número n e retorna"
    "quantidade de ocorrências da letra ou -1 se a quantidade for"
    "menor que o n"
    resultado=0
    i=0
    while i<len(frase):
        if posição[1] in 
    return resultado