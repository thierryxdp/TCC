def posLetra(string, letra, n):
   ''' 
   '''
    # 
    #if ocorrencias > numeros:
    #    return -1
    letras= str.count(string, letra)   
    if letras> n:
    
    i=0
    g=0
    
    while i<len(string):
        if string[1] == letra:
            i+=1
        if g == n:
            return i
        i+=1    
    return petra(string, letra, n):
   ''' 
   '''
    # 
    #if ocorrencias > numeros:
    #    return -1
    letras= str.count(frase, letra)    
    if letras> n:
    
    i=0
    g=0
    
    while i<len(string):
        if string[1] == letra:
            i+=1
        if g == n:
            return i
        i+=1    
    return p