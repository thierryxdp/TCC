def posLetra(texto,letra,num):
    texto=str.split(texto)
    return texto
    while num<len(texto):
        if letra in texto:
            posicao=list.index([texto],[letra],num)
        if letra not in texto
            posicao=-1
    return posicao