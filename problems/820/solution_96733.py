def posLetra(frase, letra, n):
    i=0
    ocorrencia=0
    indice=0
    while i < len(frase):
        if letra == frase[i]:
        	ocorrencia+=1
            if ocorrencia == n
            	indice=frase[i]
            else:
                indice= -1
        i+=1
    return indice