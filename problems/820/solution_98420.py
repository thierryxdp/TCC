def posLetra(frase,letra,n):
    '''...'''
    str.count(frase,letra)
    quantidade_l = len(letra)
    i=0
    r=''
    while i<len(frase):
        if n >= quantidade_l:
            str.count(frase,letra)
            str.index(frase,letra,[n])
        i+=1
    return