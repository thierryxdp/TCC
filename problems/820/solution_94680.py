def posLetra(texto,letra,n):
    cont = 0
    qtd = texto.count(letra)
    while cont < len(texto):
        if letra in texto:
            if qtd => n
            	return texto.find(letra,n)
            else:
                return -1
        cont = cont +1