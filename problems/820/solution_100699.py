def posLetra(frase,letra,n):
    h=[]
    i=0
    if frase.count(letra)>=n:
        while i<len(frase):
            if frase[i] in letra
            list.append(h,i)
        i=i+1
        return h(n-1)
    else:
        return -1