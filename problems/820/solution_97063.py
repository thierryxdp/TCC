def posLetra(string,letra,num):
    qtd_letras = ()
    ocorrencia = num
    while letra in string < str(num):
        qtd_letras += (1,)
        if qtd_letras > (ocorrencia,):
            return qtd_letras
    else:
        return -1