def posLetra(frase,l,n):
    """função que retorna a ocorrencia de uma letra numa string
    str,str,int - int"""
    indice = 0
    posicao = 0
    while < len(frase):
        posicao = posicao + str.find(frase,l,n)
    indice = indice + 1
    return posicao