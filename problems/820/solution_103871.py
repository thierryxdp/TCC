def posLetra(frase,letra,ocorrencia):
    '''recebe uma string,uma letra e um numero que indica
    a ocorrencia que quer desta e retorna a posição da string nessa
    ocorrencia'''
    l = letra
    ocorrencia = b
    str.find(frase,l) = x
    while b = 3:
        if b = 1:
            return str.find(frase,l)
        elif b = 2:
            return str.find(frase,l,x)
        elif b= 3:
            return str.replace(frase,l,'f',2) 
    return str.find(frase,l)