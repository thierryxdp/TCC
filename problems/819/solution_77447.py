def filtraMultiplos(numeros,n):
    """ Função para filtrar os multiplos de um numero n. Sua função deve receber como entrada uma lista
    de numeros e um numero, e retornar outra lista contendo todos os elementos da lista original que forem divisiveis por n
    list, int ->list"""
    numeros_elementos=len(numeros)
    indice=0
    resposta=lista[]
    
    while (indice<numero_elementos):
        if (numeros[indice] % n ==0):
            resposta += (numeros[indice],]
        indice += 1
     return resposta