def filtramultiplos(lista,numero):
    "função que recebe uma lista de numeros e retorna uma nova lista com todos os numeros divisíveis por um número qualquer"
    multiplos[]
    i=0
    while i<len(lista):
        if lista[i]%numero==0:
            multiplos=multiplos+[lista[i]]
        i=i+1
        
    return multiplos