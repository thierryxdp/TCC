def filtraMultiplos(lista, n):
    '''Retorna uma lista com os números de lista que são multiplos de n
    	list, int -> list'''
    multiplos = []
    for elemento in lista:
        if elemento%n == 0:
            list.append(multiplos, n)
    return multiplos