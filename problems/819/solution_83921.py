def filtraMultiplos(lista,n):
    """Dado uma lista de entrada e um número n, retornamos umqa lista contendo todos os números divisíveis por n; list, int -> list"""
    i= 0
    n_lista = 0
    while  i < len(lista):
        if lista[i] % n = 0:
           return list.append(n_lista,lista[i])
        else:
            return n_lista