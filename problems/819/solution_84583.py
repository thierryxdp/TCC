def filtraMultiplos(lista, m):
    cu= []
    for elemento in lista:
        if elemento%m==0 and m is in lista
            cu=cu+elemento+m
        elif elemento%m==0 and m is not in lista:
            cu=cu+elemento
        else:
            cu=cu
    return cu