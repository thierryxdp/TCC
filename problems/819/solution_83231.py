def filtraMultiplos(lista_numeros, n):
    '''Função que retorna outra lista com todos os elementos divisíveis por n, llist,int -> list'''
    x = 0
    listanum = []
    while x<len(lista_numeros):
        if list.append(lista_numeros, n):
            listanum = list.remove(lista_numeros, n)
        x = x % 1
    return listanum