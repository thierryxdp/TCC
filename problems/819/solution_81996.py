def filtraMultiplos(lista, numero):
    resutado=[]
    x=0
    while x <= len (lista)-1:
        if lista[x] % numero == 0:
            y = lista[x]
            resultado = []
            list.append(rest,y)
            x = x + 1
        elif x <= len (lista):
            lista[x]% numero != 0
            x= x+1
            return rest