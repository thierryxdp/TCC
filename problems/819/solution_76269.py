def filtraMultiplos(m,n):
    pos = 0
    resultado = []
    x = m[pos] % n == 0
    while pos < len(m):
        
            list.append(resultado,x)
        else:
            list.append(resultado,[])
        pos = pos + 1
       
    return resultado