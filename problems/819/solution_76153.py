def filtraMultiplos(lista,num):
    """..."""
    
    lista_multiplos = []
    pos = 0
    
    while pos < len(lista):
        if lista[pos]%num==0
        	lista_multiplos = lista_multiplos + (lista[pos],)
        pos = pos +1
    
    return lista_multiplos