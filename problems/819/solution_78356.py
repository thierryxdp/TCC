def filtraMultiplos(lista,n):
    
    list.append(lista,n)
    
    num = len(num)
    
    list.sort(lista)
    nova_lista(num)
    
    indice = 0
    
    while (indice < num):
        if (num[indice] % n == 0):
            
            resposta += (numeros[indice],)

        indice += 1
    return nova_lista