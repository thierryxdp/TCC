def filtraMultiplos(lista, n):
    """Função que dada uma lista de números e um número n qualquer,
    retorna outra lista contendo os os elementos da lista original que
    são divisíveis por n.
    
    Parameters:
    lista = Lista de números escolhidos
    n = Número que terá seus multiplos encontrados
    
    Returns:
    Retorna uma nova lista com todos os elementos da lista original que
    são divisíveis por n.
    list, int -> list
    """
    lista1 = []
    i = 0
    while i < len(lista):
        if i[0]%n == 0
        lista1 += 1
    i += 1
    return lista1