def filtraMultiplos(n):
    """Funcao que filtre os multiplos de um numero n"""
    multiplos = ()
    numero = 0
    while numero <len(t):
        if t[numero]%2 == 0:
            multiplos = multiplos + (t[numero],)
        numero = numero + 1
    return multiplos