def filtraMultiplos(m,n):
    pos = 0
    contador = []
     x = [n]
    while x > m:
        
        if m[pos] % x == 0:
                list.append(contador,m)
                
        else:
        
            list.append([])
        pos = pos +1
    return contador