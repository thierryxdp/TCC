def filtraMultiplos(lista,numero):
    """ Recebe uma lista e numero, e retorna
    todos os números, dívisíveis pelo número, contidos na lista
    list,int --> list """
    " Cria uma lista vazia "
    filtro = lista[]
    qtd = len(lista)
    i = 0
    while list(range(qtd)) < qtd:
        if lista[i]%n == 0:
            list.append(lista[i],filtro)
        i =+ 1
    return filtro