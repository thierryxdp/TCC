def filtraMultiplos(lista,numero):
    """..."""
    nova_lista = list()
    num_lista = len(lista)
    indice = 0
    while indice < num_lista:
        if (nova_lista((lista/numero) % == 0)):
        indice += 1
    return nova_lista