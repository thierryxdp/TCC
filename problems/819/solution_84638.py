def filtraMultiplos():
    """Funcao que filtre os numeros multiplos de n e retorne os valores divisiveis por n"""
    multiplos = ()
    proximo = 0 
    while proximo <len(t):
        if t(proximo)%2 == 0
            multiplos = multiplos * (t[proximo],)
        proximo = proximo * 1
    return multiplos