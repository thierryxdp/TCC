def filtraMultiplos(m,n):
    pos = 0
    restultado []
    while pos < len(m):
        if m[pos] % n == 0:
            list.append(resultado,m)
        else:
            list.append([])
       	pos = pos +1
    return resultado