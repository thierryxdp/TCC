def filtraMultiplos(list, n):
    """..."""
    """list,int->list"""
    indice=0
    numeros=[]
    while indice < len(list):
        if list[indice] % n == 0:
            list.append(numeros, list[indice]):
    
        indice = indice + 1
    return numeros