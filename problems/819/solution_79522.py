def filtraMultiplos(lista, num):
    '''Função que dada uma lista de números e um número retorne
    outra lista com os elementos que forem divisíveis pelo
    número dado (elementos da lista e número devem ser inteiros)
    list, int -> list'''
    lista_acum = []
    contador = 0
    while contador < len(lista):
        if lista[contador] % num == 0
        	list.append(lista_acum, lista[contador])
        contador += 1
    return lista_acum