def filtraMultiplos(lista, n):
    """Função que recebe uma lista e retorna outra lista contendo todos os elementos da lista original que forem divisíveis por n,
    list, int --> list"""
    i = 0
    novalista = []
    while i <= len(lista)-1:
        if lista[i] % n == :
            novalista.append(lista[i])
            
        i += 1
    return novalista