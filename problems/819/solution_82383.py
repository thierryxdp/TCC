def filtraMultiplos(lista,n):
    listaR = []
    indice = 0
    len(lista)
    while indice<=len(lista):
        indice=indice+1
        if lista[indice]%n==0
        	return list.append(listaR,lista[indice])