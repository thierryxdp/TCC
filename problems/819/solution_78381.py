def filtraMultiplos(lista,n):
    """...lista,int->lista"""
    resposta = list()
    numeros = len(lista)
    indice = 0
    n = len(range 10)
    while (indice < numeros):
        if (numeros[indice] / n == 0):
            return resposta