def filtraMultiplos(lista,numero):
    """..."""
    nova_lista = list[]
    num_lista = len(lista)
    i = 0
    while (indice < num_lista):
        if nova_lista = (lista[i]%numero == 0):
        i += 1
    return nova_lista