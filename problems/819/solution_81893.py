def filtraMultiplos(lista, num):
    """função que filtra os múltiplos de um número
    lista, int -> lista"""
    divisao = ()
    contador = 0
    while contador < len(lista):
        if (lista[prox]%num)==0:
            div = div +(lista[prox])
        contador += + 1
    return list(div)