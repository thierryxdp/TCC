def filtraMultiplos(lista_numero,n):
    """Dada a lista de entrada, retorne os números divisíveis por n"""
    numeros = []
    multiplo = 0
    while multiplo<len(lista_numero):
        if lista_numero[multiplo]%n==0:
            numeros = numeros+(list.extend(lista_numero,multiplo)
                                multiplos = multiplos+1
                               
    return numeros