def filtraMultiplos(lista,n):
    '''Função usada para filtrar os múltiplos de um número.
    entradas --> int
    saída --> int'''
    i = 0
    multiplos = []
    while i < len(lista):
        if lista[i] % n == 0
        multiplos += (lista[i],)
        i += 1
    return multiplos