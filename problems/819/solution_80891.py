def filtraMultiplos(lista, n):
    """A Entrada é uma lista com números junto de um número que 
    será no parâmetro e receber outra lista nova só com os 
    números da lista  original que são divisiveis pelo 
    número do parâmetro"""
    #int, int -> int
    
    ListaA = []
    Variavel = 0
    while Variavel < len(lista):
        Variavel n/2
        if n %0:
        return n.append(ListaA)