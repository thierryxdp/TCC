def filtraMultiplos(lista,n):
    multiplos = ()
    num = 1
    while num > 0:
        if lista[numero] / n:
            multiplos = multiplos + lista[num]
        numero = numero + 1
    return multiplos