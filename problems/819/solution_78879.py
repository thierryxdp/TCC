def filtraMultiplos(numeros: Tuple[int], x)
resposta = list()
indice = 0
while i in numeros
	if numeros[indice] % x == 0
    	list.append(resposta, numeros[indice])
        indice += 1
        return tuple(resposta)