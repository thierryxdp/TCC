# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    "Função que retorna a concatenação de duas str'a' e 'b' conforme forneceidas"
    "str, str -> str"
    a = str(a)
    b = str(b)
    return a+b+b+a