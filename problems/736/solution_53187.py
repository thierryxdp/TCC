def concatenacao(a, b):
    """Essa função, dadas duas strings a e b à escolha do usuário,
	 retorna a concatenação delas no formato abba;
	str, str -> str"""
    soma = str(a)+str(b)+str(b)+str(a)
    return soma