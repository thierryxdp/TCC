# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """
    Essa função recebe duas strings e retorna a concatenação 
    delas no formato 'abba'
    """
    a = str(a)
    b = str(b)
    return a + b + b + a