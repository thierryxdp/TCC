def concatenacao(a, b):
    """Função que recebe strings nas entradas a e b e retorna a concatençao delas no formato abba;
    entrada: str, str;
    saida: str."""
    x = a + b
    y = b + a
    return x + y