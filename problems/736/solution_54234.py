# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    '''
    A função retorna uma concatenação entre duas strings, a e b,
    á escolha do usuário, de modo que o o formato dela seja 'abba'
    (entrada = str, str / saída = str)
    '''
    return (a + b) + (b + a)