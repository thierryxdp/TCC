# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
""" funcao que retorna a concatenacao em formado abba, utilizando duas strings
Assinatura: str, str --> str"""
def concatenacao(a, b):
    #  a,b=str(input("string a: ")), str(input("string b: "))
    #  (conca(a,b))
    return str(a)+str(b)*2 + str(a)