# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """essa funcao dado os parametros a, b do tipo str os retorna
	 no formato abba
	str, str -> str"""
    formato = str(a)+str(b)+str(b)+str(a)
    return formato