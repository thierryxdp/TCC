# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """
    Essa funcao retorna a concatenacao de duas strings no formato abba
    Parametros: a,b(string, string)
    saida: string
    """
    return a + b + b + a