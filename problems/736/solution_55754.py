# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """Essa função retorna uma ordem diferente da dos parâmetros dados
    str -> str
    """
    (a,b)=lista
    return lista[0]+lista[2]*2+lista[0]