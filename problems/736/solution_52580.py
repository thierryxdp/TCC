# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """ funcao que concatena duas strings, no formato abba.
    str->str"""
    return str(a)+str(b)+str(b)+str(a)