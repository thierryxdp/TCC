# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """Função que, dadas duas strings (a e b) como parâmetros, retorna a concatenação delas no formato abba; str,str->str"""
    return a+b+b+a