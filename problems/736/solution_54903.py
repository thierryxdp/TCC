# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """função que retorna que retorna a concatenação de a e b
    no formato abba
    str, str -> str"""
    con=str(a)+str(b)+str(b)+str(a)
    return con