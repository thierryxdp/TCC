# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def conc[a, b]:
    """Função que promove a concatenação de duas strings diferentes
       str, str -> str"""
    return (a + b) + (b + a)