# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """funcao que dado como entrada duas strings, retorna a concatenacao entre elas. string1, string2 --> string(1,2,2,1)"""
    return a+b+b+a