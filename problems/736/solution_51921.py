# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """ Concatena as duas strings a e b fornecidas no formato abba.
    	str, str -> str
        
        Parameters:
        a: String a.
        b: String b.
        
        Returns:
        A concatenação delas no formato abba.
   	"""
 	return str(a) + str(b) + str(b) + str(a)