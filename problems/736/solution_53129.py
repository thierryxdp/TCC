# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    ''' funcao que recebe duas strings e concatena as strings
        entra:
        string
        sai
        string1 + string2*2 + string1
    '''
    str = ((a),(b))
    return str[0]+ str[1]*2 + str[0]
#ou return str[0] + str[1] + str[1] + str[0]