# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    "Função que calcula a concatenação das strings colocadas duas vezes, só que uma invertida (abba). str,str -> str."
    return a + b + b + a