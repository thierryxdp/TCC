# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """Dadas duas strings como entrada, a e b, a funcao retorna a concatenacao delas no formato abba;
    as strings de entrada devem vir entre aspas ('')
    """
    mensagem = a+b+b+a
    return mensagem