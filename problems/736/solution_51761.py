# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    """Concatena duas strings a e b no formato abba
    	str -> str
    	Parameters:
        a,b: Strings que serão somadas
        
        Returns:
        A soma das strings no formato abba
    """
    return a+b+b+a