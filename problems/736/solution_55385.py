# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, b):
    
#É feita uma soma entre a string a e a b, e após isso troca-se a ordem
#para haver uma inversão na ordem em que as strings estão, somando 
#b com a
    
    
    return str(a+b+b+a)