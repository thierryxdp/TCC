# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str, str -> str
def concatenacao(a, a):
    '''função que retorna duas strings "a" e "b" no formato "abba"
    valor de entrada:str
    valor de saída:str''' 
    return str(a)+str(b*2)+