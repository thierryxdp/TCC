def num_bombons (vt,pb):
    """função que calcula a quantidade de bombons possível de se comprar, dado o valor total em dinheiro (vt) e o preço do bombom(pb)
    float,float->float"""
    return (vt/pb)