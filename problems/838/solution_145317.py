def num_bombons(d,p):
    "Retorna o número de bombons de preço(p) possíveis de comprar comprar com uma quantia (d) de dinheiro."Entrada: int, float. Saída:int'
            if type(d)==int or type(d)==float
            return (d/p)-(d%p)