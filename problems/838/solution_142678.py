def num_bombons(d,p):
    """calcula a quantidade máxima de bombons que podem ser comprados, dados o dinheiro disponível (d) e o preço unitário do 
    bombom (p); float(em reais),float(em reais) -> int"""
    return d//p