def quant_palavras(frase):
    """A função diz qual é o número de palavras da frase, a partir da separação das palavras da frase.O parâmetro de entrada é uma string, e o parâmetro de síada é um int"""
    return len(str.split(frase)