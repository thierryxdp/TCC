def quant_palavras(frase):
    ''' funcao que dada uma frase, retorne o numero
    de palavras da frase, a frase pode ter espacos no
    inicio e no final
    str->int'''
    return len(str.split(frase))