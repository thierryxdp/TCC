def quant_palavras(frase):
    '''Retorna a quantidade de palavras da frase;
    str->int'''
    
    
    
    if frase[0]==' ':
        str.remove(frase)
    elif frase[1:]==' ':
        str.remove(frase)
    else:
        return str.split(len(frase),' ')