def quant_palavras(frase):
    """função que dado como parametro uma frase no formato string, retorna
    o número de palavras 
    str->int"""
     palavras=str.count(frase[1:len(frase)-1])
        return palavras