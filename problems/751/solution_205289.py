# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """Recebe uma frase e retorna a quantidade de palavras(Vale ressaltar que não tem como definir o que é uma palavra pela sua definição"""
    """string->int"""
    frase=str(frase)
    if any(char.isdigit() for char in frase))==True):
        print ("Frases não contém números")
    else:
        for i in range(len(frase)):
            if frase[i]==" " and i!=0 and i!=len(frase):
            	j+=1
     	print("A frase contém:")
        return j+1
    	print("palavras")