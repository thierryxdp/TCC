# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    '''Esta e a funcao que dado uma frase, retorna
    o numero de palavras da frase.
    str -> str'''
    str.split(frase)
    a = len(frase)
    return a