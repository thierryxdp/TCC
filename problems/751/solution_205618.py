# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """essa funcao, dada uma frase como parametro de entrada, retorna o numero de palavras da frase
    str -> int"""
    contador = str.split(frase)