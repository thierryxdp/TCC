# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """função que retorna a quantidade de frases de entrada, a partir da
    string(frase);
    string->int"""
    a = str.split("frase"," ")
    b = len(a)
    return b