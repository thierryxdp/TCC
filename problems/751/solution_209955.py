def quant_palavras(frase):
    '''Retorna a quantidade de palavras da frase;
    str->int'''
    
    len(frase)
    
    if frase[0]==' ':
        str.remove(frase)
    if frase[1:]==' ':
        str.remove(frase)
    else:
        return str.split(frase,' ')