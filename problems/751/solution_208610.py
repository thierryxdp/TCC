# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """recebe uma frase e retorna quantas palavras possui
    str->int"""
    return len(str.split(frase))