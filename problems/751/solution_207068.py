# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """usei split para cortar a frase e depois usei o len para saber a quantidade de palavras"""
    frase.split = x
    return len(x)