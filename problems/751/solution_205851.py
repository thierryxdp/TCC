def quant_palavras(frase):
    """funcao recebe uma frase (string) e retorna qunatas palavras compoem
    essa frase digitada pelo usuario."""
    str(frase)
    new_frase = frase.split()
    return len(new_frase)