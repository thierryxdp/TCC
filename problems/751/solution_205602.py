def quant_palavras(frase):
    """A partir de uma frase, string, retorna o numero de palavras que essa frase possui
       parameters:
       frase: string inicial 
       str->str"""
    return str.split(frase)