def quant_palavras(frase)
	"""função que a cada frase retorna o numero de palavras da frase.
	considere que a frase pode ter espaços no inicio e no fim.
    str -> int
    """
    return len(frase.split())