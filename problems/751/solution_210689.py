def quant_palavras(frase):
    """Defina é uma função que retorna a quantidade de palavras que possui na frase"""
    entrada: string 
    saida: str.split
    
        len(str.split(frase))
        return len(str.split(frase))