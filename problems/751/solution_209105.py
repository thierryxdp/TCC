# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    ''' 
        A função recebe uma frase(str) como parâmetro, gera uma lista com strings separadas por espaço e
        retorna a quantidade de palavras (int) da frase; string -> int 
        
    '''
    return len(frase.split( ))