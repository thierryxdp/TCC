def quant_palavras(frase):
    """função que retorna a quantidade de palavras contidas
    em uma frase, dada a frase como entrada
    string -> int"""
    str.split(frase) = frase_separada
    return len(frase_separada)