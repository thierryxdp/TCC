# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """Dada uma frase, a funcao retorna o numero de palavras da frase;
    Entrada: string
    Saida: int"""
    
    return str.split(str.strip(frase))