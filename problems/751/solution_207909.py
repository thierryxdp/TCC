# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    val = frase.split()
    return len(val)
    """função que separa a frase atribuída usando o espaço de referencia"""
    '''retorna o tamanho da lista que foi gerada com a função split atribuída a variável val'''