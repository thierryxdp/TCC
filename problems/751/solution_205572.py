# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string -> int
def quant_palavras(frase):
    """função que dada  um frase(string) retornar o numero de palavras da frase
    string => int"""
    lista = str.slip(frase)
    return len(lista)