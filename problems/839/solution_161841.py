from math import ceil
def carros (x, y):
     """calcula a quantidade de carros necessária, dado o número de pessoas"""
    x = capacidade do carro
    x = 5
    y = capacidade do carro
  return ceil (x/y)