def carros(p,c=5):
    """função que diz quantos carros de igual capacidade um grupo de amigos precisa levar para viajarem
    p é o número de pessoas e c é a capacidade por carro"""
    return p//c:
    if p%c>0, return (p//c)+1