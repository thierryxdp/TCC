def carros(n,a):
    '''define o número de carros necessários para uma viagem com n pessoas, caso os carros tenham uma capacidade diferente, será necessário informar o número de assentos a'''
    if a=[]:
        c=n/5
        return round(c + 0.5)
    else
    c=n/a
    return round(c + 0.5)