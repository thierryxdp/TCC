def carros(capacidade, pessoas = 5):
    tipo = type(capacidade/pessoas)
    int type(capacidade/pessoas) == float:
        return(int(capacidade/pessoas)+1)
    else:
        return(capacidade/pessoas)