def freq_palavras(frases):
    """
    Retorna a quantidade de vezes que as palavras aparecem na frase.
    str -> dict
    """
    d={}
	for p in frases:
        str.split(frases)
        str.count(frases,p)