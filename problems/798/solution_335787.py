def freq_palavras(frases):
    """
	Retorna quantas vezes a palavra apareceu na frase.
    str -> dict
    """
    palavras= str.strip.lower(frases)
    sort.palavras
    dicio={}
    repeticao=1
    for i in range(0,frases):
        if palavras[i] == palavras[i+1]:
            repeticao+=1
            dict.get(dicio,palavra[i],repeticao)
        else:
            repeticao= 1
        return dicio