def freq_palavras(frases):
    dic = {}
    posicao = palavra.find(palavra)
    for palavra in frases:
        if posicao >= 0 and n > 1:
        posicao = palavra.find(palavra, posicao + 1)
        dic = dic + palavra:posicao
    return dic