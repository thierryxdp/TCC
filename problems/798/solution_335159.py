def freq_palavras(frases):
    '''...'''
    
    lista=str.split(frases)
    dic=''
    chave=dict.keys(dic)
    valor=dict.values(dic)
    
    
    for len(lista) in dic:
        if len(lista)<dic[chave]:
            dic[chave]=valor
    return dic