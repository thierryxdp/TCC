def iter(iteravel,elem):
    soma = 0
    for numero in iteravel:
        if numero == elem:
            soma = soma + 1
    return soma

freq_palavras(frases):
    i = 0
    for i in range(len(frases)):
        if frases[i]
        
        i = i + 1
    return zip(