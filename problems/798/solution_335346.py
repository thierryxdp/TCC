def freq_palavras(frases):
    """função que retorna  o numero de vezes em que as 
    palavras foram repetidas:str->dict"""
    i=0
    D=[]
    lista=str.split(frases)
    while i<len(lista):
        list.append(D,[lista[i],list.count(lista,lista[i])])
        i=i+1
    return dict(D)