def freq_palavras(frases):
    '''função vice versa
    list -> int'''
    elemento = 0
    for caractere in frases:
        if letra == caractere:
            return lista
        
    while elemento < len(frases):
        
        if letra in frases [elemento]:
            
            um = str.index(frases,letra)
            list.insert(lista,um,letra)
            del lista[elemento+1]
            
        elemento+=1