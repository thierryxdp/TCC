def freq_palavras(frases):
    frase = str.split(frases)
    dict = {}
    contador = 0
    while i < len(frase):
        lista = list.count(frase,frase[i])
        lista = dict [frase[i]]
        contador = contador + 1
    return dict