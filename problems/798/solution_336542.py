def freq_palavras(frases):
    """Função que recebe uma string e retorna um dicionário onde
    cada palavra dessa string seja uma chave e tenha como valor
    o número de vezes que cada palavra aparece.
    str->dic
    """
    dic = {}
    primeiro = (frase.split())
    for elemento in string:
        dic[primeiro]=list.count(string,elemento)
    return dic