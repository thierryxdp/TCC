#função transforma uma string de palavras num dicionário contendo tanto as palavras quanto quantas vezses elas se repetiram
def freq_palavras(iteravel,elem):
    '''recebe uma frase e retorna um dicionario com a repetição das palavras da frase
    str-> dicionário'''
    qtd=0
    for valor in iteravel:
        if valor==elem:
            len(str.split(iteravel))
            qtd+=1
    return {freq_palavras(),qtd}