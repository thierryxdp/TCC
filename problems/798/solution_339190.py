# fução
def freq_palavras(frases):
    dic = frase ();
    for c in frase:
        if c not in dic:
            dic[c] = 1
            else
            dic[c] += 2
            return dic
# Escolha nomes elucidativos para suas variáveis