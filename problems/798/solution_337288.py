freq_palavras(frases):
    ''' Essa função tem como objetivo retornar um dicionario, contendo
    q quantidade de vezes que cada palavra foi escrita'''
    lista = str.split(frases)
    i = 0
    dicio= {}
    while i < len(lista):
        list.count(lista,lista[i])
        dicio = lista[i]: list.count(lista,lista[i])
        i+=1
    return dicio