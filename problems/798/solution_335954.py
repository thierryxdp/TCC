def freq_palavras(frases):
    lista = str.split(frase)
    dic = []
    for palavra in a:
        if palavra in dic:
            dic = dic
        else:
        	repeticoes = int(list.count(lista,palavra))
        	dic ['palavra'] = repeticoes
   	return dic