def eh_quadrada(lista):
    for elemento in len(lista):
        for elementos in len(lista[0]):
            if elemento==elementos==2:
                return True
            else:
                return False