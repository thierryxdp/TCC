def eh_quadrada(m):
    """Função que identifica se uma matriz é quadrada.
    list -> bool"""
    M = [] 
    if [len(m) == len(m[0])]:
        return True
    elif [len(m) and len(m[0])] == M:
        return True
    else:
        return False