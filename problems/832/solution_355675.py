def eh_quadrada(mat):
    '''a funç˜ao booleana para identificar se uma matriz é quadrada.
       list->bool'''
    if nLin==nCol or len(mat)==0:
    soma=0
    nLin=len(mat)
    nCol=len(mat[0])
    for i in range(nLin):
        for j in range(nCol):
            soma+=soma
    else:
        return False