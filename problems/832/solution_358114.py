def eh_quadrada(matriz):
    '''função que recebe uma matriz e verifica se ela é quadrada.
    list -> bool'''
    i=0
    linhas = len(matriz)
    colunas = len(matriz[0])
    if linhas == colunas:
        return True
    elif:
        matriz == []
    else:
        return False