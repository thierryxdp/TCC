def eh_quadrada(matriz):
    '''função que verifica se dada uma matriz ela é quadrada, ou seja, mesmo número de linha e colunas por meio de uma função booleana'''
    y= []
    if len(matriz)== len(matriz[0]):
        return True
    if matriz==y:
        return True
    else:
        return False