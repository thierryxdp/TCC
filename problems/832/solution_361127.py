def eh_quadrada(matriz):
    """Função que retorna se uma função é quadrada ou não. Entrada: Lista. Saida: strig"""
    i=0
    nlin = len(matriz)
    ncol = len(matriz[0])
	if nlin == ncol:
        Matriz= 'True'
    if nlin != ncol:
        Matriz ='False'
    if nlin=():
        Matriz ='False'
    
    return Matriz