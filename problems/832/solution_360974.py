def eh_quadrada(m):
    linhas = len(m)
    colunas = int
    if m == []:
        return True
    for i in range(linhas):
        if linhas == colunas:
            colunas = len(m[0])
            return True
    if linhas != colunas:
        return False