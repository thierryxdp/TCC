def eh_quadrada(matriz):
    '''recebe uma matriz e retorna se ela é quadrada.
    list ->bool'''
    if linham ==0:
        return quadrada
    linham =len(matriz)
    colunam =len(matriz[0])
    quadrada =True
    if linham ==0:
        return quadrada
    if linham ==colunam:
        return quadrada
    else:
        return not quadrada