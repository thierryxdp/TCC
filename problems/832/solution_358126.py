def eh_quadrada(matriz):
    '''função que recebe uma matriz e verifica se ela é quadrada.
    list -> bool'''
    
    matrizvazia = []
    linhas = len(matriz)
    colunas = len(matriz[0])
    
    if linhas == colunas or matriz = matrizvazia:
        return True
    else:
        return False