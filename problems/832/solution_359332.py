def eh_quadrada(A):
    numero=len(A)
    lista=[]
    if A==[]:
        return True
    for elem in range(numero):
        numero2=len(A[elem])
        list.append(lista,numero2)
    if sum(lista)==(len(A)*A[0]):
        return True
    else:
        return False