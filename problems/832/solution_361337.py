def eh_quadrada(matriz):
    '''Função que recebe uma matriz e retorna o valor booleano
    True se ela for quadrada e False caso contrário. Obs:Uma matriz
    vazia é considerada quadrada. list(list)->bool'''
    if:
        if len(matriz)==0 or len(matriz)==len(matriz[0]):
            return True
    else:
        return False