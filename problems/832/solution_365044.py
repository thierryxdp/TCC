def eh_quadrada(M):
    for i in range(len(M)):
        for j in range(len(M[0])):
            if len(M[0]) == len(M):
                return True
            if M == [ ]:
                return True
            else:
                return False