def eh_quadrada(matriz):
    matriz = []
    for i in range(len(matriz)):
            for j in range(len(matriz[0])):
                 if [i]==[j]:
                    return True
                 else:
                    return False