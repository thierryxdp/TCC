def eh_quadrada(matriz):
    '''Dado uma matriz, a função deve retornar True se a matriz
    é quadrada e False se não é, se a função for vazia, ela 
    deverá retornar True também;
    list(list)-> bool'''
    
    if(len(matriz) ==len(matriz[0]):
       return True
    
    else:
       return False