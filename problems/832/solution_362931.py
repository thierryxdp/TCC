def eh_quadrada(matriz):
    ''' Dada uma matriz, retorna se a matriz
    é quadrada ou não.
    list -> bool'''
    lin = len(matriz)
    col = len(matriz[0])
    for i in matriz:
        for j in range matriz i:
            if i == j or len(matriz) == 0:
                t = True
            else: 
                t = False
    return t