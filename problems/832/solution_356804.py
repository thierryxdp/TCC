def eh_quadrada(M):
    """Função que dada uma matriz indentifica se ela é quadrada ou não, sendo
    uma matriz vazia considerada quadrada.
    bool --> bool"""
    linhas = len (M)
    colunas = len (M[0])
    matriz = []
    if linhas == colunas or M == matriz:
        if matriz== matriz:
        return True 
    else:
        return False