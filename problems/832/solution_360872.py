def eh_quadrada(matriz):
    '''dada uma matriz, retorna se ela é quadrada
    list->bool'''
    
    coluna=len(matriz)
    
    for coluna in matriz:
        if len(coluna)= coluna:
            return False
    return True