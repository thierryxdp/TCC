def eh_quadrada(matriz):
    linhas = len(matriz)
    colunas = len(matriz[0])
    for i in range(linhas):
        list.append(linhas,[0]*colunas)
        for j in range(colunas):
            list.append(colunas,i)
            if colunas == linhas:
                return False
            else:
                return True