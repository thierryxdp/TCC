def eh_quadrada(matriz):
    ''''''
    
    for linha in matriz:
        if len(linha)!=len(matriz):
            return False
        if len(linha)==len(matriz):
            return True
        if (len(linha)=len(matriz))=0:
            return True