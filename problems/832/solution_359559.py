def eh_quadrada(matriz):
    nlin = len(matriz)
    ncol = len(matriz[0])
    if nlin == 0:
        return True
    for i in range(nlin):
        for j in range(ncol):
            if range(nlin) == range(ncol):
                return True
            else:
                return False