def eh_quadrada(matriz):
    '''Função que retorna True caso a matriz seja quadrada e False se não for.
    list -> bool'''
    
    
    linhas = len(matriz[i)
    colunas = len(matriz[i][j])
   	
    for i in range(linhas):
        if linhas != 0:
        	for j in range(colunas):
                if colunas != 0:
                    return False
                else:
                    return True