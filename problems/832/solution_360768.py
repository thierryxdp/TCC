def eh_quadrada(matriz):
    somador = 0
    somador2 = 0
    for contador in range(len(matriz)):
        somador[contador] = somador + 1
        for contagem in range(len(matriz)):
            somador2[contagem] = somador2 + 1
	somador == somador2