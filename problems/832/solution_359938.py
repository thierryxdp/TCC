def eh_quadrada(matriz):
    """Identifica se uma matriz ( é quadrada ou não
    matriz -> list
    """
    if matriz == []:
        return True
    else:
        return True if len(matriz) == len(l[matriz]) else False