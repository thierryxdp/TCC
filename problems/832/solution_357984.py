def eh_quadrada(matriz):
    total = 0 
    for i in len(matriz):
        for j in len(matriz[i]):
        	total = total + 1
            if len(matriz[i]) = len(matriz[i][j]):
                return True
            else:
                return False