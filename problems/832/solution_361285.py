def eh_quadrada(m):
    """Retorna um valor booleano que garante se uma matriz m
    dada como entrada é quadrada ou não, ou seja, se possui
    o mesmo número de linhas e colunas.
    list(list) -> bool"""
    if m = []:
        return True
    elif len(m) == len(m[0]):
        return True
    else:
        return False