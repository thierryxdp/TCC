def eh_quadrada(m):
    numeroLinhas=len(m)
    numeroColunas=len(m[0])
    if m==[]:
        return True 
    else:
        if numeroColunas==numeroLinhas:
        	return True
    	else:
        	return False