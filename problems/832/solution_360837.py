def eh_quadrada(matriz):
    '''Dado uma matriz, caso seja quadrada, retorna True, e False caso contrario.
    list -> bool'''
    if len(matriz) == 0
    if len(matriz) == len(matriz[0]):
        return True
    else: 
        return False