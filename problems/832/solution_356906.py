def eh_quadrada(M):
    """Recebe uma matriz 'M' e identifica se ela é quadrada, retorna-se o valor booleano 'True' se ela for quadrada e 'False' caso contrário;list->bool"""
    if M==[[]]:
        return True
    elif len(M)=len(m[0]):
        return True
    else:
        return False