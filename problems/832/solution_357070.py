def eh_quadrada(matriz):
    linhas = 0
    colunas = 0
    for i in range(len(matriz)):
        linhas = linhas + 1
        for j in range(len(matriz[i])):
            colunas = colunas + 1
            if linhas == colunas
            return t