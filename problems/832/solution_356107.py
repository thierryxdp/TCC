def eh_quadrada(matriz):
    '''função que identifica se uma matriz de entrada é quadrada.
    list -> bool'''
    
    list.append (matriz, a)
    
    return matriz == [[a]] or len(matriz) == len(matriz[0])