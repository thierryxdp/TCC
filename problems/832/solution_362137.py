def eh_quadrada(matriz):
    '''retorna true ou false se for ou não matriz quadrada'''    
    linha=len(matriz)
    coluna=len(matriz[0])
    matriz[linha][coluna]
    for linha in range(0, ):
        for coluna in range(0, ):           
            if linha==coluna:
                return True
            else:
                return False