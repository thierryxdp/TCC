def eh_quadrada(matriz):
     "retorna a expressão booleana true se a matriz for uma matriz quadrada e a expressão booleana false caso contrario"
    linha = len(matriz)
    coluna = len(matriz[0])
    for i in matriz:
        if linha == coluna:
            return True
        if matriz[[],[]]
            return True
        else:
            return False