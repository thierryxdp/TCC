def eh_quadrada(list):
    """Identifica se uma matriz é quadrada ou não, retornando em valores booleanos
    lista --> boolean"""
    M=list
    if len(M)==len(M(0)):
        return True
    else:
        return False