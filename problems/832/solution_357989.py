def eh_quadrada(matriz):
    for i in len(matriz):
        for j in len(matriz[i]):
            if len(matriz) = len(matriz[i]):
                return True
            else:
                return False