def eh_quadrada(listas):
    matriz = []
    matriz 
    for i in range(len(listas)):
        for j in range(len(listas[i])):
            if len(i) == len(j):
                return True
           	else:
                return False