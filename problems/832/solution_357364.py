def eh_quadrada(matriz):
    "funcao que identifica se a matriz é quadrada ou não. list(list)->bool"
    linha=len(matriz)
    coluna=len(matriz[0])
    for i in matriz:
        if linha==coluna:
            return True
        if eh_quadrado==9[0]):
            return True
        else:
            return False