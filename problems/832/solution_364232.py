def eh_quadrada([[]]):
    '''
    '''
    matriz = []
    for num_linha in range(m):
        linha = []
        for coluna in range(n):
            if coluna == num linha:
                return True
            else:
                return False