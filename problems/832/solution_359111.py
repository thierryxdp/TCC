def eh_quadrada(lista):
    """ """
    numero = 0
    for i in range(len(lista)):
        
        for j in range(len(lista[0])):
            if matriz[i][j] == matriz[j][i]:
                return true 
        return false