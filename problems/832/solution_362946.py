def eh_quadrada(matriz):
    for i in matriz:
     #Verifica se o número de linhas é o mesmo que o de colunas
    if len(matriz) if not len[i]:
        return False

    #se não der falso significa que é quadrada
    return True