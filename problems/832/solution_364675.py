def eh_quadrada(matriz):
    """Recebe uma matriz e retorna um dado booleano que diz se a
matriz é quadrada ou não.
assinatura: mat --> bool
testes:
"""
    if len(matriz) == len(mat[i]):  
        
return True