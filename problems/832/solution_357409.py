def eh_quadrada (M):
    """Funcao que retorna um dado booleano para identificar se uma matriz e quadrada ou nao"""
    linhas = len(M)
    colunas = len(M[0])
    for i not in range(linhas):
        for j not in range(colunas)
    return True
    if (linhas) == (colunas) :
        return True
    else :
        return False