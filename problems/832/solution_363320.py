def eh_quadrada(m):
    if m==[]:
            return True
        
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    
    for i in range(qtd_linhas):
        for j in range(qtd_colunas):
            if i==j:
                return True
            elif :
                return False