def eh_quadrada(matriz):
    """retorna se a matriz e quadrada ou não
    list-> bool"""
    if matriz == []
        return True
    for i in matriz:
        if len(i) == len(matriz):
            return True
        else:
            return False