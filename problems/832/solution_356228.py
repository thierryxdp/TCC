eh_quadrada(matriz):
    """ Função que identifica se a matriz é quadrada ou não
    
    """
    linhas = len(matriz)
    if linhas == 0:
        return True
    colunas = len(matriz[0])
    if a==b:
        return a==b
    else:
        return a==b