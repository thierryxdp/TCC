def eh_quadrada(lista):
    """ define se uma matriz é quadrada;lista->bool"""
    lista2=[]
    for linha in lista:
        if len(linha)==len(lista):
            list.append(linha,lista2)
    return lista==lista2