def eh_quadrada(x):
    'devolve um valor booleano para a matriz. Se ela for quadradra renorna sim, caso contario retorna nao. list->valor booleano'
    x1=='é quadrada'
    if len(x) == len(x[0]):
        return  x1 == 'é quadrada'
    else:
        return x1 == 'não é quadrada'