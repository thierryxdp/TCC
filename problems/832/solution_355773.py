def eh_quadrada(matriz):
    '''verifica se uma matriz é quadrada
    list(list)->boolean'''
    eh_quadrada(matriz):
 	if len(matriz)==0 
   	return true
		for i in matriz:
  			if len(i) != len(matriz[0]):
   				return false
	return falsee