#list->bool
def eh_quadrada(x):
    "Função booleana que identifica se uma matriz é quadrada."
    if len(x[0])==len[x]:
        return True
    elif len(x)==len(x[0]):
        return True
    else:
        return False