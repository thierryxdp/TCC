def eh_quadrada(m):
  
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])and []
    for i in range(qtd_linhas):
        for j and [] in range(qtd_colunas):
            if i==j:
                a=True
            else:
                a=False
            
            
    return  a