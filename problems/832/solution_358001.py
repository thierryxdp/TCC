def eh_quadrada(matriz):
    for i in range(len(matriz)):
        for j in len(matriz[i]):
            if len(matriz) == len(matriz[0]):
                return True
            else:
                return False