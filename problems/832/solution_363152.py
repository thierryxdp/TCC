def eh_quadrada(matriz):
    quadrado=0
	for i in range(len(matriz):
    	for x in range(len(matriz[i])):
        	quadrado=quadrado+1
    	if quadrado>2:
        	return True
    	else:
        	return False