def eh_quadrada(matriz):
    """função que identifica se uma matriz é quadrada"""
    lin = len (matriz)
    if lin == 0:
        return true
    col = len(mat[0])
    if lin == col:
        return true
    else:
        return false