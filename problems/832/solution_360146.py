def eh_quadrada(matriz):
    '''função que dada uma matriz retorna se ela é quadrada ou não:matriz->bool'''
    linha = [0]
    coluna = [0]
    while len(linha) < len(coluna):
        return False
    if len(linha)%len(coluna) == 0:
        return True