def eh_quadrada (matriz):
    ''' funcao booleana que retorne True se uma matriz for quadrada (cujo numero de linhas e o mesmo de colunas) ou vazia, caso contrario e False
    int -> bool '''
    return (True) or (False)