#Questão 1
def eh_quadrada(matriz):
    """Função que define se uma matriz é quadrada ou não,
    True para sim e False para não;
    list -> bool"""
    for i in matriz:
        for j in matriz[i]:
            if len(i) == len(matriz[j])
                return True
            else:
                return False