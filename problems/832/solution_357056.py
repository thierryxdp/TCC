def eh_quadrada(m):
    linha=0
    coluna=0
    for i in len(m):
        linha=linha+1
        for j in len((m)[0]):
            coluna=coluna+1
     if len(range(linha)=len(range(coluna)
     return sim