def eh_quadrada(m):
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    for i in range(qtd_linhas):
        for j  in range(qtd_colunas):
            b=[]
            if i==j:
                a=True
            elif m==[]:
                a=True
            else:
                a=False
           
    return  a