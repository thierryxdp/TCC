def eh_quadrada(matriz):
    for l in range (len(matriz)):
        for c in range (len(matriz[l])):
            if (matriz[l] == matriz [c])
                return True
            else:
                return False