def eh_quadrada(m):
    linhas = len(m)
    colunas = int
    if matriz == []:
        colunas = linhas
        return True
    for i in range(linhas):
        colunas = len(m[0])
        if (linhas == colunas):
            return True
    if linhas != colunas:
        return False