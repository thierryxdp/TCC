def eh_quadrada(matriz):
    linhas = len(matriz)
    colunas = len(matriz[0])
    for i in matriz:
        for j in linhas:
            if linhas == colunas:
                return True
            else:
                return False