def eh_quadrada(matriz):
    '''identifica se a matriz dada na entrada é quadrada ou não;
    list(list)->bool'''

    for l in range(len(matriz)):
        for c in range(len(matriz[l])):
            if len(matriz) == len(matriz[l]):
            elif matriz == []:
                return True
            else:
                return False