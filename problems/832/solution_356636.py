def eh_quadrada(M):
    """Identifica se uma matriz é quadrada ou não, retornando em valores booleanos
    lista --> boolean"""
    if len(M)-1==len(M[0])-1:
    	return True
    else:
        return False