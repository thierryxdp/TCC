def eh_quadrada(matriz):
    """Funcao que identifica se uma matriz e quadrada ou nao tendo como
    entrada uma matriz e retornando True se for quadrada e False se nao"""
    linha = len(matriz)
    coluna = len(matriz[0])
    if linha == coluna
        return True
    else
        return False