def eh_quadrada(matriz):
    """Função que retorna um valor booleano sobre uma matriz se quadrada ou não
    int -> bool"""
    if len(matriz) == len(matriz[i]):
        return True
    else: 
        return False