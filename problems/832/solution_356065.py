def eh_quadrada(matriz):
    """Fumção que recebe uma matriz, retornando se essa matriz é quadrada ou não
    entrada: list(list)
    retorno: bool"""
    matriz[]= True
    nlin= len(matriz)
    ncolun= len(matriz[0])
    if nlin == ncolun:
        return True
    else:
        return False