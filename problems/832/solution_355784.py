def eh_quadrada(matriz):
    
    quadrada=[]
    
    for i in range(len(matriz)):
        for j in range(len(matriz[i])):
            if len(matriz)==len(matriz[i]):
                list.append(quadrada,1)
    if 1 in quadrada:
        return True