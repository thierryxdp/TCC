def eh_quadrada(matriz):
    l = len(matriz)=[]
	i = range(len(matriz))
    j = range(len(matriz[0]))
    for n in matriz:
        if l in matriz:
            return True
        if i!=j:
            return False
        else:
            return True