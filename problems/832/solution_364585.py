def eh_quadrada(matriz):
    """ Funçao que identifica se uma matriz é quadrada"""
    
    nlin=len(matriz)
    ncol=len(matriz[0])
    
    if len(matriz)>0 
    	if (nlin==ncol:)
    		return True
    elif (len(matriz)==0
    	return True
    else:
		return False