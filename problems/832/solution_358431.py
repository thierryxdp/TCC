def eh_quadrada(matriz):
    "retorna true se a matriz for uma matriz quadrada e false caso contrario"
    linha = len(matriz)
    coluna = len(matriz[0])
    for i in matriz:
        if linha == coluna:
            return True
        else: matriz[]
            return False