def eh_quadrada(M):
    '''
    retorna, em booleano, se uma matriz é quadrada ou não
    list -> bool
    '''
    saida = bool
    if len(M)==len(M[0]):
        saida = True
    elif:
        saida = False
        
    return saida