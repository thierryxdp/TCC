def eh_quadrada(matriz):
    """Retorna True se uma dada matriz for quadrada e False caso contrario"""
    #ent int(list) saida bool
    if len(matriz)>0 and len(matriz)=[]:
        if len(matriz) == len(matriz[0]):
            return True
        else :
            return False