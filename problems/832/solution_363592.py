def eh_quadrada(matriz):
    '''diz se a matriz dada é quadrada
    matriz vazia também é quadrada
    list -> bool
    '''
    for i in range(0,len(matriz)):
        for j in range(0,len(matriz[i]):
            if len(matriz) == len(matriz[i]) and len(matriz[coluna]:
                return True
            else:
                return False
    return True