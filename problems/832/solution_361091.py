def eh_quadrada(matriz):
    '''funcao que identifica se uma matriz dada como entrada e quadrada, retornando true se for e false se nao;
       list(list)-> bool'''
    matriznaoquadrada= []
    if matriz == matriznaoquadrada:
        return True
    else:
        return False