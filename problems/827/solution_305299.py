def qtd_divisires(num):
    """Funcao que calcula e retorna a quantidades de divisores de um determinado numero(num);
    int->int"""
    for n in rang(1,num+1):
        if (num%n)==0:
            div.append(i)
    return len(divs)