def qtd_divisoresFor(X):
	"""Funcao que dado um determinado numero, retorna a quantidade de
	divisores que ele tem."""
	entrada = (5)
	for proximo in X:
            if proximo%2 == 0
        for proximo X+1:
            if proximo%2 == 0
        return divisores