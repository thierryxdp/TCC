def qtd_divisores(n):
    
    lista = list(range(1,n+1))
    i=0
    L = []
    
    for e in lista:
        if (n%lista[i])==0:
            list.count(lista,lista[i])
        i+=1
        return lista