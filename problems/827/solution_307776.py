qtd_divisores (x):
    """
    	Função que calcula quantos divisores tem o número 
        dado.
        int -> int
    """
    divisores = []
    for i in range(10):
        if i%x == 0:
            list.append(divisores,x)
    return divisores