def qtd_divisores (i):
    '''Calcula quantos divisores tem um inteiro i, int->int'''
    divisores = []
    for elemento in range(i):
        if i%(elemento+1)=0
        divisores = divisores + [elemento + 1]
    return len(divisores)