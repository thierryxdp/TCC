def qtd_divisores(numero):
    
    N = range(numero+1)
    divisores= []
    
    for divisor in N:
        if divisor % numero = 0:
            list.append(divisores,divisor)
    
    return divisores