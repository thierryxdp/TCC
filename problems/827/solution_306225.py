def qtd_divisores(n):
    
    lista = list(range(1,n+1))
    i=0
    L = []
    divisores=list.count(list.append(L,lista[i]))
    
    for e in lista:
        if (n%lista[i])==0:
            list.append(L,lista[i])
            list.count(L,lista[i])
        i+=1
    return divisores