def qtq_divisores(numero):
    """retorna a quantidade de divisores q um numero tem"""
    x=1
    lista=[]
    while x<=numero:
    if numero%x=0:
        list.append(lista,x)
    x=x+1
    return len(lista)