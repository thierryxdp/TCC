def qtd_divisores(numero):
    '''Retorna quantos divisores um numero tem;
       Entrada: int;
       Saida: int;
    '''
    divisor = 1
    if divisor <= numero:
        if numero%divisor == 0:
            lista = []
            list.append(lista, divisor)
    return len(lista)