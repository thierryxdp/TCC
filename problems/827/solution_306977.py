def qtq_divisores(numero):
    """retorna a quantidade de divisores q um numero tem"""
    x=0
    lista=[]
    while x<=numero:
    if 10%x=0:
        list.append(lista,x)
    x=x+1
    return len(lista)