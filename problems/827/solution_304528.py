def qtd_divisores(numero):
    lista = range(numero)
    pos = 0
    lista_divisores = []
    for elemento in lista:
        if numero/(elemento+1) == int():
            list.append(lista_divisores,numero/(elemento+1))
        pos = pos + 1
    return len(lista_divisores)