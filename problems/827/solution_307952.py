def qtd_divisores(numero):
    
    N = range(numero+1)
    divisores= []
    
    for divisor in N:
        if 0 = divisor / numero:
            list.append(divisores,divisor)
    
    return divisores