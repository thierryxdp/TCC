def qtd_divisores(n):
    """funcao que calcula a quantidade de divisores de um numero."""
    lista=[]
    for x in range(10):
        if n%x==0:
            list.append(lista,x)
    return len(lista)