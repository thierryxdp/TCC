def melhor_volta(x):
    '''Dada uma matriz 6x10 com os tempos, em segundos, dos corredores em cada volta, retorna de quem foi a melhor volta, com qual tempo e em qual volta.
    list -> tuple'''
    melhor_de_cada_j=[]
    for i in range(len(x)):
        #Todas as voltas de i jogadores.
        for j in range (len(x[i]):           
        	list.append(melhor_de_cada_j,min(x[i]))
        	#Coloca em uma lista o melhor tempo de cada jogador.
    #Terá o tempo gasto.
    for a in range(len(x)):
        for b in range(len(x[a])):
        	v=list.index(min(melhor_de_cada_j)
    #V = índice da melhor volta
    return (min(melhor_de_cada_j),v)