def melhor_volta(matriz):
    menores_tempos = ()
    for i in matriz:
        menor_tempo = min(i)
        menores_tempos += (menor_tempo,)
        for j in i[min(melhores_tempos)]:
            melhor_volta = find(min(i))
        return min(melhores_tempos),find(min(melhores_tempos)),melhor_volta