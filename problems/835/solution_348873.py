def melhor_volta(matriz):
    lista=[]
    for i in range(len(matriz)):
        z=min(matriz[i])
        list.append(lista,z)
    menor=min(lista)
    quando=list.index(lista,menor) +1
    return quando,menor