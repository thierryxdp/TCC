def melhor_volta(m):
    tupla = ()
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    
    a=sum(m[0])
    b=sum(m[1])
    c=sum(m[2])
    d=sum(m[3])
    e=sum(m[4])
    f=sum(m[5])
    g=
    h=
    i=
    j=
    k=
    l=
    m=
    n=
    o=
    p=
    
    
    for i in range(6):
        for j in range(10):
           
                tupla = sum(m[5][5])

    return tupla