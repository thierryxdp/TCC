def melhor_volta(mat):
    if len(mat) == 6 and len(mat[0]) == 10:
        bests = []
        for x in mat:
            for y in x:
                list.append(bests,min(x))
    return list.index(mat(min(bests)),min(bests)