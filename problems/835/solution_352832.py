def melhor_volta(m):
    tupla = ()
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    
    
    
   
    for i in range(6):
        for j in range(10):
            if i and j in m:
                list.sort(m[4])
                p=m[4][0]
                b=list(map(sum, m))
            
    return   list.sort(m[4])