def melhor_volta(matriz):
    lista = []
    for i in matriz:
        for c in i:
            list.append(lista,c)
        menor = min(lista)
        index = lista.index(menor)
    return (index, lista)