def melhor_volta(m):
    """função q calcula e retorna o vencedor de uma corrida: matriz->tuple"""
    tempos_minimos=[]
    voltas=[]
    for i in range(len(m)):
        tempo=[]
        for j in range(len(m[i]):
                       list.append(tempo,c[i][j])
                       if len(tempo)==10:
                       list.append(tempos_minimos,min(tempo))
                       list.append(voltas,list.indext(tempo,min(tempo)))
    campeao=list.index(tempos_minimos,min(tempos_minimos))+1
    tempo_campeao=in(tempos_minimos)
    volta_campea=list.index(voltas,campeao)
                       return tuple(campeao,tempo_campeao,volta_campea)