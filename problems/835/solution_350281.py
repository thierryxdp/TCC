def melhor_volta(tempos):
    """retorna de quem foi a melhor volta da prova, com qual tempo e em que volta;
    list(list) -> tuple"""
    melhor_volta_por_corredor=m
    m=[]
    i=0
    while i<len(tempos):
        x=list(i+1,min(tempos[i]),list.index(min(tempos[i]))
        list.append(m,x)
        i=i+1
    
    return m