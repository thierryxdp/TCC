def melhor_volta (x):
    n_linhas=len(x)
    n_cols=len(x[0])
    tupla=()
    for i in range(n_linhas):
        for j in range(n_cols):
    		for el in (n_cols):
       			a=min(n_cols)
    tupla=((i),(a),(j),)
    return tupla