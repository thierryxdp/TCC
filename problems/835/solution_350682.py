def melhor_volta(m):
    '''Função que calcula e retorna a tupla de quem foi a melhor volta , com qual tempo e em que volta. list(list)-> tuple'''
    for i in range(len(m)):
        for j in range(len(m[i])):
            if m[i][j] = min(m[i][j]):
                v = len(m[i][j])
                c = len(m[i][j][i])
    t = 1
    return (c, t, v)