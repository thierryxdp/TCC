def melhor_volta(lista):
    soma = 0
    l = []
    for x in lista:
        for y in range(len(x)):
            l. append(x[y])
    menor_tempo = min(l)
    for x in lista:
        for y in range(len(x)):
            a = lista.index(menor_tempo)
    return menor_tempo, a