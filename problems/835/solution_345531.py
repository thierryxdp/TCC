def melhor_volta(matriz):
    corredor1=[]
    corredor2=[]
    corredor3=[]
    corredor4=[]
    corredor5=[]
    corredor6=[]
    listatotal=[]
    contlinha = 0
    contcoluna = 0
    for i in range(len(matriz)):
        for j in range(len(matriz[i])):
            list.append(listatotal,matriz[i][j])
            
        
    for i in range(len(matriz)):
        contcoluna = 0
        for j in range(len(matriz[i])):
            if matriz[0][j] != min(listatotal):
                contcoluna = contcoluna + 1
            elif matriz[1][j] != min(listatotal):
                contcoluna = contcoluna + 1
            elif matriz[2][j] != min(listatotal):
                contcoluna = contcoluna + 1
            elif matriz[3][j] != min(listatotal):
                contcoluna = contcoluna + 1
            elif matriz[4][j] != min(listatotal):
                contcoluna = contcoluna + 1
            elif matriz[5][j] != min(listatotal):
    
    
 	return contcoluna