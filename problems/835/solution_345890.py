def melhor_volta(matriz):
    lista=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[0])):
            resposta=min(matriz[i][j])
            list.append(lista,matriz)
    return lista