def melhor_volta(matriz):
    """ define a mlhor volta entre seis corredores e quem deu essa volta;list->tuple"""
    resposta=[]
    for volta in matriz:
        list.append(resposta,min(volta)):
            for tempo in volta:
                if resposta[0] == tempo:
                    resposta2=list.index(tempo,resposta[0])
    return (resposta[0],resposta2)