def melhor_volta (lista):
    'dada uma lista com os tempos de corredores numa oista de kart, retorna o menor tempo e o motorista que o fez, e a volta que ele foi feito. str -> str'
    count = 0
    tempos = []
    for i in range(len(lista)):
        for b in range(lista[0]):
        tempos = tempos.append(b)
        a= min(b)
    return a