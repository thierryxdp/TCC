def melhor_volta(m):
    final = ()
	for i in range(len(m)):
		soma = 0
		for j in range(len(m[0])):
			soma += m[i][j]
		list.append(somas,soma)
	minimo = min(somas)
	pos = list.index(somas,maior)
	return m[pos],maior