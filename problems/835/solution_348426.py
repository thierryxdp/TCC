def melhor_volta(matriz):
    retorno = (0,float('inf'),0)
     for linhas in range(6):
        for colunas in range(10):
            if matriz[linhas][colunas] < tupla[1]:
            tupla = (linhas+1,matriz[linhas][colunas],colunas+1)
    return float('inf')