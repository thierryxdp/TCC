def melhor_volta(m):
    tupla = ()
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    
    
    
    p=(m[4][0])
    for i in range(6):
        for j in range(10):
            a = list.sort(m[4])
            b=list(map(sum, m))
            c=list.index(m,530)
    return  list.index(m,530)