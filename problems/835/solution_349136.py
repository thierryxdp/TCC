def melhor_volta(matriz):
    resultado=[]
    for i in matriz:
        tempo=min(i)
        temfi=min(t)
    #quem=index[tempo]
    #resultado.append(quem)
    	resultado.append(temfi)
    return tuple(resultado)