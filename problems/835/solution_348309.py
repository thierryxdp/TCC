def melhor_volta(matriz):
    """
"""
    num=0
    lista=[]
    lista2=[]
    for num in range(len(matriz)):
        lista.append(min(matriz[num]))
    
    #for num in range(len(matriz)):
    #    if min(lista) == min(matriz[num]):
    return lista