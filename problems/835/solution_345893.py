def melhor_volta(matriz):
    lista=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[0])):
            matriz[i][j]=funcao
            resposta=min(funcao)
            list.append(lista,funcao)
    return lista