def melhor_volta(matriz):
	i=0
    listaAux=[]
    lista=[]
    listaAux2[]
    c=1
    for corredores in matriz:
        menorTempo=min(corredores)
        i=list.index(corredores,menorTempo)
    	listaAux.append(i)
        listaAux.append(menorTempo)
    while c <len(listaAux):
      	listaAux2.append(listaAux(c))
        c+=2
    return listaAux2