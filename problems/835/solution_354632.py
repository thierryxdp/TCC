def melhor_volta(matriz):
    mt=[]
    for i in range(len(matriz)):
        list.append (mt,min(matriz[i]))
    quem=list.index(mt,min(mt))+1
    menort=min(mt)
    volta=list.index(matriz,min(mt))+1
    return (quem,menort,volta)