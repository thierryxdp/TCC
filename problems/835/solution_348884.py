def melhor_volta(matriz):
    lista=[]
    for i in range(len(matriz)):
        z=min(matriz[i])
        list.append(lista,z)
    menor=min(lista)
    quando=list.index(lista,menor) +1
    volta=list.index(matriz[quando],menor)
    return quando,menor,z