def melhor_volta(matriz):
    lista=[]
    indices=[]
    for i in range(len(matriz)):
            tempo=min(matriz[i])
            list.append(lista,tempo)
            x=list.index(matriz[i],tempo)
            list.append(indices,x+1)
    resultado=min(lista)
    y=list.index(indices,resultado)
    return indices