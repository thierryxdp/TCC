def melhor_volta(m):
    l=[]
    
    for i in range len(m):
        for j in range len(m[0]):
            l=l+m[i][j]
    a=min(l[]
    b=min(l[]
    c=min(l[]
    d=min(l[]
    e=min(l[]
    f=min(l[]