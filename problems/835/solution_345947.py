def melhor_volta(matriz):
    lista=[]
    resultado=0
    
    for i in range(len(matriz)):
            tempo=min(matriz[i])
            list.append(lista,tempo)
   	resultado=min(lista)
    return resultado