def melhor_volta(s):
    '''funçao que recebe como uma
    entrada uma matriz e retorna uma tupla
    informando de quem foi
    a melhor volta da prova, com qual
    tempo e em que volta.
    list->tuple'''
    tempo=[]
    for l in s:
        tm = min(l)
    list.append(tempo,tm)
    x = min (tempo)
    corredor = list.index(tempo,x)+1
    for l in range (len(s)):
    for c in range(len(s[l])):
    if x == m[l][c]:
        v=c+1
    return (corredor.x,v)