def melhor_volta(matriz):
    """dada uma matriz 6 x 10 com os tempos em segundos de 10 corredores de Kart para cada uma de suas 6 voltas, retorna uma tupla informamdo: de quem foi a melhor volta da prova, com qual tempo e em que volta,
		(list) -> tuple"""
    minimos=[]
    for i in range(len(matriz)):
     	list.append(minimos,min(matriz[i])
  	a= min(minimos)
  	for x in range(len(matriz)):
      	if a in matriz[x]:
        	b=i+1
  	c=list.index(matriz[b-1],a)
  	return (b,a,c)