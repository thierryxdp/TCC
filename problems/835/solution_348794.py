def melhor_volta(matriz: list) -> tuple:
    
    lista_de_minimos = []
    linhas = len(matriz)
    colunas = len(matriz[0])
    
    for i in range(linhas):
        lista_de_minimos += min(linhas[i]),
        
    return lista_de_minimos