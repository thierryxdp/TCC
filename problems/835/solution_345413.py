def melhor_volta(matriz):

    linhas = len(matriz)
    colunas = len(matriz[0])
    volta = []
    
    for i in range(linhas):
        for j in range(colunas):
            list.append(volta,min(matriz[i][j]))
    return volta