def melhor_volta(matriz):

""" função que recebe como argumento uma matriz com dados sobre os corredores,
suas voltas e tempos e retorna os dados com a melhor volta """

menosresTempo = []
menorTempo = 0
volta = 0
corredor = 0
for linha in matriz:
	menoresTempos.append(min(linha))
    menorTempo = min(menoresTempos)
    corredor = list.index(menoresTempos, manorTempo)
    if menorTempo in linha:
        volta = list.index(linha, menorTempo)
return (jogador + 1, menorTempo, volta + 1)