def melhor_volta(matriz):
    """Retorna a melhor volta,o tempo e qual volta
    Entrada: list(list)
    Saída: tuple
    """
    best= []
    for i in range(len(matriz)):
            list.append(best, min(matriz[i]))
    a = list.index(best, min(best))
    b = min(best)
    c = list.index(matriz[a], b)+1
    return a+1, b, c