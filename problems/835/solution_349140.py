def melhor_volta(matriz):
    resultado=[]
    for i in matriz:
        tempo=min(i)
        for i in tempo:
            tempofi=min(tempo)
        
    #quem=index[tempo]
    #resultado.append(quem)
    		resultado.append(tempofi)
    return tuple(resultado)