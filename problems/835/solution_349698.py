def melhor_volta(matriz):
    """Retorna em ordem: o piloto que fez a melhor volta, o tempo da volta e a ordem.
    Entrada: list(list)
    Saída: tuple
    """
    melhores = []
    for i in range(len(matriz)):
        list.append(melhores, min(matriz[i])
    return list.index(min(melhores))+1, min(melhores)