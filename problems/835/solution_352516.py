def melhor_volta(m):
    voltas= len(m[0])
    pessoas = len(m)
    melhor_da_vez=min(m[1])
    tempo= 0
    for i in range(pessoa):
            if min(m[i])<melhor_da_vez:
             volta =list.index(min(m[i])+1
             tempo = min(m[i])
             pessoa= i+1
             melhor_da_vez = min([i])
                               
    return (pessoa,tempo,volta)