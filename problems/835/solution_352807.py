def melhor_volta(m):
    tupla = ()
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    
    
    
    p=(m[4][0])
    for i in range(6):
        for j in range(10):
            a = list.sort(m[5])
            p=(m[5][0])
            b=list(map(sum, m))
            
    return   list.index(list(map(sum, m)) ,530),p