def melhor_volta(matriz):
    x = []
    for i in matriz:
        list.append(x, min(i))
        a = list.index(x, min(x))
    b = list.index(matriz[a], min(x))    
    y = (a + 1, min(x), b + 1)    
    return y