def melhor_volta(matriz):
    resultado=[]
    for i in matriz:
        tempo=min(i)
    #quem=index[tempo]
    #resultado.append(quem)
    	resultado.append(tempo)
        tempf=min(tempo)
        
    return tuple(resultado)