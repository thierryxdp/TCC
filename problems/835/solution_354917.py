def melhor_volta(Matriz):
    '''retorna tupla que diga melhor volta, qual tempo e qual volta
    list->list'''
    
    TempoVolta=[]
    
    for x in Matriz:
        TempoTotal=min(x)
        list.append(TempoVolta,TempoTotal)
    TempoCorredor=min(TempoVolta)
    Corredor=list.index(TempoVolta,TempoCorredor)+1
    
    for x in range (len(Matriz):
        for z in range (len(Matriz[1])):
                    if y==Matriz[x][z]:
                       VoltaCorredor=z+1
    return (Corredor,TempoCorredor,VoltaCorredor)