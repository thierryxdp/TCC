# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def colchao(medidas,H,L):
    """Para saber se o colchão passa pela porta, digite;"""
    x=H*L
    medida= int(tuple[0])
    medida1=int(tuple[1])
    medida2=int(tuple[2])
    y= medida*medida1*medida2
    if x<=y:
    	return True
	else:
       	return False