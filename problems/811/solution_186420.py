def colchao(medidas,H,L):
    """função que calcula e retorna se um colchao passam por uma porta
    list, int, int ->  bool"""
    [a,b,c] = medidas
    if c>= L and b<=H
        return True
    else:
        return False