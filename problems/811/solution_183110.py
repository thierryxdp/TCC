def colchao(medidas,H,L):
    """Função usada para resolver o problema, onde medidas é uma lista com os tamanhos inteiros, A, B e C e H e L são
    os tamanhos inteiros da altura e largura da porta, respectivamente."""
    
    int =  a, b, c, h, l, aux
    
            
    // ordenar a, b, e c
    if (a > b)
        aux = a 
        a = b;
        b = aux;
            
    if (b > c):
        aux = b 
        b = c;
        b = aux;
        
    if (a > b):
        aux = a 
        a = b;
        b = aux;
        
     // tornar l menor que h
    
    if (l > h):
       aux = h;
        h = l 
        l = aux
        
    if ( l >= a && h >= b )
         return("True/False"):
    
    else:
        return 0