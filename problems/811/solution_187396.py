def colchao (medidas, H, L):
    if medida[1] >= H:
        return false
    if medida[1] >= L:
        return false
    if medida[2] >= H:
        return false
    if medida[2] >= L:
        return false
    Else:
        true