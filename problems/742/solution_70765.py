# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    ''' substitui na string s o caractere na posicao x pela letra representada por i str,int,int->str'''
    return str(s).replace([x],str(i))