# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    
    ''' funcao que recebe uma strig s, um caractere x e um número inteiro i, entre 0 e o comprimento da string, e retorna uma strig igual a s, exceto que o elemento da posição i deve ser substiruido pelo caractere x
        str str int -> str
    '''    
    
    return str(s) + i + str(x)