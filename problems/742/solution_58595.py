# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui('stringA', caractere, inteiro):
    """Funcao que retorna uma string com um elemento substituido
    pelo caractere escolhido, na posição do numero inteiro escolhido.
    Entrada: string, int, int
    Saida: string
    
    parameters:
    stringA: string para ser utilizada
    caractere: caractere para ser colocado
    inteiro: posicao onde o caractere sera alocado
    """
    text = stringA
    
    return text.replace('inteiro', caractere)