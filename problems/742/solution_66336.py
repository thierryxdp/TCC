# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    """retorna a string s de entrada com o valor de posição i dela substituído pelo caractere x"""
    """string, int, int -> string"""
    return s.replace(str(s[i]),x)