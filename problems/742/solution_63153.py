def substitui(s,x,i):
    """essa função recebe uma frase, uma letra e uma posição e retorna a mesma frase com a letra substituida na posição pedida;
    str,str,int--->str"""
    return str.replace(s[i],x)