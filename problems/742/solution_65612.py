def substitui(s,x,i):
    """Funcao que ao receber, um string s, um caractere x e um numero inteiro i, retorna a string s: str, float,int=>str"""
    for index, value in enumerate(s):
    if value =i:
        s[index] = x