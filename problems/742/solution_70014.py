def substitui(s, x, i):
    ''' função que retorna a palavra dada por s substituindo a letra localizada na posição i por uma nova letra dada 'x'; str, str, int -> str'''
     nome1=str(x)
    return nome1[:i]+str(x)+nome1[i:]