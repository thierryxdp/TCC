# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    ''' retorna a string 's' com o caractere(x) no lugar(i) da palavra escolhida. i>=0 e i<= tamanho da string(s)
    return s[:i]+'x'+t[i+1:]