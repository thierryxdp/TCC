# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui (s,x,i):
    """ Introduz o número inteiro x na posição i no string s.
entrada: str, int, str
saida: str"""
   i=len(str(s))
   len[i] = x
    return str
    return str[0:1] + x + str[i+ 1:]