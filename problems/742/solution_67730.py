# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    '''dado s, o caractere da posição i é substituído pelo caractere x'''
    s = str()
    return s.replace(i,'x')