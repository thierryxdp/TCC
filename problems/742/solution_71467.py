def substitui(s,x,i):
    a = s
    b = x 
    c = i
    
    '''Dada uma string s, um caractere x e um número inteiro i sendo 0<i<comprimento da string. 
    Retorne a string s, porém o termo x será introduzido no índice i.
    '''
    	if str(a), str(b), int(0 < c < len(str(s)))
        	return str(%04s)%a