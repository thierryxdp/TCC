# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    '''Dado (s)->string, (x)->numero inteiro, (i)->inteiro entre zero 
    o comprimento da string, retorna(s,x,i),com o valor do i sendo substituido por x'''
    i=x
    return(s,x,i)