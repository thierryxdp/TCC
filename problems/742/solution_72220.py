# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    """retorna uma string igual a s, exceto que o elementoda posição i deve ser substiruido pelo caractere x. string, int, int -> string"""
    return str(s), but replace(i,'x')