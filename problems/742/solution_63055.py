# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    if (s!=str,0>i>len(s)):
        return 'Por favor verifique se a variavel "i" está entre 0 e o comprimento de "s"'
    else:
        return s+i+x