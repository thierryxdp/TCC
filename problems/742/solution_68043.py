# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    '''função que dado uma string s, um caractere x e um número inteiro
    i entre 0 e o comprimento da string, retorna uma string igual a s, 
    com i substituído pelo x.
    entrada: string, int, caractere
    saída: string, caractere'''
    if (0<=i<= len(s)):
        return tupla[0] and tupla[i]