# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    '''Recebe uma string , um caractere x e um número inteiro i entre 0 e o comprimento da 
    string.A função retorna a string s com a posição i alterada, substituído pelo caractere
   	string, int, int -> string 
    '''
    
    return str(s[,i] + tup(x) + s[i,])