# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    """recebe str s, um caracter x, e um número inteiro i 
    entre 0 e o comprimento da string e retorna uma str 
    igual a s, exceto que o elemento da posição i deve
    ser substituído pelo caracter x.
    str,str,int->str"""
    return s[:i]+,x,+s[i:]