# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    "dados: uma string 's', um caractere 'x' e a posição 'i', retorna a string s com o caractere que ocupa a posição i substituido pelo caractere dado. str, str,ont -. str"
    return s[0:(i)]+x+ s[(i+1):]