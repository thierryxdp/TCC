def substitui(s,x,i):
    #Entrada, dos três parâmetros, respectivamente,
    #definidos por string, x e um número inteiro que valha 
    #entre zero e o comprimento da string
    #Retornando uma string igual a s, contudo o i não pode ser x
    #str, str, int --> str
    return str = s and i == x