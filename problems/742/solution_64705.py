def substitui(s,x,i):
    """função que substitui 's','x' e 'i' para a formação de uma nova string"""
    """string, string, int -> string"""
    var = str(s)
    y = s
    y[i]
    x[i]
    return str(s) - str(y[i]) + str(x[i])