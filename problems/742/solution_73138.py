# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(string,caractere,numerointeiro):
    "função que retorna uma string igual a s"
    s=string(s,x,i[1])
    x=letra(s,x,i[2])
    i=int(s,x,i[3])
    d= s+x+i
    return d