# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    """Função que dados os parâmetros s,x,i, retorna a uma string igual a s, sendo o elemento da posição i substituido pelo caractere x, onde se tem str,str,int->str"""
    string=s
    caractere=x
    numeroInteiro=i[0:]
    numeroInteiro=caractere[0:]
    return string