# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):

#Establecida a string s e o valor i, deve parar a string antes que o
#i-ésimo termo chegue para substituí-lo pelo x, voltando a string s
#do ponto após o i e a finalizando

    return s[0:i]+ x + s[i+1:]