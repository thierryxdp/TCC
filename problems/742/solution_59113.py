# função que retorna uma string modifica a partir da substituição de um caracter por outro em uma posição também escolhida pelo usuário
# Escolha nomes elucidativos para suas variáveis
# string, int, int -> string
def substitui(s,x,i):
    return s.replace(s,x,i)