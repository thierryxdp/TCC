def substitui(s,x,i):
    '''Função que substituí a letra indicada pela posição
    determinada no i, a substituindo pela letra escolhida
    na variável x, sendo s a variável onde que coloca a palavra
    entrada=string,string int saida= string'''
   return s[0:i]+x+s[i+1:]