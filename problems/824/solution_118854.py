def uppCons(frase):
    '''funcao que recebe uma frase de entrada e retorna essa mesma frase cim todas as suas consoantes em letras maiusculas;
    str->str'''
    f = ' '
    prox_letra = 0
    while prox_letra < len(frase):
        if prox_letra =! 'a' or 'A' and 'e' or 'E' and 'i' or 'I' and 'o' or 'O' and 'u' or 'U':
            str.upper(frase[prox_letra])
        prox_letra = prox_letra + 1
    return f