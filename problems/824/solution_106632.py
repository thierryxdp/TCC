def uppCons(frase):
    '''recebe uma frase e retorna a frase com todas as suas consoantes em maiúsculas
    string -> strin'''
    novaFrase = ""
    indice = 0
    while indice < len(frase):
        if frase[indice] not in 'aeiouAEIOU':
            str.upper(i)
        novaFrase = novaFrase + str(indice)
        indice = indice + 1
    return novaFrase