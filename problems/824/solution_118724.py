def uppCons(frase):
    proximo=0
    frase_nova=''
    while proximo<len(frase):
        if frase[proximo]!='aeiouAEIOU'
           frase[proximo]=upper(frase[proximo])
        frase_nova=frase_nova+str(frase[proximo])
        return frase_nova