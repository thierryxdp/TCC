def uppCons(frase):
    """ Recebe a "frase" e  retorna todas as
    consoantes para maiúsculas """
    consoantes = "qwrtypsdfghjklçzxcvbnm"
    i = 0
    qtd = frase(len)
    lista = list(frase)
    lista2 = []
    while i <= qtd:
        if lista[i] in consoantes:
            list.upper(lista[i])
        list.append(lista2,lista[i])
        i += 1
   	return lista2