def uppCons(frase):
    """Função que recebe uma frase e retorna a mesma em maiúscula
    str -> str"""
    c = 0
    while c <= len(frase):
        if frase[c] == str.upper
        c+= 1
    return frase