def uppCons (frase):
    """Retorna a mesma frase de entrada com as consoantes em maiúsculas; str -> str"""
    str.upper(frase)
    vogais = [a, e, i, o, u]
    while x<len(frase):
        str.lower(vogais) in frase
    x=