def uppCons(frase: str) -> str:
    """ recebe uma frase e retorna outra frase com todas as suas consoantes
    em maiúsculas """
    
    total = ()
    i = 0
    
    while i < len(lista):
        if lista[i] =:
            total = total + (lista[i],)
        i = i + 1
        
    return list(total)