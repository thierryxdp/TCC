def uppCons(frase):
    i=0
    frase_certa =""
    while i<len(frase):
        if frase[i] in "bcdfghjklmnpqrstvwxyzç"
           frase_certa += frase[i].upper()
        else:
            frase_certa += frase[i]
        i += 1
    return frase_certa