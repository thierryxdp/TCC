def uppCons (frase):
    '''Funcao que recebe uma frase e retorna a mesma com todas as consoantes em maiuscula'''
    vogais = ['a', 'e', 'i', 'o', 'u']
    frase[i] == letra
    i = 0
    for letra in frase:
        if letra not in vogais:
            str.replace(letra, str.upper(letra), i)
            return frase