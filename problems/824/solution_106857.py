def uppCons(frase):
    """Para que seja retornado a quantidade de repetições de um
    número indicado, digite;
    int->int"""
    cons = 'B', 'C', 'D', 'Z', 'H', 'K', 'L', 'M', 'N', 'P', 'Q', 'R', 'S', 'T', 'V', 'X'
	while cons in frase:
    	if cons in frase:
        	return cons.upper(frase)
    	else:
        	return cons