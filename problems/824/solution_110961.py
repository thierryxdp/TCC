def uppCons(frase):
    """Dada uma frase, põe as consoantes em caixa
alta e o resto da frase igual str -> str """
    
    len(frase[:])
    a = frase.upper()
    g = a.find("A")
    h = a.find("E")
    j = a.find("I")
    k = a.find("O")
    l = a.find("U")
            
    for g in frase:
        
        return a.replace("A", "a")
    for h in frase:
        
        return a.replace("E", "e")
    for j in frase:
        
        return a.replace("I", "i")
    for k in frase:
        
        return a.replace("O", "o")
    for l in frase:
        
        return a.replace("U", "u")