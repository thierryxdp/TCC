# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def alternate_comb(list1, list2) :
    assert len(lista1) == len(lista2)
 
    n = len(lista1)
    combined = []
 
    for i in range(n):
    combined += lista1[i], lista2[i]
 
    return combined