def intercala(lista1, lista2):
    """Coloque um comentário dizendo o que a função faz e quais são os parâmetros de entrada e saída"""
    lista3 = list()
    for a,b in zip(lista1,lista2):
        lista3.append(a)
        lista3.append(b)
        return lista3
        if numeros da lista 1, antes de serem intercalados:
            return {lista1}
        if numeros da lista 2, antes de serem intercalados:
            return {lista2}                                            
        if numeros da lista 3, com numeros intercala:
            return lista1 + lista3