# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def intercala(lista1, lista2):
    """função que dadas duas listas de tamanho 3, gera uma nova lista formatada intercalando os elementos"""
    lista3 = ()
    if  sorted(lista3):
        list.extend(lista3,lista1)
        list.extend(lista3,lista2)
    return lista3