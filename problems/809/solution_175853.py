# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def intercala(lista1, lista2):
	"""Intercala os elementos de mesma posicao de duas listas de tres elementos
    tuple -> str"""
    a = str(lista1[0]) + '.' + str(lista2[0])
    b = str(lista1[1]) + '.' + str(lista2[1])
    c = str(lista1[2]) + '.' + str(lista2[2])
    lista = a + '.' + b + '.' + c
    return countList(lst1, lst2)