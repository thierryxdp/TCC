# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def intercala(lista1, lista2):
    """Essa função soma duas listas intercalando os valores das mesma
tem como entrada valores de L1 e L2 e retornam a intercalação L3
:L1: str, float, int -> list
:L2: str, float, int -> list
"""
def lista1(x,y,z):
    return [x, y, z]
def lista2(k,j,w):
    return [k, j, w]
def intercala(lista1,lista2):
    return 
lista3 = [lista1[0], lista2[0], lista1[1], lista2[1], lista1[2], lista2[2]]