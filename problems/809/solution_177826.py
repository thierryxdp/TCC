# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def intercala(lista1, lista2):
    """
    A função recebe duas listas com mesmo número de elementos e retorna uma 
    lista3 intercalando os elementos da lista1 e lista2, começando pela primeira (lista1);
    list, list -> list
    """
    l3 = []
    if len(lista1) != len(lista2)
    	return "Erro: as listas tem números de elementos diferentes"
    for i in range(len(lista1));
    	l3.append(lista1[i])
        l3.append(lista2[i])
    return l3