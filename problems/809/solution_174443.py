# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def intercala(lista1, lista2):
    """Função que recebe duas listas de tamanho 3 e retorna outra lista, que
       é formado pelos elementos das 'lista1' e 'lista2', intercalados entre si
       parâmetros de entrada:list,list
       parâmetros de saída:list"""
    lista1=[a1,a2,a3]
    lista2=[b1,b2,b3]
    lista3=[a1,b1,a2,b2,a3,b3]
    return lista3