def intercala(lista1, lista2):
    """função que dada duas listas de tamanho 3, retorne uma nova lista intercalando os termos"""
    return (len(lista1[0]), len(lista2[0], len(lista1[1], len(lista2[1]), len(lista1[2], len(lista2[2])