def intercala(lista1, lista2):
    """função que dada duas listas de tamanho 3, retorne uma nova lista intercalando os termos"""
    
    return (str.find(lista1[0]) + str.find(lista2[0] + str.find(lista1[1] + str.find(lista2[1]) + str.find(lista1[2] + str.find(lista2[2])