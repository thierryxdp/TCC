def intercala(lista1, lista2):
    """Coloque um comentário dizendo o que a função faz e quais são os parâmetros de entrada e saída"""
    assert len(lista1) == len(second)
    n = len(lista1)
    combined = []
    for i in range(n):
        combined.append(lista1[i])
        combined.append(lista2[i])
        return combined