# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def lista(l1,l2):
    "Funcao que retorna os elementos intercalados de duas listas"
    #str, str, str -> str

    return [l1[0]]+[l2[0]]+[l1[1]]+[l2[1]]+[l1[2]]+[l2[2]]