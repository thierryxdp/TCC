def busca(m, setor):
    encontra = []
    for i in range(len(m)):
        for j in range(len(m[0])):
        	if setor in m[i][j]:
            	for j in range(len(m[0])):
                	if j == 2:
                    	break
                	list.append(encontra, j)
    return encontra