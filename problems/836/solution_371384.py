def busca(setor, matriz):
    funcionarios = []
    for linha in matriz:
        for string in linha:
        	if string == setor:
            	list.append(funcionarios, linha)
    return funcionarios