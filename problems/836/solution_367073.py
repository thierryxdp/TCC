def busca(matriz,nome):
    l=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[0])):
            if str.lower(string) in str.lower(lista[i][0]):
                list.append(l,lista[i])
    return l