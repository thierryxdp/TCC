def busca(setor, matriz):
    contador = 0
    retorno = []
    for len(matriz) > contador:
        for a in matriz:
            if setor in matriz:
                list.append(returno, setor)
        contador = contador + 1
    return retorno