def busca(setor,M):
    lista=[]
    for i in range(len(M)):
        for j in range(len(M[0])):
        	if setor in M[i][j]:        
                list.append(lista,setor)            
    return lista