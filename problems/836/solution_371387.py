def busca(setor, matriz):
    funcionarios = []
    for linha in matriz:
        for string in linha:
        	if string == setor:
            	list.append(funcionarios, list.remove(linha,setor))
    return funcionarios