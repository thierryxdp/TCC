def busca (setor,matriz):
    '''c'''
    resp=[]
    resp2=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[i])):
            if setor in (len(matriz[j])):
                list.append(resp,i)
    return resp