def busca(s,m):
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    lista_vazia=[]
    for i in range(qtd_linhas):
        for j in range(qtd_colunas):
            if s == qtd_colunas[0][2] and qtd_colunas[2][2]:
                lista_vazia= qtd_linhas[0]-qtd_colunas[0][2]
    return lista_vazia