def busca(setor,matriz):
    """..."""
    resp = []
    
    for str[i] in range(len(matriz)):
        for str[j] in range(len(matriz[0])):
            if matriz[i][j] == setor:
                return True
            if matriz