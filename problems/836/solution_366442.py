def busca(setor,matriz):
    ''' '''
    lista = []
    for s in range(len(matriz):
        if setor == matriz[s][2]:
            list.remove(matriz[s],setor)
            lista = lista + matriz[s]
    return lista