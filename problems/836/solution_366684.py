def busca(lista_funcionarios,setor):
    resultado=[]
    for i range(0,len(lista_funcionarios)):
        if setor in lista_funcionarios[i]:
            list.append(resultado,lista_funcionarios[i])
        else:
            return resultado
    return resultado