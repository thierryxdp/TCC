def busca(s,m):
    '''funçao que recebe uma matriz e busca por setor; str->list'''
    x=[]
    for setor in range(len(z)):
        if y==z[setor][2]:
            list.remove(z[setor],y)
            list.append(x,z[setor])
    return x