def busca(m, setor):
    encontra = []
    for i in range(len(m)):
        if str(setor) in m[i]:
            for j in range(len(m[0])):
                if str(setor)!=m[i][j]
                	list.extend(encontra, m[i])
                
                    
    return encontra