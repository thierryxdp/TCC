def busca(setor, matriz):
    contador = 0
    lista = []
    for i in range(len(matriz)):
        if matriz[i][2] == setor:
            list.append(lista, matriz[i])
            list.remove(lista[contador], setor)
            contador += 1
    return lista