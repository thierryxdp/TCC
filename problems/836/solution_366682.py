def busca(lista_funcionarios,setor):
    lf=lista_funcionarios
    resultado=[]
    for i range(0,len(lf)):
        if setor in lf[i]:
            list.append(resultado,lf)
        else:
            return resultado
    return resultado