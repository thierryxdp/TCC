def busca(setor,M):
    """coment"""
    lista=[]
    linhas=len(M)
    for linha in range(linhas):
        if M[linha][2]==setor:
            while linha<len(M):
                list.append(lista,M[linha])
                linha+=1
    return lista