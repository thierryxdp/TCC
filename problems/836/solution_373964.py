def busca(s,m):
    qtd_linhas=len(m)
    qtd_colunas=len(m[0])
    lista_vazia=[]
    for i in range(qtd_linhas):
        for j in range(qtd_colunas):
            if s== qtd_colunas[1][2]:
                lista_vazia= qtd_colunas[1][0],qtd_colunas[1][1],qtd_colunas[1][3]
    return lista_vazia