def busca(setor,matriz):=->
    """ dado um setor, busca os dados dos funcionarios que ali trabalham;str,list->list"""
    resposta=[]
    i=0
    while(i<len(lista)):
        if string in str.join(" ",lista[i]):
            list.append(resposta,lista[i])
        i+=1        
    return [resposta-resposta[2]]