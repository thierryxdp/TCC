def busca(x,matriz):
    ret=[]
    for i in range(len(matriz)):
        linha=[]
        if(matriz[i][2]==x):
            list.append(linha, matriz[i][0])
            list.append(linha, matriz[i][1])
            list.append(linha, matriz[i][3])
        	list.append(ret, linha)
    return ret