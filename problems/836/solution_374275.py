def busca(setor,matriz):
    m = matriz
    s = 'setor'
    registro = 0
    for s in range(0,len(matriz[0])):
        registro = (matriz[s])
    for s in range(2,len(matriz[0])):
        registro2 = list.append(registro,matriz[s])
        return registro2