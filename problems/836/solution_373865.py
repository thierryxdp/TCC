def busca(m, setor):
    encontra = []
    for i in range(len(m)):
        for j in range(len(m[0])):
        	if str(setor) in m[i][j]:
                	list.append(encontra, m[i][0])
                    list.append(encontra, m[i][1])
                    list.append(encontra, m[i][3])
                    
    return encontra