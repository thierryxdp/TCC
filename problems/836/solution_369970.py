def busca(nome,matriz):
    x=[]
    y=0
    for a in matriz:
        for b in a:
            if b==nome:
         		list.append(x,matriz[y])
                list.remove(x[y],nome)
    	y+=1
    
    return x