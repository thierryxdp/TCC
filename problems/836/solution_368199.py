def busca(setor,lista):
    "Funcao que retorna lista de funcionarios de um setor"
    m=[]
    for i in range(len(setor)):
        n=[]
        for j in range(len(lista)):
            if lista[i][2] == setor:
                n.append(lista[0],lista[1],lista[3])
            m.append(n)
        	return m
        else:
            return '[]'