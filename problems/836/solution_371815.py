def busca(matriz):
    lista = []
    for contador in matriz:
        for contagem in contador:
            if "Contabilidade" in matriz[contador][contagem]:
                list.append(lista, matriz[contador][contagem])
	return lista