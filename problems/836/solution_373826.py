def busca(matriz,setor):
    resultado = []
    n = len(matriz)
    for i in range(n):
        matriz[i][2] = j
        if(j=setor):
            resultado.append([matriz[i][0],matriz[i][1],matriz[i][3]])
    return resultado