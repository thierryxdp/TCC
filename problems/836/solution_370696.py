def busca(funcionarios, setor):
    """Função que busca dentro de uma lista(contatos), os contatos que possuem
    um dado nome em parte de seu nome.
    lista, str --> lista"""
    resultadoBusca = []
    
		for apelido in range(len(funcionarios)):
            if str(numero) in str(contatos[apelido][chip]):
                list.append(resultadoBusca,funcionarios[apelido])

    return resultadoBusca