def busca(setor,matriz):
    """Função que recebe uma matriz e faz uma busca
       por setor e retorna os dados de todos os 
       funcionários daquele setor.
       str,list->list"""
    busca = []
    for linha in range(0,len(matriz)):
        for coluna in range(0,len(matriz[linha])):
            matriz[linha][coluna] == setor:
                dados = matriz[linha]
                dados.append(dados)
    return busca