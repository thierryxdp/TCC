def busca(setor,matriz):
    linha = 0
    coluna = 0
    posicao=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[i])):
            if matriz[i][j] == setor:
                list.append(posicao,i)
            	linha = linha + i
            	
            
                    
    return posicao