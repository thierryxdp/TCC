def busca(setor, matriz):
    pessoas_setor = []
    for l in matriz:
        for dados in matriz[i]:
            if setor == matriz[l][2]:
                pessoas_setor.append(matriz[i])
    return pessoas_setor