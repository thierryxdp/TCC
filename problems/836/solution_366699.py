def busca(setor,matriz):
    ''' função que recebe uma matriz e faz uma busca por setor; list->list '''
    lista[]
    for i in range (len(matriz)):
        if setor == matriz [i][5]:
            list.remove(matriz[i],setor)
            lista = lista + [matriz[i]]
    return lista