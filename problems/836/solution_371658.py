def busca(pessoa,setor):
    lista1=[]
    for i in range(len(pessoa)):
        if setor in pessoa[i]:
            resulta=[pessoa[i][0],pessoa[i][1],pessoa[i][3]]
            list.append(lista,resulta)       
    return lista1