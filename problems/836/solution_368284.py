def busca(setor,M):
    lista=[]
    for i in range(len(M)):
        for j in range(len(M[0])):
        	if setor in M[i][j]:        
                list.append(lista,M[i]) 
                list.pop(lista,setor,0)
    return lista