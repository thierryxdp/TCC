def busca(setor,matriz):
    matris=[]
    retorno=[]
    acumulador=0
    for i in matriz:
        acumulador2=0
        for j in matriz[acumulador]:
            if setor == matriz[acumulador][acumulador2]:
                lista+=matriz[acumulador]
                list.remove(matris,setor)
                retorno+=matris
            acumulador2+=1
            return acumulador 1