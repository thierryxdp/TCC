def busca(setor, matriz):
    contador = 0
    retorno = []
    while len(matriz) > contador:
        for a in matriz:
            if setor in matriz:
                list.append(returno, matriz[contador])
        contador = contador + 1
    return retorno