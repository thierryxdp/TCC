def busca(setor,matriz):
    lista = []
    matrizes = range(len(matriz))
    for i in matrizes:
        local == matriz[i][2]
        if setor == local:
            lista = list.append(lista,matriz[i])
    return lista