def busca(setor, matriz):
    '''b'''
    registros = []
    lista_setor = []
    list.append(lista_setor, setor)
    linha = len(matriz)
    for i in range(linha):
        if setor == matriz[i][2]:
            list.append(registros, [matriz[i][0],matriz[i][1],matriz[i][3]])
    return registros