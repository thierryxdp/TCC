def busca(setor,matriz):
    lista = []
    for i in list(range(len(matriz))):
        for j in list(range(len(matriz[0]))):
            if str(setor) in matriz[i][j]:
                list.append(lista, matriz[i])
    			list.dell(lista, matriz[i][2])
    return lista