def busca(funcao, matriz):
    funcionarios = []
    for dados in range(len(matriz)):
        if matriz[dados][2] == funcionarios):
            list.remove(matriz[dados], funcao)
            list.append(funcinarios, matriz[dados])
    return funcionarios