def busca(st,mt):
    '''Função que retorna os dados de todos os funcionários
    daquele setor de entrada.
    st=str,mt=list->list'''
    p=0
    tt=[]
    for p in range(len(mt)):
        if st in mt[p]:
            del mt[x][2]
            tt=tt+[mt[p]
    return tt