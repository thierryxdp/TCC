def busca(setor,matriz):
    '''dado o setor e a matriz, retorna os dados do funcionário
    string, lista(lista) -> lista(lista)'''
    funcionarios = []
    for i in range(len(matriz)):
        if A[i][2] == setor:
            list.append(funcionarios, A[i])
    return funcionarios