def acima_da_media(lista):
    proximo=0
    novalista=[]
    list.sort(lista)
    lista
    lista=lista[::-1]
    while len(novalista)!=len(lista)//2:
        novalista=novalista+[lista[proximo]]
        proximo=proximo+1
    return novalista