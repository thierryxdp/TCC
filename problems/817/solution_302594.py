def media(lista):
    l=len(lista)
    p=sum(lista)/l
    lista.insert(0,p)
    lista.sort()
    indice=lista.index(p)
    if max(lista) == p
        return []
    else:
        if max != p
            return lista[indice+1:]