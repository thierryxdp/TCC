def acima_da_media(listaNota):
    x = sum(listaNota)/len(listaNota)
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    return listaNota