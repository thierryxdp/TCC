def acima_da_media (x):
    soma = sum(x)
    quantidade = len(x)
    media = soma / quantidade
    x.append(media)
    x.sort()
    valorcentral + 0.1 = x.index(media)
    return x[valorcentral + 1:]