def acima_da_media(listaNota):
    x = sum(listaNota)/len(listaNota)
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if len(listaNota) > 1 and listaNota[-2] < x:
       del listaNota[-2]
    if len(listaNota) > 1 and listaNota[-2] < x:
       del listaNota[-2]
    if len(listaNota) > 1 and listaNota[-2] < x:
       del listaNota[-2]
    if len(listaNota) > 1 and listaNota[-2] < x:
       del listaNota[-2]
    if len(listaNota) > 2 and listaNota[-3] < x:
       del listaNota[-3]
    if len(listaNota) > 2 and listaNota[-3] < x:
       del listaNota[-3]
    if len(listaNota) > 2 and listaNota[-3] < x:
       del listaNota[-3]
    if len(listaNota) > 2 and listaNota[-3] < x:
       del listaNota[-3]
    if len(listaNota) > 3 and listaNota[-4] < x:
       del listaNota[-4]
    if len(listaNota) > 3 and listaNota[-4] < x:
       del listaNota[-4]
    if len(listaNota) > 3 and listaNota[-4] < x:
       del listaNota[-4]
    if len(listaNota) > 3 and listaNota[-4] < x:
       del listaNota[-4]
    if len(listaNota) > 3 and listaNota[-4] < x:
       del listaNota[-4]
    if len(listaNota) > 4 and listaNota[-5] < x:
       del listaNota[-5]
    if len(listaNota) > 4 and listaNota[-5] < x:
       del listaNota[-5]
    if len(listaNota) > 4 and listaNota[-5] < x:
       del listaNota[-5]
    if len(listaNota) > 4 and listaNota[-5] < x:
       del listaNota[-5]
    if len(listaNota) > 5 and listaNota[-6] < x:
       del listaNota[-6]
    if len(listaNota) > 5 and listaNota[-6] < x:
       del listaNota[-6]
    if len(listaNota) > 5 and listaNota[-6] < x:
       del listaNota[-6]
    if len(listaNota) > 5 and listaNota[-6] < x:
       del listaNota[-6]
    if len(listaNota) > 6 and listaNota[-7] < x:
       del listaNota[-7]
    if len(listaNota) > 6 and listaNota[-7] < x:
       del listaNota[-7]
    if len(listaNota) > 6 and listaNota[-7] < x:
       del listaNota[-7]
    if len(listaNota) > 6 and listaNota[-7] < x:
       del listaNota[-7]
    if len(listaNota) > 7 and listaNota[-8] < x:
       del listaNota[-8]
    if len(listaNota) > 7 and listaNota[-8] < x:
       del listaNota[-8]
    if len(listaNota) > 7 and listaNota[-8] < x:
       del listaNota[-8]
    if len(listaNota) > 7 and listaNota[-8] < x:
       del listaNota[-8]
    list.sort(listaNota)
    return listaNota