def acima_da_media(notas):
    '''Recebe uma lista com as notas dos alunos e retorna uma lista ordenada com as
    notas que ficaram acima da média: lista -> lista'''
    def maiores(lista,n):
        list.append(lista,n)
        list.sort(lista)
        a = list.index(lista,n)
        return lista[(a+1):]
    m = (sum(notas))/(len(notas)
    return maiores(notas,m)