def acima_da_media(lista):
    """Função que fornece uma lista de números maiores que n"""
    n = int(sum(notas)/int(len(notas)
    list.append(lista,n)
    list.sort(lista)
    return lista[list.index(lista,n)+ 1:]