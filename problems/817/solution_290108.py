def acima_da_media(notas,media):
    list.sort(notas)
    media= sum(notas_/len(notas)
    ident=list.index(notas,media)
    lista=notas[ident+1:]
    return lista
    lista2= lista[:]+[n]
    y=list.sort(lista2)
    ident= list.index(lista2,n)
    
    return lista2[ident+1:]