def maiores(notas, n):
	list.append(notass, n)
	list.sort(notas)
    posicao = int((list.index(notas, n))
    del notas[:posicao + 1]

def acima_da_media(notas):
    '''
    '''
    
    media = sum(notas)/len(notas)
    maiores(notas, media)
    return(notas)