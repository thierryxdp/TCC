def acima_da_media(listaNota):
    x = sum(listaNota)/len(listaNota)
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-1] < x:
       del listaNota[-1]
    if listaNota[-2] < x:
       del listaNota[-2]
    if listaNota[-2] < x:
       del listaNota[-2]
    if listaNota[-2] < x:
       del listaNota[-2]
    if listaNota[-2] < x:
       del listaNota[-2]
    if listaNota[-3] < x:
       del listaNota[-3]
    if listaNota[-3] < x:
       del listaNota[-3]
    if listaNota[-3] < x:
       del listaNota[-3]
    if listaNota[-3] < x:
       del listaNota[-3]
    if listaNota[-4] < x:
       del listaNota[-4]
    if listaNota[-4] < x:
       del listaNota[-4]
    if listaNota[-4] < x:
       del listaNota[-4]
    if listaNota[-4] < x:
       del listaNota[-4]
    if listaNota[-4] < x:
       del listaNota[-4]
    if listaNota[-5] < x:
       del listaNota[-5]
    if listaNota[-5] < x:
       del listaNota[-5]
    if listaNota[-5] < x:
       del listaNota[-5]
    if listaNota[-5] < x:
       del listaNota[-5]
    if listaNota[-6] < x:
       del listaNota[-6]
    if listaNota[-6] < x:
       del listaNota[-6]
    if listaNota[-6] < x:
       del listaNota[-6]
    if listaNota[-6] < x:
       del listaNota[-6]
    list.sort(listaNota)
    return listaNota