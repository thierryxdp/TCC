def acima_da_media(lista):
    media = sum(lista)/len(lista)
    texto = list.append(lista,media)
    texto = list.sort(texto)
    #posicao = list.index(lista,media) + list.count(lista,media)
    return texto