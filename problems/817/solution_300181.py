def acima_da_media(lista):
    '''Função que retorna as notas que ficaram acima da média.
    list->list'''
    
    list.sort(lista)
    a<=5
    z= list.index(lista,5)
    y= list.index(lista,a)
    if 5 in lista:
        del lista[:z]
        return lista
    else:
        del lista[:y]
        return lista