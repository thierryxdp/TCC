def bolos(A, B, C):
    y = A / 2
    x = B / 3
    z = C / 5
    if y == x == z:
       return int(y)
    elif y == x < z:
        return (z)
    elif y > x == z:
        return (y)
    elif y == z < x:
        return (x)