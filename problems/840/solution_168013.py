def bolos(a,b,c):
    """calcula quantos bolos serão feitos com a quantidade dada de trigo(a),ovos(b), leite(c)
    Entrada: int
    Saída: int"""
    a= resultado a//2,resultado b=b//3,resultado c=c//5
    return min(a,b,c)