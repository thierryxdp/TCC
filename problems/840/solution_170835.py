def bolos (a,b,c):
    "calcula a quantidade maxima de bolos possiveis para fazer com os ingredientes dados (int,int,int->int)"
    x=
    if min(a/2,b/3,c/5)<=max(a/2,b/3,c/5):
        return int(min(a/2,b/3,c/5))
    else:
        return int(min(a/2,b/3,c/5)-1)