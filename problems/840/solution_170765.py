import math
def bolos(a,b,c):
    """função que tem como parâmetros a quantidade "a" de xíc. de farinha, "b" de ovos e "c" de colheres de sopa de leite, que calcula e retorna a quantidade exata de bolos que se pode fazer com esses ingredientes"""
    if ((a/2==int),(b/3==int),(c/5==int)):
                            return min(int((a/2==int),(b/3==int),(c/5==int)))
                            else return (0)