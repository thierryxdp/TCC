import math
def bolos(a,b,c):
    """calcula e retorna a maior quantidade possivel de bolos a serem feitos a partir da quantidade disponibilizada de xicaras de farinha de trigo(a), ovos(b) e colheres de sopa de leite(c)"""
    ingredientes=a,b,c
    return min(ingredientes)//2 if min(ingredientes)=a
    return min(ingredientes)//3 if min(ingredientes)=b
    return min(ingredientes)//5 if min(ingredientes)=c