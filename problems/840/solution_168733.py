def bolos(A,B,C,x=2,o=3,l=5):
         """
        Retorna a média dos pesos ponderados
        : param A: float
        : param B: float
        : param C: float
        : param x: float
        : param o: float
        : param l: float
        : return: float
    """
    """
        Retorna a média dos pesos ponderados
        float, float, float, float -> float
    """
    """
        Retorna a média dos pesos ponderados
        Parâmetros:
            entrada:
                qtd1,qtd2,p1,p2: float
            saída:
                float
    """
    return(A*x+B*o+C*l)/(x+o+l)