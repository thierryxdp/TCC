def inverte(x):
    """inverte as palavras de uma frase
    x->frase
    str->str"""
    if len(x)==47:
        str.replace(x[0:12],',',' ')+str.replace(x[12:],'-',' ')==y
        str.lower(y)==y
        str.split(y)==y
        y[::-1]==y
        str.join(y)==y
        return y