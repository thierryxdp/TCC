def inverte(x):
'''x'''
If '!' in x:

x=str.replace(x,'!',' ')

If'?' in x:
x=str.replace(x,'?' ,' ')

If '.' in x:

x=str.replace(x,'.',' ')

If '...' in x:

x=str.replace(x,'...',' ')

If ':' in x:

x=str.replace(x,':',' ')

If ';' in x:

x=str.replace(x,';',' ')

If '-' in x:

x=str.replace(x,'-',' ')

Y=x.slip( )

List1=list.reverse(y)

B=' '.join(y)

Return str.lower(b)