def inverte (x:str):
    if '.'or'!'or'?'or'-'or','or';'or':' in x:
        return = ''.join(return list.reverse(str.split(str.lower(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(x,'.',''),'!',''),'?',''),'-',' '),',',''),':',''),';','')))))
    else:
        return x