def remove(x):
    
    
    return str.lower(str.replace(str.replace(str.replace(str.replace(str.replace(x,',',' '),'...',' '),'.',' '),'?',' '),'!',' '))

def inverte(remove(x)):
    
    y = str.split(x)
    
    list.reverse(y)
    
    return str.join(' ',y)