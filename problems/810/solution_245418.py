def inverte (frase):
    '''Função que retira a pontuação da frase, inverte o sentido e coloca tudo em letras minúculas;
    str -> str'''
    
    pontuacao = ('-',',',':',';','.','!','?')
    
#primeiro retirar a pontuação:
    
    if pontuacao in frase:
        str.lower(msg)
        msg = str.replace(frase,('-',',',':',';','.','!','?'),' ') 
    
    
    str.reverse(lista)
    lista = msg.split()
    
    return str.reverse(lista)