def retira_pontuacao(frase):
    """função que retorna uma frase onde os caracteres de pontuação foram substituídos por espaços, sendo dada a frase
    str--->str""""
    n = str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(frase,"."," "),"!"," "),"?"," "),","," "),"-"," "),":"," "),";"," ")
    return n