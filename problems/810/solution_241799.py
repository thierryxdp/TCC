def inverte(f):
    f.split(f)
    """retirar as pontuações da frase de entrada f, usando o mesmo código do exercício anterior e, em seguida, inverter a ordem das palavras desa mesma frase"""
    
    f=f.replace('!',' ')
    f=f.replace('?',' ')
    f=f.replace('.',' ')
    f=f.replace(';',' ')
    f=f.replace(',',' ')
    str.lower('')
    f.reverse()
    return f