def conta_frases(texto):
    """..."""
    
    if texto.replace('...','.'):
    if texto.replace('?','.'):
    if texto.replace('!','.'):
    if texto.replace('.','.'):
        if str.split(texto,'.'):
            return texto.count('.')