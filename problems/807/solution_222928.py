def conta_frases(frase):
    ponto = frase.count(".")
    excla = frase.count("!")
    inter = frase.count("?")
    reti = frase.count("...")
    if reti == n:
    return ponto = ponto -n*3
    return ponto + excla + inter + reti