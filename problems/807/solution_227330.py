def conta_frases(frase):
   if len(frase.replace('...','.'))
   return (frase.split('.'))
   if len(frase.replace('?','.'))
   return (frase.split('.'))
   if len(frase.replace('!','.'))
   return (frase.split('.'))