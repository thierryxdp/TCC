def conta_frases(x,y,t,u):
    '''comentario
    str->int'''
    resposta==()
    ponto==(len(str.split(x,'.')))-1
    exclamacao==(len(str.split(y,'!')))-1
    interrogação==(len(str.split(t,'?')))-1
    reticencias==(len(str.split(u,'...')))-1
    return ponto+exclamacao+interrogacao+reticecias