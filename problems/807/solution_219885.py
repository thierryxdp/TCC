def conta_frases(frase):
    if frase.split('.'):
        frase = frase
        if frase.split('!'):
            frase = frase
            if frase.split('?'):
                frase = frase
                if frase.spli('...'):
                    frase = frase
                    return len(frase)