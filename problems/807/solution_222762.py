def conta_frases(texto):
    """retorna quantas frases há no texto dado."""
    str.partition(texto, '.')
    str.partition(texto, '!')
    str.partition(texto, '?')
    str.partition(texto, '...')
    return texto