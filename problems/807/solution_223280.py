def conta_frases(texto):
    """..."""
    
    if '...' in texto:
        return str.replace("...",".")
    if str.replace("texto","...","."):
         return str.count(texto,'!') + str.count(texto,'?') + str.count(texto,'...') + str.count(texto,'.')
    else:
        return str.count(texto,'!') + str.count(texto,'?') + str.count(texto,'.')