def conta_frases(texto):
    '''função que conta o numero de frases que aparecem em um  texto;
    str -> int'''
    if frase1 == str.replace(texto,'.',''):
        
        if frase2 == str.replace(frase1,'!',''):
        
    if frase3 == str.replace(frase2,'?',''):
        
    if frase4 == str.replace(frase3,'...',''):
        
    if frase5 == str.replace(frase4,';',''):
        
    else:
        x = str.count(frase5)
        return x