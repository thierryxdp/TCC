def conta_frases(texto):
    """..."""
    
    if texto.replace('!','.'):
        if texto.replace('?','.'):
            if texto.replace('...','.'):
                if texto.replace('.','.'):
                    return texto.count(texto.split('.'))