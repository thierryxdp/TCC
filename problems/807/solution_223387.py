def conta_frases(texto):
    """..."""
    if '...' in texto: 
        return str.count(texto,'...') + str.count(texto, '!') + str.count(texto, '?') + str.count9texto, '.')
    else:
        return str.count(texto,'!') + str.count(texto, '?') + str.count(texto, '.')