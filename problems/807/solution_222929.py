def conta_frases(frase):
    ponto = frase.count(".")
    excla = frase.count("!")
    inter = frase.count("?")
    reti = frase.count("...")
    if reti == n:
    return (ponto-n*3) + excla + inter + reti