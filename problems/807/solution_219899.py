def conta_frases(texto):
    str(texto) = texto.split(".")
    str(texto) = texto.split("!")
    str(texto) = texto.split("?")
    str(texto) = texto.split("...")
    numero = len(texto)
    return numero