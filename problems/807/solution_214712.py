def conta_frases(texto):
    """Conta o número de frases que aparecem no texto, que é o parâmetro
       str --> int"""
    return texto = texto.count(texto,".") + texto.count(texto,"...") + texto.count(texto,"?") + texto.count(texto,"!")