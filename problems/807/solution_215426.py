def conta_frases(frase):
    '''função que retorna a quantidade de frases que existe de entrada, tal
    que o separador de cada frase são os pontos: final, exclamação, interrogação 
    reticências, tal que quando separados os pontos de exclamação e interregação
    não serão repetidos;
    string->int'''
    a = str.split(frase)
    b = len(b)
    return b