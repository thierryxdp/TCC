def conta_frases (frase):
    '''Conta a quantidade de frases, str->int'''
    frase = str.split(fase
    x = frase.count ("aula...") + frase.count ("tarde...")
    frase = list(frase)
    return frase.count ('.') + frase.count('!') + frase.count('?') - 2 * x