def conta_frases(frase):
    
    if tamanho=str.split(frase,'.'):
        return len(tamanho)
    
    if tamanho=str.split(frase,'?'):
        return len(tamanho)
    
    if tamanho=str.split(frase,'!'):
        return len(tamanho)
    if tamanho=str.split(frase,'...'):
        return len(tamanho)