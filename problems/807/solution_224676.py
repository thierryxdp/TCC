# Conta o número de frases
# periodo
# str->int
def conta_frases(periodo):
    """Função que calcula e retorna a quantidade de frases do período(valor de entrada)"""
    """str->int"""
    if "..." in periodo
    return str.count(periodo,".") + str.count(periodo,"!")+str.count(periodo,"?")+str.count(periodo,"...")*-3
	else:
    return str.count(periodo,".") + str.count(periodo,"!")+str.count(periodo,"?")