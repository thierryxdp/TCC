def conta_frase(texto):
    '''Funcao que retorna o numero de frases
    que aparecem em um texto
    Dados de entrada: str
    Valor de saida: int
    '''
    total_frase = str.split(texto,'.'), str.split(texto,'!'), str.split(texto,'?'), str.split(testo,'...')
    return len(total_frase)