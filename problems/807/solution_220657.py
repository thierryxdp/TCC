def conta_frases(frase:str)->int:
    '''Funcao que retorna numero de frases que aparecem no texto'''
    str.index
    return str.count(frase,'.',0,str.index(frase,'!'))+str.count(frase,'!',0)+str.count(frase,'?',0)+str.count(frase,'.',str.index(frase,'?'),'.')