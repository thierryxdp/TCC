def conta_frases (texto):
    "reticencias" == "..."
    if "reticencias":
        return "reticencias"/3
    str.count(texto, ".")
    str.count(texto,"!")
    str.count(texto, "?")
    str.count(texto, "...")
    return str.count(texto, ".") + str.count(texto,"!") + str.count(texto, "?") + str.count(texto, "...")