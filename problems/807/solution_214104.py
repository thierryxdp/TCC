def conta_frases(texto):
    """Funçao que conta o numero de frases que aparecem no texto"""
    if "!" in texto:
        lista=[str.replace(texto,"!","/")
               if "?" in texto:
               lista=[str.replace(lista,"?","/")
                      if lista=[str.replace(lista,"...","/")
                                  if lista=[str.replace(lista,".","/")
                                              return str.split(lista,"/")