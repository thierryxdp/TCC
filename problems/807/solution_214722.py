def conta_frases(texto):
    """Conta o número de frases que aparecem no texto, que é o parâmetro
       str --> int"""
    a = str.count(texto,".")
    b = str.count(texto,"...")
    c = str.count(texto,"?")
    d = str.count(texto,"!")
    if a > 3
    a = str.count(texto,".") -2
    return a + b + c + d