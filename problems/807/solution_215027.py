def conta_frases (texto):
    """Conta quantas frases possui um texto, na forma 
    string.
    str -> int"""
    str.strip (texto, ".") = a
    str.strip (a, "!") = b
    str.strip (b, "?") = c
    str.strip (c, "...") = d
    str.split (d, " ") = textopronto
    return len (textopronto)