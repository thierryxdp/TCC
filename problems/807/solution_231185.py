def conta_frases(x):
    ponto = str.count(x,'.')
    exc = str.count(x,'!')
    retcs = str.count(x,'...')
    interrog = str.count(x,'?')
    if ponto !=1:
    ponto==0
    x = int(ponto + exc + retcs + interrog)
    return x