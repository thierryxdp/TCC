def conta_frases(texto):
    """..."""
    
        if str.replace('...','.')
        if str.replace('?','.')
        if str.replace(texto,'!','.')
        if str.replace(texto,'.','.')
        return texto.split('.')