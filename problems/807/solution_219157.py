def conta_frases(frases: str) -> int:
    """dhjgdfjg"""
    
  	frase = frase.replace("...", "A")
    frase = frase.replace("?", "A")
    frase = frase.replace(".", "A")
    frase = frase.replace("!", "A")
    return frase