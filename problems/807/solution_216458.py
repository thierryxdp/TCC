def conta_frases(frase):
    ponto = '.'
    x = frase.count(ponto)+frase.count("!")+frase.count("?")
    return (x)
    if (frase.count(ponto)+frase.count("!")+frase.count("?")) = 6:
        print (4)