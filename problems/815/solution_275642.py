def insere(lista_numero,n):
    """retorna a inserção de um número inteiro n na lista_numero,da forma que ela continue ordenada; list,int-->list"""
    x=lista_numero.append(lista_numero,n)
    y=x.sort(lista_numero)
    return y