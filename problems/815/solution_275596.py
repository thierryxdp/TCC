def insere (lista_numero, n):
    '''Insere numa lista ordenada (1ª entrada) um número n (2ª entrada) de tal forma que a lista continue ordenada.'''
    lista = tuple (lista_numero)
    n = tuple ('n')
    lista = lista +
    lista = list (lista)
    return sorted (lista)