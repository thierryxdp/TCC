def insere(lista_numero,n):
    """A função inclui o n na lista e a retorna em ordem crescente;
	list,int -> list"""
    newlist = lista_numero
    list.append(newlist,n)
    list.sort(newlist)
    return newlist