def insere(lista_numero,n):
    """Função que retorna uma posição correta para que a lista continue
    com a forma ordenada
    assinatura: list, int -> list
    """
    list.append(lista_numero,n)
    list.sort(lista_numero)
    return lista_numero