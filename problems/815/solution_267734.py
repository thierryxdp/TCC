def insere(lista_numero,n):
    """A função recebe uma lista numérica ordenada de forma crescente e um número(n) na entrada e retorna a mesma lista com esse número inserido nela de forma que a mesma continue ordenada;
list,int->list"""
    list.append(lista_numero,n)
    list.sort(lista_numero)
    return lista_numero