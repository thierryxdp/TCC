def insere(lista_numero,n):
    '''Recebe uma lista de números ordenados de forma crescente e insere o valor de entrada n no local correto; list->list'''
    llist.append(lista_numero,n)
    llist.sort(lista_numero)
    return lista_numero