def insere(lista_numero, n):
    if n in lista_numero:
        return lista_numero.sort
    else:
        lista = list(list.insert(lista_numero, len(lista_numero), n))
        listaord = lista.sort
        return listaord