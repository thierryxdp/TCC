def insere(lista_numero, n):
    """essa funçao recebe uma lista e um numero inteiro n, e deve retornar n na posiçao correta"""
    """entrada: list"""
    """saida: list"""
    L = list.append(lista_numero, n)
    lista nova = list.sort(L)
    return lista nova