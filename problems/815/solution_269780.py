def insere(lista_numero, n):
    """função que dada uma lista ordenada, inclue um número(n) na posição correta,
	permanecendo a lista ordenada
	list, int -> list"""
    
    lista = lista_numero
    list.append(lista, n)
    list.sort(lista)
    
    return lista