def insere(lista_numero,n):
    n=str(n)
    n=list(n)
    lista_numero=[]
    for k in lista_numero:
        return str(k)
    lista_numero=list(lista_numero)
    soma=lista_numero+adicional
    return list.sort(soma)