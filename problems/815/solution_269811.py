def insere(lista_numero,n):
    '''Função que recebe uma lista ordenada de números inteiros e um número n.
    Retorna n na posição correta para que a lista continue ordenada;
    list, int -> list'''
    list.append(lista_numero, n)
    list.sort(lista_numero)
    return lista_numero