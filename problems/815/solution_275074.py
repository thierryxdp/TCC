def insere (lista_numero,n):
    '''Dado uma lista ordenada de números inteiros 
    (lista_numero) e um número inteiro (n), a função
    deve incluir esse número na posição correta,
    mantendo a lista ordenada;
    list, int -> list'''
    
    list.append(lista_numero,n)
    
    list.sort(lista_numero)
    
    return (lista_numero)