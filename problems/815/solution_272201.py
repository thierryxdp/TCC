def insere(lista_numero, n):
    '''Dentro de uma lista de números inteiros em ordem crescente,
    essa função que coloca um número inteiro exatamente em sua posição.
    append() adiciona esse número no final da lista, mas sort() põe
    a lista de volta em ordem crescente.
    list, int >> list
    '''
    
    list.append(lista_numero, n)
    list.sort(lista_numero)
    return(lista_numero)