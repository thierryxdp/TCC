def insere(lista_numero, n):
    '''Inlcui n na posição correta, na lista_numero em ordem crescente
    float, float -> float'''
    x = lista_numero
    list.append (x, n)
    list.sort(x)
    return x