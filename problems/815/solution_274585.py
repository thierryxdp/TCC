def insere(lista_numero,n):
    '''Função que, dada uma lista ordenada de
    números inteiros e um número inteiro n, retorna
    a mesma lista adicionada de n na posição correta
    list, int -> list'''
    lista = list.append(lista_numero, n)
    return list.sort(lista)