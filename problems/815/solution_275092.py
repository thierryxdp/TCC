def insere(lista_numero, n):
    '''função em que dada uma lista ordenada (crescente) de números
    inteiros e um número (n), inclua (n) na posição correta, de tal maneira
    que a lista continue ordenada; list,int ->int'''
    list.sort(lista_numero)
    list.append(lista_numer, n)
    return lista_numero