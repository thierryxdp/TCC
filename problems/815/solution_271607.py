def insere(lista_numero,n):
    """Dada uma lista ordenada(crescente) de números inteiros e um
    número inteiro n. A função retorna a lista dada como parâmetro com
    o número n incluso na posição correta, ou seja, com a lista ainda ordenada.
    A lista da como parâmetro deve ser escrita entre colchetes[];
    	list, int --> list"""
    list.append(lista_numero,n)
    list.sort(lista_numero)
    return lista_numero