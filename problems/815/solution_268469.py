def insere(lista_numero, n):
    ''' Retorna uma lista de numeros ordenados com o novo número n, inserido mantendo
        a ordem.
        list, int -> list'''
	list.append(lista_numero, n)
	list.sort(lista_numero)
	return lista_numero