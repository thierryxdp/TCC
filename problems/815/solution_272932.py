def insere(lista_numero,n):
    '''inserir um elemento novo na lisata ordenada e manter a ordem;
    list -> list'''
  
        lista = list.sort(lista_numero)
    
    return (list.append(lista_numero,n))