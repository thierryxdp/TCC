def insere(lista_numero, n):
    if n in lista_numero:
        return lista_numero.sort
    else:
        indice = len(lista_numero)
        listaord = lista_numero.sort
        lista = listaord.insert(n - 1, n)
        return lista