def insere(lista_numero,n):
    inteiro=list(str(n))
    lista_numero_nova=lista_numero+inteiro
    list.sort(lista_numero_nova)
    for c in lista_numero_nova:
        return int(c)
 	return lista_numero_nova