def insere(lista_numero,n):
    """dada uma lista ordenada(crescente) de números inteiros e n, retorna a lista com n na
posição correta para que a lista continue ordenada.
lista,int->lista."""
    list.append(lista_numero,n)
    list.sort(lista_numero)
    return lista_numero