def insere(lista_numero,n):
    """Função que recebe um parâmetro (lista_numero) de numeros inteiros, em ordem crescente
    e um valor (n) inteiro, e retorna uma segunda lista com o valor (n) inserido
    de forma ordenada.
    list, int -> list"""
    
    list.append(lista_numero,n)
    list.sort(lista_numero)
    
    return lista_numero