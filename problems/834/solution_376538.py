def media_matriz(matriz):
    lista = []
    acumulador = 0
    for i in range(len(matriz)):
        for j in range(len(matriz[i])):
            list.append(matriz[i][j],lista)
        	acumulador = acumular + 1
    return lista