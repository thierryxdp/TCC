def media_matriz (matriz_int):
    '''Função que retorna a média de todos os números da matriz não vazia fornecida
     list(list) -> float '''
    total=0
    soma=0
    media=0
    if matriz != []:
        for ind_linha in range(0, len(matriz)):
            for ind_coluna in range(0, len(matriz[lin])):
                media = ((soma += matriz[ind_linha][ind_coluna]) /(tamanho += 1))
    return round(media, 2)