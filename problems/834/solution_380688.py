def media_matriz (matriz_inteiros):
    '''Função que retorna a média de todos os números da matriz não vazia fornecida
     list(list) -> float '''
    for linnha in range(0, len(matriz)):
        for coluna in range(0, len(matriz[lin])):
                total += len(matriz)
                soma += matriz[linha][coluna]
                media = soma / tamanho
                return round(media, 2)