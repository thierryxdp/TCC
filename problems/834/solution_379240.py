def media_matriz(matriz):
    lista=[]
    for i in range(len(matriz)):
        x=(sum(matriz[i]))/(len(matriz[i]))
        list.append(lista,x) 
    a=len(lista) 
    b=sum(lista)
    c= round(b/a,2)
    return c