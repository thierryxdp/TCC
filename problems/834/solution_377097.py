def media_matriz(matriz):
    """Recebe uma matriz de inteiros não vazia e retorna a
       média de todos os elementos da matriz
       parâmetros de entrada:list(list)
       parâmetros de saída:float"""
    linha=len(matriz)
    coluna=(matriz[0])
    for aij in linha:
        sum(matriz[0][0])/len(matriz[0][0])