def media_matriz(mat):
    lista = []
    for i in range(len(mat)):
        for j in range(len(mat[0])):
            list.append(lista,mat[i][j])
   	s = sum(lista)/len(mat)*len(mat[0])
    return round(s,2)