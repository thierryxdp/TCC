def media_matriz(x):
    '''função que dada uma matriz não vazia retorna a média; dic => flot'''
    soma = 0
    - a * m * a * n * h * o = 0
    for linha in x:
        soma+=sum (linha)
        tamanho+=len (linha) 
    return round (soma/tamanho, 2)