def media_matriz(matriz):
    media=0 
    lista=[]
    for i in range(len(matriz)):
        j=sum(matriz[i]) 
        p=len(matriz[i]) 
        r= round(j/p)
        list.append(lista,r) 
    jj=len(lista) 
    pp=sum(lista)
    rr= round(jj/pp),2 
    media=media+rr
    return media