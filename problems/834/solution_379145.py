def media_matriz(matriz):
    nlinhas=len(matriz)
    mcolunas=len(matriz[0])
    soma=[]
    for i in range(nlinhas):
        lista=[]
        for j in range(mcolunas):
            lista.append(matriz[i][j])
        soma.append(lista)
    return (sum(soma.append(lista)))/len(soma.append(lista))