def media_matriz(matriz):
    '''Calcula a media de uma matriz.list->float'''
    soma = 0
    for linha in matriz:
         for elemento in linha:
                soma+=elemeto
    x=(soma/(len(matriz)*len(matriz[0])
    return round(x,2)