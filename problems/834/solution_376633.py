def media_matriz(ma):
    """funcao que recebe uma matriz e retorna a media de todos os numeros da matriz.
    list->float."""
    lis=[]
    for i in range(len(ma)):
        for l in range(len(ma[0])):
            list.append(lis,l)
    return round(sum(lis)/len(lis),2)