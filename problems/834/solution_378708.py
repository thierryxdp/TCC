def media_matriz(matriz):
    media=0 
    lista=[]
    for i in range(len(matriz)):
        j=sum(matriz[i]) 
        p=len(matriz[i]) 
        r= round(j/p)
        list.append(lista,r) 
    jj=len(lista) 
    pp=sum(lista)
    rr= round(pp/jj),2 
    media=rr
    return media