def media_matriz(matriz):
    """Funcao calcula e retorna a media de todos os numeros da matriz;
    lista->float"""
    cont=[]
    for i in range(len(matriz)):
        for j in range(len(matriz[0])):
            list.append(cont,matrriz[i][j])
    
    return sum(cont)len(cont)