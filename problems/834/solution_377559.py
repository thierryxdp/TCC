def media_matriz(matriz):
    '''
    Funcao que tem como entrada uma matriz nao vazia e 
    retorna a media de todos os numeros dessa matriz.
    	Parametros:
        	entrada:
            	matriz : list
            saida:
            	float
    '''
    a = []
    b = []
    for i in matriz:
        a.append(sum(matriz[i])
        b.append(len(matriz[i])
    return round(a/b,2)