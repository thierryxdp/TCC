def media_matriz(matriz):
    total=0
    mediador=0
    for i in range(len(matriz)):
        mediador=mediador+matriz[i]
    	for j in range(len(matriz[j])):
            total=total+matriz[i][j]
    return total/mediador