def media_matriz(m):
    '''função que recebe uma matriz de inteiros e retorna a média de
    todos os números
    list -> float'''
    numeros = []
    for i in range(len(m)):
        for j in range(len(m[0])):
            list.append(numeros,j)
    return round(sum(numeros) / len(numeros),2)