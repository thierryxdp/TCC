def media_matriz(matriz):
    "Retorna a média de todos os números da matriz. matriz->float"
    media_matriz = []
    for i in matriz:
        list.append(media_matriz,i)
        soma = sum(media_matriz)
        tot = soma/len(media_matriz)
    return round(tot, 2)