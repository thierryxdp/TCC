def media_matriz(matriz):
    lista=[]
    soma2=0
    for i in range (len(matriz)):
        soma=0
        for j in range (len(matriz[0])):
                        soma=soma+matriz[i][j]
        list.append(lista,soma)
    for i in range(len(lista)):
                        soma2=soma2+lista[i]
    media=soma2/(len(matriz[0])*(len(matriz))
    return media