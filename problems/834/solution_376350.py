def media_matriz(matriz):
    lista1=[]
    for L in range(len(matriz)):
        for C in range(len(matriz[L])):
            list.append(lista1,matriz[L])
    total = len(matriz)*len(matriz[0])
    return sum(lista1)/total