def media_matriz(matriz):
    ''' retorna a média de todos os números da matriz;
    list(list(int))->list(list(float))'''
    
    linha=len(matriz)
    coluna=len(matriz[0])
    qtnum=linha*coluna
    
    for i in linha:
        for j in coluna:
            soma=linha+coluna

    media = soma/qtnum