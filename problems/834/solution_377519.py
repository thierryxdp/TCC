def media_matriz(matriz):
    """calcula a media dos elementos de uma matriz;
    matriz, -> int"""
    
    m = matriz
    c = []
    
    for i in range(len(m)):
        a = m[i]
        list.append(c, a)
    return sum(c)/((len(c))