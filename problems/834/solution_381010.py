def media_matriz(m):
    '''funçao que dada uma matriz de inteiros não vazia, retorna a média de todos os números da matriz; list->int'''
    x = 0
    for a in range(len(b)):
        for c in range(len(b[a])):
            x=x+b[a][c]
    media=x/(len(b)*len(b[0]))
    return round(media,2)