def media_matriz(matriz):
    """ retorna a média de todos os numeros da matriz com precisao de duas casas decimais"""
    lista=[]
    for linha in len(matriz):
        list.extend(lista,matriz[linha])
    b=sum(lista)
    c= b/(len(lista))
    return c(round,2)