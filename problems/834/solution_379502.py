def media_matriz(m):
    '''retorna a media de todos os elementos de uma matriz
    matriz -> int'''
    lista = []
    for i in m:
        for j in i:
            list.append(lista,j)
    resultado = round(sum(lista)/(len(m)*len(m[0])),2)
    return resultado