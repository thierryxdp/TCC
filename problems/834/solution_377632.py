def media_matriz(matriz):
    """
"""
    media=[]
    sequencia=0
    contagem=0
    for sequencia in range(len(matriz[sequencia])):
        
        for contagem in range(len(matriz[sequencia][contagem]))
        	media.append(matriz[sequencia][contagem])
    return media