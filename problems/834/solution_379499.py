def media_matriz(m):
    '''retorna a soma de todos os elementos de uma matriz
    matriz -> int'''
    lista = []
    for i in m:
        for j in i:
            list.append(lista,j)
    resultado = sum(lista)
    return resultado