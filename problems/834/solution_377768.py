def media_matriz(matriz):
    """funcao que calcula a media de uma matriz
    entrada matriz saida float"""
    c=range(len(matriz))
    d=range(len(matriz[0]c:))
    z=5
    i=0
    while i<len(c)
        s=sum(matriz[0])
        soma+=s
        i+=1
        return soma/z