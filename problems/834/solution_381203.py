def media_matriz(matriz):
    elen = []
    soma = 0
    med  = soma/len(elen)
    for i in range(len(matriz)):
        for j in range (matriz[i])):
            if matriz[i][j] != 0:
                elen += matriz[i][j]
    for z in range(len(elen)):
        soma += elen[s]
        return med