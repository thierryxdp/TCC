def media_matriz(matriz):
    lista=[]
    for i in range(len(matriz)):
        j=sum(matriz[i]) 
        p=len(matriz[i]) 
        r= round(j/p)
        list.append(lista,r) 
    jj=len(lista) 
    pp=sum(lista)
    rr= round(pp/jj,2)
    return rr