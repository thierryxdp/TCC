def media_matriz(matriz):
    "Retorna a média de todos os números da matriz. list->float"
    soma_matriz = sum[]
    
    for i in matriz:
        elementos = len(matriz)
        soma = sum[]
        list.append(soma_matriz, i)
        tot = soma_matriz/elementos
    return round(tot, 2)