def primo (numero):
    """Verifica se o número de entrada (que deve ser inteiro e positivo) é primo. int -> bool"""
    primos = 0
    for x in range (1, numero + 1):
        if numero % == 0:
            primos += 1
        if primos == 2:
            return true
        else:
            return false