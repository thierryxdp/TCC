def primo(numero):
    """dado um numero positivo, a função e o mumero
    	é positivo ou não: retorna valor int->bool"""
	indice=1
	lista=[]
	while indice<=numero:
		if numero%indice==0:
			lista=lista+[indice]
			indice=indice+1
	else:
		indice=indice+1
		if len(lista)==2
		return True
    else:
		return False