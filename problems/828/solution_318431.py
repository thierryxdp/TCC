primo(num):
    """funcao que dado um numero inteiro positivo, verifica se esse numero e primo
    ou nao, retornando True caso seja e False caso contrario
    int->bool"""
    lista = []
    for i in range(2,num):
        if num%i == 0:
            list.append(lista,i)
        return lista == []
        if num%i != 0:
        return lista == []