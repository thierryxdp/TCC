def primo(n):
    """Funcao calcula e retorna se um dado numero(n) e primo ou na0"""
    lp=2
    Nprimos=()
    for num in range(2,n):
        if primo(lp):
            Nprimos.append(lp)
        lp+=1
            return true
        else:
            return false