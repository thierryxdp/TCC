def fatorial(numero):
    
    """Funçao que recebe um numero e calcula o fatorial do mesmo"""
    
    proximo = número - 1
    
    while proximo > 0:
        
        if número == número:
            
            número = (número) * (proximo)
        proximo = proximo - 1
        
    return número