def fatorial (numero):
    '''...'''
    
    i=0
    r=() 
    fat = list(range(numero))
    elemento = len(list(range(numero)))
    
    while i<len(list(range(numero))):
        elemento == elemento[i] * elemento[i]
        i+=1
        
    return fat