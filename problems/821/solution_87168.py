def fatorial(numero):
    
    """Funçao que recebe um numero e calcula o fatorial do mesmo"""
    
	proximo = numero - 1
    
    while proximo > 0:
        
        if numero == numero:
            
            numero = (numero) * (proximo)
        proximo = proximo - 1
        
    return numero