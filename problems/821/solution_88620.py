def fatorial (numero):
    '''...'''
    
    i=0
    r=() 
    fat = list(range(numero))
    
    while i<len(list(range(numero))):
        if fat[i] = fat[1] * fat[i]+1:
            list.append (r,fat)