def fatorial(n):
    lista = [n]
    while n != 1:
        n = n - 1
        lista += [n,]
    i = 0
    lista = []
    while i < len(var):
        mult = var[i] * var[i+1]
        lista += [mult,]
        i = i + 1
    return mult