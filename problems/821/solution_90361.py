def fatorial(n):
    multiplica=[n,]
    i = 0
    soma = []
    while n>1:
        list.append(multiplica,n-1)
        a = multiplica[i] * multiplica[i+1]
        list.append(soma, a)
        i += 2
    return sum(soma)