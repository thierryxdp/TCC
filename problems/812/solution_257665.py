def retira_pontuacao(frase):
    """recebe uma frase e retorna outra sem as pontuações 
    	da primeira
        str -> str"""
    s = frase
    return s.replace(s.replace(s.replace(s.replace(s.replace(s.replace(s.replace(s.replace(s, "/", " "),":", " "),"|"," "),","," "),"..."," "),"?"," "),".", " "),"!"," ")