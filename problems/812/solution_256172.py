def retira_pontuacao(frase):
    '''Funcao que retorna a frase depois de
    substituir todos os caracteres de pontuacao
    e pontuacao de encerramento de frase por
    espaco.
    Dados de entrada: str
    Valor de saida: str
    '''
    frase_nova = str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace,"..."," "),"?"," "),"!"," "),"."," "),";"," "),":"," "),","," "),"-"," ")
    return frase_nova