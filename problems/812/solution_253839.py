def retira_pontuacao:
    """retorna a frase  substituindo os caracteres de pontuacao po espaco"""
    return str.replace(str.replace(str.replace(str.replace(str.replace('frase','-'),','),':'),';'),'.')