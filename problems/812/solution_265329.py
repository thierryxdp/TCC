def retira_pontuacao(frase):
    
	str.replace(frase,"."," ") and 
	str.replace(frase,";"," ") and 
	str.replace(frase,"-"," ") and
	str.replace(frase,":"," ") and 
	str.replace(frase,","," ") and 
	str.replace(frase,"!"," ") and 
	return str.replace(frase,"?"," ") and