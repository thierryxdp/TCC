def retira_pontuacao(texto):
    a = ()
    lista = str.replace(texto, "-","  " )
    lista = str.replace(lista, ",", ())
    lista = str.replace(lista, ":", a)
    lista = str.replace(lista, ";", a)
    lista = str.replace(lista, ".", a)
    
    return (lista)