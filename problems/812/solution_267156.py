def retira_pontuacao(frase):
    frase = str.del(frase,'!')
    frase = str.del(frase,'?')
    frase = str.del(frase,'.')
    frase = str.del(frase,'!')
    frase = str.del(frase,'!')
    frase = str.del(frase,'!')
    frase = str.del(frase,'!')
    return frase