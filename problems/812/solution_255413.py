def retira_pontuacao(frase):
    """dada uma frase, a função retorna a mesma frase, porém sem pontuação.
    string-->string
    
    Parâmetros
    frase: frase utilizada como entrada para retirar a pontuação"""
    return str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(str.replace(frase,'-',' '),',',' '),':',' '),';',' '),'.',' '),'?',' '),'!',' ')