def retira_pontuacao(frase):
    """Faca uma funcao que dada uma frase retorne a uma frase onde todo os
    caracteres de pontuacao tenham sido substituidos por espaco. """
    
    index = str.find(frase)
      
    if (not index == -1) and (retirar caracteres + index + size < frases)
       return frase[:index] + retirar(frase[index + size:], palavra, 0, carcatere)
    else:
        return frase[:index] + frase[index + size:]