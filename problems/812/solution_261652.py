def retira_pontuacao (texto):
    """Retorna uma frase onde todos os caracteres de pontuação tenham sido 
    substituídos por espaço. str -> str"""
    str.replace(texto, ',' , ' ' ))
    str.replace(texto, texto[-1], ' ')
    #c = str.replace(texto,':',' ')
    #d = str.replace(texto,';',' ')
    
    
    return a