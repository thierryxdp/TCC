def soma_h(N):
#Retorna o valor H de acordo com a equação dada no exercício.
#Entrada: int
#Saída: float
    """
    contagem = 0
    soma = 0
    while contagem < N:
        soma += 1/(contagem+1)
        contagem += 1
    return round(soma, 2)