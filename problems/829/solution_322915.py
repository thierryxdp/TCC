def soma_h(n):
    """
    Função que calcula o valor de h, de acordo com a fórmula dada.
    int -> float
    """
    
    divisores = list(range(1,n+1))
    soma = 0
    soma_total = 0
    
    for elemento in divisores:
        if n in divisores:
        	soma = soma + (1/divisores[i]) 
            i = i + 1
        	soma_total = 1 + soma_h
        
    return round(soma_total,2)