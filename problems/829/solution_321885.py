def soma_h(n):
    '''soma os resultados de 1 dividido por todos os númetos até N
    int -> float'''
    lista = list(range(1,n+1)
    soma = 0 
	for numero in lista:
		divisao = 1/numero
        soma = 0 + divisao
    return round(soma,2)