def soma_h(n):
    """Calcula e retorna o valor de H com N termos."""
    soma = 0
    for numeros in n :
        sum(numeros)
        soma += 1
        resultado = round(soma, 2)
    return resultado