def soma_h(n):
    """..."""
    total = []
    denominador = 1
    
    while denominador <= n:
        x = 1/denominador
        list.append(total, x)
        denominador = denominador + 1
    return round(sum(total), 2)