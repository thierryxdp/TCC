# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def hashtag(s):
    if ( len (y) / 2 == int):
                   return  k == len(y) / 2
          else:
                    return "por favor coloque uma string de numero par "
          return '#'+ y[0:k] + '#' + y[k:] + '#'