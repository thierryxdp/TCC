def hashtag(string):
    'Dada uma string, retorna a string com # no início, meio e fim. Entrada: str. Saída: str'
    meio=(len(string))//2
    meio=str(#)
    return '#'+string[0:(meio+1)]+meio+string[(meio):]# Coloque um comentário dizendo o que a função faz