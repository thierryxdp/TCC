# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def inteiro (m):
    m = (len[s])/2
    return int(m)
def hashtag(s):
     m = (len[s])/2
#calcula a metade do tamanho da string, isto é, seu índice do meio
    return '#' + s[0:int(m)] + '#' + s[int(m):] + '#'