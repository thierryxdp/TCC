# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def hashtag(s):
    '''adiciona # no início, meio e fim de uma string
    entrada:str
    saida:str'''
    var= "s";
    metade= Math.floor(string.length / 2);
    return string.substr(0) +'#'+ string.substr(0,metade) + '#' + string.substr(metade) + '#'