# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def hashtag(s):
    "Funcao que retorna o #, no inicio, no meio dele mesmo e no final"
    return '#' + texto[:len(texto)/2] + '#' + texto[len(texto)/2:] + '#'