# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def hashtag(s):
    '''funcao que recebe uma string e retorna um caractere # no inicio, meio e final dela'''
    #inicio = str1[len(str)//2]
    #final = str1[len(str)//2]
    #str = "#" + inicio + "#" + final + "#"
    str = "#" + str[len(str)//2] + "#" + str[len(str)//2] + "#"