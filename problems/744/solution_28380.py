import math
def hashtag(umastring):
    '''insere uma # no começo,no meio, e no final da string
    str -> str'''
    umastring = s
    metade = math.floor(len(s) / 2);
    return '#'+s[0:metade]+'#'+s[metade:0]+'#'