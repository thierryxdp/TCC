# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
# str-> str
def hashtag(s):
    """função que coloca # no inicio, no meio e no final da string "s"
    string->string"""
    return ultimo = None
  for atual in string:
    if atual == ultimo:
      yield 'x'
    yield atual
    ultimo = atual