def lingua_p(palavra):
    nova = []
    vogal == 'a'
    for letra in palavra:
        if letra == vogal:
            nova = nova + nova.append(vogal)
            nova1 = nova + ''.join(nova)
        else:
            nova = nova + nova.append(letra)
            nova1 = nova + ''.join(nova)

    return nova1