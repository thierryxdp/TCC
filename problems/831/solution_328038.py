def lingua_p(palavra: str) -> str:
    
    a = str.split(palavra)
    vazio = ''
    
    for letra in palavra:
        if "aeiou" in letra:
            vazio = vazio + letra + p
            
	return vazio