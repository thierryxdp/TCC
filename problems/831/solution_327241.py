def lingua_p(palavra:str) -> str:
    lista = list(palavra)
    l = palavra(x)
    for i in palavra():
        x = 0 
        if l == 'aeiouAEIOU':
            list.insert(lista, i, p)
            list.insert(lista, i+1, l)
            x +=1
    return list.join(lista)