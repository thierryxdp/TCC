def lingua_p(palavra):
    contador=0
    for letra in palavra:
        if letra in "AEIOUaeiou":
            str.join('pe',str.partition(palavra,letra)) = palavrap
            letra=letra+1
        else:
            return palavrap