def lingua_p(palavra):
    contador=0
    for letra in palavra:
        if letra in "AEIOUaeiou":
            str.partition(palavra,letra).join('pe') = palavrap
            letra=letra+1
        else:
            return palavrap