def lingua_p(palavra):
    str.split(palavra)
    i=0
    for letra in palavra:
        return palavra
        if letra.lower() in 'aeiou':
        	palavra.insert(i+1, 'p')
            i+=1
    return palavra