def lingua_p(palavra):
    contador=0
    for letra in palavra:
        if letra in "AEIOUaeiou":
            str.partition(palavra,letra)= palavra_aberta
            str.join('pe',palavra_aberta) = palavrap
            letra=letra+1
        else:
            return palavrap