def lingua_p(palavra):
    palavra = str.lower(palavra)
    lista = []
    for letra in palavra:
        if letra in 'aeiou':
            i = str.find(palavra,letra)
            list.append(lista,palavra[:i+1]
    nova_palavra = str.join('p',lista)
	return nova_palavra