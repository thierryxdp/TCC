def lingua_p(palavra):
    palavra = str.lower(palavra)
    lista = []
    for letra in palavra:
        if letra in 'aeiou':
            i = str.find(palavra,letra)
            lista = lista + list.append(lista,palavra[:i+1]
	return lista