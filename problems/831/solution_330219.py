def lingua_p(palavra):
    palavrinha = str.lower(palavra)
    
    for a in (palavrinha):
                lingua = str.replace(palavrinha,'a','apa')
   ''' for e in (palavrinha):
                lingua = str.replace(palavrinha,'e','epe')
    for i in (palavrinha):
                lingua = str.replace(palavrinha,'i','ipi')
    for o in (palavrinha):
                lingua = str.replace(palavrinha,'o','opo')
    for u in (palavrinha):
                lingua = str.replace(palavrinha,'u','upu')'''
    
    return lingua