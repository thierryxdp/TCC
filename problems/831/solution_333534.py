def lingua_p(palavra):
    c = 'a'
    lst = []
    for pos,char in enumerate(palavra):
        if(char == c):
            lst.append(pos)
        palavraa = palavra.replace([lst],'apa')
    return palavraa