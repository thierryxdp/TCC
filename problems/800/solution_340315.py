def total(lista,dicionario):
    '''retorna o preço de uma lista a se comprar'''
    
    total_pagar=[]
    
    for item in lista:
        if item in dicionario:
            list.append(total_pagar,dict.get(dicionario, item))
            
   	return round(sum(total_pagar,2)