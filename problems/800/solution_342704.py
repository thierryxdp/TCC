def total(lista, preco):
    i = 0
    n_lista= []
    for x in range(len(lista)):
        if lista[i] in dict.keys(preco):
            list.append(i, preco[lista[i]])
        i += 1
    return round(n_lista, 2)