# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def total(lista,dicio):
    """
    list, dict -> float
    """
    precos = list(dicio.keys())
    for lista[0] in palavras:
        return precos[0]