def total(lista, dicionario):
    y = 0
    x = 0
    for x in range(len(dicionario)):
        a = lista[x]
        dict.get(dicionario, dicionario[a]) = c
        y = y + c        
    x = x + 1   
    return y