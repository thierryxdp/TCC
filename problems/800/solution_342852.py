def total(lista,precos):
    '''função que recebe uma lista de compras e um dicionário contendo o preço de cada produto disponivel
    e retorna o valor total da compra.
    list,dict--->int ou float'''

    disponiveis= []
    for produto in lista:
        if dict.get(precos,produto,0)!=0:
       		 list.append(disponiveis,dict.get(precos,produtos,0)
                            
	return round(sum(dispo),2)