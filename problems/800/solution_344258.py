def total(lista,dicionario):
    """ x """
    v = 0
    for P in range(len(lista)):
        x = lista[P]
        if x in dic.keys(dicionario):
            v += dic.get(dicionario,i)
    return round(v,2)