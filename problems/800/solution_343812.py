def total(ls,dc):
    l=list(dict.keys(dc))
    m=[]
    k=0
    for i in ls:
        if i in l:
            m.append(dc[i])
    for k in range(len(m))
    	while p<=len(m):
        	m+=m[k]
        	k+=1
    	return round(m,2)