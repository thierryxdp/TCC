def total(compras, preco):
    '''retorna o valor total dos itens da lista de compras'''
    '''lista, dici -> int'''
    
    valor = []
    
    for itens in lista:
        if intes in dicionario:
            list.append(valorr, dict.get(preco,itens))
    return round (sum(valor),2)