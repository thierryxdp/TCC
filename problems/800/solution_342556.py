def total(lista_de_compras,produtos):
	type(lista_de_compras)== list
	type(produtos)==dict
	for lista in produtos.items():
		if lista_de_compras in produtos.keys()
        produtos.get(lista_de_compras) = soma
		return sum(soma)