def total(lista, dicionario):
    '''funçao que dada uma lista de compras e um dicionario com os valores de cada produto
    de cada produto que esta dsiponivel na loja, e retorna o valor das lista que esteja disponivel
    list, dict -> float'''
    x = 0
    for elementos in range (len(1)):
         m = lista[elementos]
        if m in dict.keys(dicionario)
             x = x + dict.get(dicionario,m)
    return round(x,2)