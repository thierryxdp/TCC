# Coloque um comentário dizendo o que a função faz
# Escolha nomes elucidativos para suas variáveis
def total(ls,dic): 
f=list(dict.keys(dic))    
	for ls in dic:
		if f in ls:
		a=list(dict.values(dic))
		b=sum(a)
		return round(b,2)