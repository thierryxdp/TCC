def total(lista_comp,produtos):
    total=0
    i=0
    for i in range(0,):
        if lista_comp[i] == produtos.keys():
             total=total+lista_comp[i][:]
        total.round(total,2)
        return total