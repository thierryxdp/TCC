def total(ls, d):
    """Somo os produtos especificado em /ls/
    usando o dicionário /d/ como tabela de preços.
    list, dict --> float
    """
    precos = []
    for p in ls:
        list.append(precos, 100 * dict.get(d, p, 0))
    return sum(precos) / 100