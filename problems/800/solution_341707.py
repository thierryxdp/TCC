def total(compras,catalogo):
    valor = []
    for produto in dict.keys(catalogo):
        if compras in dict.values(catalogo):
            lista = list.append(valor, dict.values(catalogo))
    return sum(valor)