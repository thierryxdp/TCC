def total (lista, dicio):
   
    valor=0
    a=list(dict.keys(dicio))
    b=list(dict.values(dicio))
    
    for chave in dicio:
        if dicio[chave]==lista:
            valor= valor
    return b